# Agent Lessons — operational memory for the autonomous loop

This is the self-learning layer. Every autonomous agent (ship, groom, review) **reads this file at the start of every run**, right after `AGENTS.md`, and **appends a new entry whenever it hits a failure or takes a healing action that a future run should know about.**

The goal is compounding: a problem solved once should never cost a full debugging pass again. `AGENTS.md` is the *contract* (what you must / mustn't do); this file is *experience* (what actually went wrong and how it was fixed).

## How to write an entry

- One entry per lesson. Keep it to 1–3 lines.
- Format: `- YYYY-MM-DD [agent/phase] SYMPTOM → CAUSE → FIX`
- Only append a **novel** lesson. Before adding, scan the entries below; if the lesson is already here, do nothing (don't re-log known failures).
- Entries ride on whatever branch you're already working (the feature branch during a ship, the PR branch during a heal, the `chore/gtm-` branch during a groom). Never push to `main` directly just to record a lesson.
- Prune only during a `groom` run, and only to merge exact duplicates. Never delete a lesson that still describes live behavior.

## Gating vs non-gating CI checks (load-bearing — read before judging mergeability)

Branch protection gates merges on THREE checks:

- `lint`
- `unit-tests`
- `e2e-tests`

`e2e-tests` (hardened by ticket `docs/backlog/0006`) runs the Playwright suite against a real local Supabase (`supabase start`) seeded by `tests/e2e/fixtures/seed.sql`. There is no `|| true` masking — a red Playwright run or a failed seed fails the job and blocks the merge. It is a real gate, treated exactly like `lint` and `unit-tests`.

**Every other check is informational and must be ignored when deciding whether a PR can merge** — including `Vercel`, `Vercel Preview Comments`, and the nightly `ai-contract-validation` / `full-test-suite` jobs. A red Vercel check never blocks a merge and is never a reason to "fix" anything.

## Entries

- 2026-05-11 [legacy-agent] The previous single-agent loop entered a retry loop producing ~20 identical commits titled "feat: push 55 local changes to remote" (batches `1/3`, `2/5`, `2b/5`, ...) within 90 minutes → the agent had nothing new to commit but kept pushing because its loop didn't gate on "is there a diff?" or "is there an open PR for my work?" → ALWAYS check `git diff --quiet HEAD` and `gh pr list --state open --base main --head feat/*` before opening another PR. If a PR is already open for the work, exit cleanly; don't pile on.
- 2026-05-10 [legacy-agent] CONTRIBUTING.md was overwritten with an empty string twice in a day (commits `9a82901` and `2d66833`) → an Edit step shipped an empty replacement → before any Write on `AGENTS.md`, `CONTRIBUTING.md`, `docs/LESSONS.md`, or `docs/backlog/README.md`, Read the file first and confirm the new content is non-empty. Never `> file` or write a blank string to these load-bearing docs.
- 2026-05-13 [legacy-agent] The previous single-agent loop included `git push origin main` in its prompt, which silently failed against branch protection for weeks → branch protection on `main` blocks direct pushes from any non-bypass identity → autonomous agents MUST use `git push -u origin HEAD` to a `feat/<ticket-id>-<slug>` branch and `gh pr create --fill`, never push to main.
- 2026-05-20 [ship] Single-PR-at-a-time gate without a healing phase causes one stuck PR to freeze ALL shipping → if step 1 exits whenever any `feat/` PR is open, a `BEHIND` or red PR no one tends will block the backlog indefinitely → tend the PR FIRST (rebase BEHIND, recover red gating check, wait if healthy) and only ship a new ticket when nothing's stuck.
- 2026-05-20 [ship] A PR with all gating checks green still won't auto-merge and shows `mergeStateStatus: BEHIND` → branch protection requires the branch to be up to date with `main`, and nothing rebases it automatically → run `gh pr update-branch <n>`; CI re-runs on the new merge commit and auto-merge fires when green.
- 2026-05-20 [ship] `Vercel` check reports FAILURE but the PR is otherwise mergeable → Vercel is NOT a branch-protection gating check → ignore its state entirely when deciding mergeability or whether to attempt a fix. Only `lint` and `unit-tests` gate (and `e2e-tests` once 0006 ships).
- 2026-05-20 [infra] First attempt to promote `e2e-tests` to a PR-gating check failed: PR #209 ran the suite without Supabase + without seeded data, and `share-flow.spec.ts` failed expecting "Alice Walker" / "E2E Test Team" rows that don't exist → CI's dummy env vars (`localhost:54321` for Supabase, `dummy` for Stripe) make data-dependent specs structurally unrunnable → reverted to `|| true` informational mode; gating promotion now lives in ticket 0006 (Harden e2e-tests for PR-gating). Do NOT re-promote until that ticket lands.
- 2026-05-20 [ship/heal] ~14 vitest tests "fail" locally (`localStorage.clear is not a function`, jsdom render tests timing out at 5s, `player-of-match` date asserting `Apr 27` vs `Apr 28`) while `main` is green → the local machine runs **Node 25** but CI pins **Node 20** (`.github/workflows/ci.yml`), and the machine TZ isn't UTC — Node 25 + jsdom breaks the native `localStorage`/timers and shifts date formatting → these are NOT regressions. Confirm by running the suspect files on a pure `origin/main` worktree under the same Node; if `main` fails identically, the local gate's vitest signal is environmental — push and let CI (Node 20) arbitrate. Never skip/weaken the tests to make them pass.
- 2026-05-20 [ship/heal] Most open `feat/*` PRs (#40,#46,#49,#50,#52,#53,#54,#56,#57,#58,#60,#65,#109) are ORPHANS of the pre-2026-05-18 history (root `f61614c`) — `git merge-base origin/main <pr>` returns nothing, so they're permanently `DIRTY` and unhealable (already escalated; humans are re-filing them, e.g. #45 merged). The strict "tend the LOWEST-numbered PR" rule would re-escalate one orphan per run forever and starve genuinely-healable PRs → treat orphans as not-tendable (skip, surface in summary) and tend the lowest PR that actually **shares an ancestor** with `main` (verify with `git merge-base`). PR #210 was such a PR: DIRTY only on the slimmed-vs-legacy `CONTRIBUTING.md`, resolved in favor of `main`.
- 2026-05-20 [ship] A ticket whose engineering notes name a new vitest file `*.spec.ts` (e.g. ticket 0001 said `tests/stripe/webhook.spec.ts`) will silently never run → `vitest.config.ts` has `exclude: ['**/*.spec.ts']` because `.spec.ts` is reserved for Playwright (`playwright.config.ts` testDir `./tests/e2e`); a `.spec.ts` under `tests/` is invisible to `npx vitest run`, so the "proof" never gates → always create vitest files as `*.test.ts` (every existing one is), regardless of the ticket's wording; note the deviation in the Implementation log.
- 2026-05-20 [ship/0002] Groomer tickets describe billing in shorthand that doesn't match the schema/API: ticket 0002 said `organizations.plan` and `canAccess(orgId, feature)`, but the real column is `organizations.tier` (no `plan` column) and `canAccess(tier: Tier, feature)` takes a tier STRING, and `/api/stripe/create-checkout` reads `interval` not `billing` → assert the REAL contract (read `src/types/database.ts`, `src/lib/tier.ts`, the route) and document the reconciliation in the Implementation log; don't invent a `plan` column or change the `canAccess` signature to match the prose.
- 2026-05-20 [ship/0002] `src/lib/stripe.ts` freezes its `PRICE_IDS` map from `process.env` AT MODULE LOAD; vitest hoists `import`s above test setup, so setting `STRIPE_PRICE_*` in `beforeEach` (or even at file top) is too late and `getPriceId()` returns `''` → in the `@/lib/stripe` mock, override `getPriceId`/`tierFromPriceId` deterministically rather than fighting env-load order; keeps the route's real logic exercised without a CI env change.
- 2026-05-20 [ship/0002] `/api/me` caches `{coach,teams}` (incl. `organizations.tier`) under `me:${user.id}` for 2 min, so a paid webhook's tier flip is masked until TTL → chose BUST-ON-WEBHOOK over a shorter TTL: a `bustOrgMeCache(admin, orgId)` helper in the webhook looks up the org's coaches and `memBust(me:${coach.id})` after every billing mutation (cache key == auth user id == `coaches.id`). Billing events are rare; `/api/me` is hot — bust the rare path, don't tax the hot read.
- 2026-05-20 [ship/0002] The "mark shipped" chore (PR #214) flipped the ticket frontmatter to `shipped` but FORGOT the `docs/backlog/README.md` index-table row, which still read `groomed` → PHASE 2 of the ship loop picks the next ticket from the INDEX TABLE, so a stale index re-picks an already-shipped ticket on the next run → when flipping a ticket to `shipped`, update BOTH the ticket frontmatter AND its index row in `README.md` (the 0001 chore #212 did both; the 0002 chore missed the index). Reconciled here in `chore/0002-sync-backlog-index`.
- 2026-05-20 [ship/0003] Local vitest won't even load `vitest.config.ts` on the loop's machine Nodes: v16 throws `node:util` has no `styleText`, and v20.13/v20.18 throw `ERR_REQUIRE_ESM` on `std-env` → the installed toolchain is vite 8 (engines `^20.19.0 || >=22.12.0`) + vitest 4, so any Node below 20.19 (or the too-new v25 from the other LESSON) is unusable → run the gate under `nvm use 20.19.0` (matches CI's `node-version: 20`, which resolves to ≥20.19 on GitHub runners); `nvm install 20.19.0` once if absent.
- 2026-05-20 [ship/0004] The webhook's `customer.subscription.updated` branch sets `tier = tierFromPriceId(item.price.id) || 'free'`, but Stripe KEEPS the price item on the subscription through retries and even after it lapses to `unpaid`/`canceled` → so the price alone always resolves to the paid tier and a dead subscription would keep its features forever, while `canAccess(tier, feature)` is status-agnostic and can't fix it → make the grace-window decision IN the webhook: gate on `sub.status` — hold the priced tier for non-terminal statuses (`active`/`past_due`/`trialing`) and force `tier:'free'` for terminal ones (`unpaid`/`canceled`/`incomplete_expired`). The tier value then IS the grace decision; `canAccess` stays a pure string lookup.
- 2026-05-20 [ship/0004] Changing a billing banner's CTA from `<Link>` to `<button>` (past-due banner now POSTs `/api/stripe/portal` + `window.location.assign(url)` instead of linking to `/settings/upgrade`) silently breaks the SIBLING ticket's component test that asserted `getByRole('link', {name:/update your payment method/i})` (0003's cancel-banner spec uses the past-due banner as its "shows instead" negative) → when you change a shared UI element's role, grep tests for the old role/name and update them in the SAME PR (link→button), documenting the intent is unchanged; never disable the sibling test to go green.
- 2026-05-20 [ship/0003] A plain `npx vitest run` of the full 89-file suite locally fails ~80 files with `[vitest-pool]: Failed to start forks worker` (only ~15 files actually run) → the constrained machine can't fork that many parallel workers under `pool:'forks'`; the handful that ran showed ONLY the known-environmental fails (date-TZ + jsdom render timeouts), never the new files → run the full suite with `npx vitest run --no-file-parallelism` to serialize files and avoid pool exhaustion (vitest 4 rejects `--poolOptions.*` CLI flags with `Unknown option`), or just run the new files in isolation and let CI's `unit-tests` job arbitrate the rest.
- 2026-05-21 [ship/0005] The `customer.subscription.created` webhook resolves the org ONLY by `eq('stripe_customer_id', sub.customer)`, which silently misses a first-time org whose `stripe_customer_id` is still null — so the new `customer_email` checkout branch (no pre-created customer) could never persist the freshly-minted customer id back from this event → stamp `subscription_data: { metadata: { org_id } }` in `create-checkout` (Stripe propagates session metadata to the SUBSCRIPTION) and add a fallback in the webhook: look up by customer id first, else by `sub.metadata.org_id`, then write `stripe_customer_id` on the update so the next checkout takes the reuse path. `checkout.session.completed` also carries the id, but the AC named `customer.subscription.created` specifically.
- 2026-05-20 [ship/0006] Turning on `supabase start` in CI for the first time applies EVERY migration to a fresh tracked DB, surfacing latent migration bugs the hosted DB never re-checks: (1) `031_volleyball_seed.sql` listed 12 sports columns but only 10 values → `ERROR: INSERT has more target columns than expressions` (SQLSTATE 42601); (2) two files shared version prefix `031` (`031_long_session_audio_v2` + `031_volleyball_seed`) → `schema_migrations_pkey` duplicate-key (SQLSTATE 23505) because the CLI keys applied migrations on the leading `<version>_` token → before promoting e2e to a real gate, ensure migration files have UNIQUE version prefixes and balanced insert column/value counts; the fix for the dup was renaming one file to `0315_…` (CLI version-sorts so it lands between 031 and 032).
- 2026-05-20 [ship/0006] The e2e gate fired auto-merge on the harness PR BEFORE e2e finished, because `e2e-tests` was (correctly) NOT yet a required check during bootstrap — required checks were still just lint+unit-tests. So a green e2e on `main` can only be reached via FOLLOW-UP PRs after the harness lands; chase the e2e failures PR-by-PR, then add `e2e-tests` to `required_status_checks.contexts` ONLY after a PR-run shows it green. Never add it before a green e2e exists or the merge gate deadlocks (the PR itself would need a check that's never passed).
- 2026-05-20 [ship/0006] A real-rendered e2e gate exposes contract gaps that network-mock-only specs never hit: `getByLabel('Email')` failed because /signup + /login had `<label>` text with no `htmlFor`/`id` (a real a11y gap — fix the PAGE, not the test), and `getByText('E2E Test Team')` was a strict-mode violation because the share page renders the team name in both the `<h1>` and the greeting sentence (target the heading by role) → when promoting mock-based specs to run against real data, expect label-association + duplicate-text failures and fix the accessible markup / tighten the locator; never weaken the assertion.
- 2026-05-20 [ship/0006] `supabase start` on a cold GitHub `ubuntu-latest` runner is highly variable: ~7-8 min typical but observed up to ~17 min (image pulls + 33 migrations + healthchecks) with no failure — don't mistake a slow `Start local Supabase` step for a hang. Capture its emitted URL/keys with `supabase status -o env --override-name api.url=NEXT_PUBLIC_SUPABASE_URL …` (more robust than parsing default output) into `$GITHUB_ENV`, seed with `psql "$SUPABASE_DB_URL" -v ON_ERROR_STOP=1`, and scope Playwright to `--project=chromium` (the workflow only installs chromium; mobile-chrome would fail to launch).
- 2026-05-21 [ship/0007] An e2e `getByRole('button', { name: 'Basketball', exact: true })` found no element on /onboarding/setup even though the page renders a "Basketball" sport card → the card `<button>` wraps TWO spans (an emoji icon `🏀` and the name), so Playwright's computed accessible name is the concatenation `"🏀 Basketball"`, not `"Basketball"` — `exact: true` therefore never matches → for icon+label buttons, match the accessible name by a substring regex (`{ name: /basketball/i }`, no `exact`) so the emoji prefix is tolerated; keep the names unique enough to stay strict-mode-safe. (`tsc`/`playwright --list` can't catch this — it's a runtime accessible-name mismatch only the seeded e2e run surfaces.)
- 2026-05-21 [ship/0009] An e2e spec mocked `/api/share/<token>` via `page.route()` and asserted rendered content, but the assertions only ever pass in CI because of the SEED, not the mock → the parent portal `/share/[token]` is a SERVER component whose `getShareData()` does a server-side `fetch` that `page.route()` (browser network layer only) does NOT intercept; verified by pointing the dev server at a reachable-but-empty Supabase and watching the "Alice Walker" assertion fail with the mock active → for server-component pages, back EVERY e2e assertion with a real `tests/e2e/fixtures/seed.sql` row (the browser mocks are vestigial for rendered HTML); make the spec's mock fixtures mirror the seed 1:1 so the constants used in assertions match the seeded rows exactly.
- 2026-05-21 [ship/0009] Seeding a `player_of_match` plan for the e2e spotlight test would have failed the `e2e-tests` job at the seed step (psql `ON_ERROR_STOP=1`) → the `plans_type_check` CHECK constraint was last touched in `009_add_newsletter_plan_type.sql` and only allows legacy types up to `'newsletter'`; it rejects `weekly_star`/`player_of_match`/`skill_challenge`/etc. that `src/app/api/ai/*` write today (the hosted DB tolerated them out-of-band, but the fresh CI DB applies the tracked constraint) → when seeding a row whose `type` is a newer enum value, grep the migrations for the table's CHECK constraint and add a numbered migration aligning it with what the routes actually insert; a stale CHECK constraint is invisible until a fresh-DB seed exercises it.
- 2026-05-21 [ship/0008] A vitest calling a no-param App Router handler as `GET(req())` failed `tsc` with `TS2554: Expected 0 arguments, but got 1` (only `npx tsc --noEmit` catches it; `npx vitest run` happily passes the extra arg) → in this Next version a route with no dynamic `params` and no body declares `export async function GET()` with ZERO parameters, so a unit test must invoke it as `GET()` (auth/cookies come from the mocked `createServerSupabase`, not a passed `Request`) → call no-arg handlers with no args in tests; only pass a `Request` when the real handler signature actually accepts one (e.g. POST bodies, or GET routes that read `request.url`). Always run `tsc --noEmit` after writing route tests, not just vitest.
- 2026-05-21 [ship/0011] A share/CTA component that forwards a URL via `navigator.share` / `navigator.clipboard` renders NO `<a href>`, so neither the vitest component test nor the seeded e2e can assert the constructed URL the usual way (`getByRole('link')` / `getAttribute('href')` find nothing) → expose the exact URL `handleShare()` forwards on the trigger button via a stable `data-share-url={appUrl}` attribute and assert that in both the component test and the Playwright spec; it's a zero-UI-change testable surface (unlike the team-card CTA, which IS a real `<a>` and can be asserted by href directly). The seeded share-flow coach reuses the SAME row as team-card-flow, so the lazily-generated code is the same deterministic `AAAAAA` — no new seed row needed when threading a referral code onto a second surface for an already-seeded coach.
- 2026-05-21 [ship/0012] `gh pr checks <n> --watch` died mid-run with `HTTP 502: 502 Bad Gateway (https://api.github.com/graphql)` while `e2e-tests` was still pending, and a bare re-`--watch` immediately re-502'd → the GraphQL endpoint `--watch` polls had a transient outage, and `--watch` aborts (doesn't retry) on a 502 → don't treat a `--watch` 502 as a CI failure; fall back to a bounded `for`-loop polling plain `gh pr checks <n>` (REST-backed, parses the `e2e-tests` row) with `sleep 30` until the gating check resolves. e2e took ~3.5 min here (Supabase + seed), well under the ~17 min LESSONS ceiling.
- 2026-05-21 [ship/0012] `gh pr create` aborted with "you must first push the current branch to a remote, or use the --head flag" even though `git push -u origin HEAD` had set up tracking → in the agent's non-interactive shell `gh` didn't resolve the current branch's tracking ref → always pass `--head <branch>` explicitly to `gh pr create` (alongside `--base main`) rather than relying on the implicit current-branch detection.
- 2026-05-21 [ship/0010] Running the gate as `nvm use 20.19.0 && <cmd>` inside the agent's bash tool exits 3 and SWALLOWS ALL output (even `which node`) — the non-interactive shell can't source nvm cleanly, so the gate looks broken when it isn't → invoke the pinned Node directly by prepending its bin to PATH for that one command: `N20="$HOME/.nvm/versions/node/v20.19.0/bin"; PATH="$N20:$PATH" npx vitest run …` (verify the binary exists with `ls "$N20/node"` first). This gives Node 20.19.0 with clean stdout and a real exit code without depending on `nvm use`.
- 2026-05-21 [ship/0013] Unit-testing a `next/og` `opengraph-image.tsx` route + an inline `generateMetadata` is awkward two ways: rendering a real `ImageResponse` drags in the satori/WASM pipeline (slow/fragile in vitest+jsdom), and `generateMetadata`'s title/description logic is buried behind a server-side `getShareData()` fetch → (1) `vi.mock('next/og', () => ({ ImageResponse: class { status=200; headers=new Headers({'content-type':'image/png'}); constructor(el,opts){…} } }))` so the route is exercised and you assert status/contentType + that it was constructed once (per the ticket: "assert status/contentType rather than pixel content"), mocking `global.fetch` per-case for the data-present / null / throwing branches; (2) extract the metadata branching into a PURE `buildShareMetadata(data, {token, appUrl})` in `src/lib/` and have `generateMetadata` delegate — now the spotlight-vs-generic title/description is unit-testable with plain objects and the generic path stays byte-identical. Image + metadata must share ONE disambiguation helper (`isMatchSpotlight`/`buildSpotlightPreview`) so they never disagree on the label.
- 2026-05-21 [ship/heal] The 2026-05-20 "orphan feat/ PRs" verdict (#40,#46,#49,#50,#52,#53,#54,#56,#57,#58,#60,#65,#109) was a SHALLOW-CLONE ARTIFACT, not reality: the loop's checkout is a shallow clone (`git rev-parse --is-shallow-repository` → true) and `git merge-base origin/main origin/<branch>` returns EMPTY on truncated history, so every PR falsely looked parentless → after `git fetch --unshallow` ALL 13 share an ancestor with main (e.g. `5f86428…`) and are healable; GitHub's `mergeStateStatus` (computed server-side on full history) was authoritative the whole time (#56 read BEHIND, not DIRTY). FIX: before ever judging a feat/ PR an orphan, `git fetch --unshallow` (or `--deepen`) FIRST and re-run merge-base, and trust GitHub's `mergeStateStatus` over a local shallow merge-base. #40 was then healed by a clean union merge — DO NOT skip these PRs as orphans.
- 2026-05-21 [ship/heal/0040] Merging a long-stale feat/ branch (pre-dating a feature) that ADDS a new query method to a shared route silently 500s NEWER sibling tests: #40 added `.from('sessions').gte().lte()` to GET /api/share/[token], and the 0009 (weekly-star) + 0011 (referral-code) tests' hand-rolled Supabase query-builder mocks implemented `gte` but not `lte`, so `chain.lte` was undefined → threw → route's try/catch returned 500 instead of 200 (only the merged tree surfaces it; each test passes on its own tip) → after such a merge, grep every test that imports the changed route and add the newly-used builder method to its mock (real supabase-js has `.lte`); it restores mock fidelity, leaves all assertions intact, and is NOT weakening a test.
- 2026-05-21 [ship/heal/#46] Merging main into a long-stale feat/ branch where BOTH sides independently added a same-named hook in NON-conflicting regions silently produces a duplicate declaration: /capture/review/page.tsx ended up with two `const { data: rosterPlayers } = useQuery(...)` (main's reassignment-picker query + the branch's coverage-chips query), which git textual-merges as two valid blocks → `TS2451 Cannot redeclare block-scoped variable` that NO conflict marker, lint, nor a partial vitest surfaces — ONLY `tsc --noEmit` does → after resolving the textual conflicts of such a catch-up merge, ALWAYS run `tsc`; when both sides added the same query/var, keep the SUPERSET one (main's `RosterPlayer[]`, incl. nickname/name_variants), repoint the consumer (the coverage `useMemo`) at it, and delete the narrower duplicate. That's restoring a single source of truth, not weakening anything.
- 2026-05-21 [ship/#18] Attribute-only src/ changes (adding aria-label to existing elements) still count as untested under test-first discipline — reviewer blocks on "N src/ files changed, 0 test files" regardless of change type → for aria-label regressions, render the actual page component (mock useRouter/useActiveTeam/fetch as needed, wrap in QueryClientProvider if required) and assert `screen.getByRole('button', { name: 'exact label' })`; one test file covering 2–3 representative labels across the changed set satisfies the discipline without one-per-file coverage.
- 2026-05-21 [ship/heal/#49] `src/app/(dashboard)/home/page.tsx` is a recurring DIRTY hotspot for the pre-agent feat/ branches (#49,#52,#54,…): each adds its own feature to the SAME three regions (the lucide-react import list, the top-of-component `useState` block, and the helper-function block before `// Core stats`), so a catch-up merge produces real conflict markers there (not silent dupes). #49 (Quick-Game: `Trophy` icon + game state + `quickStartGame()`) vs main (Quick-Observe: `CheckCircle2`/`Check`/`BarChart2` + `qo*` state + `handleHomeQuickObsSave()`/`dismissQo()`) → resolve as a UNION of both features, deduping the icons present on both sides (`X`, `Loader2`), keep main's `// Core stats` comment, then run `tsc` to confirm no duplicate declaration. Each branch's JSX lives in different parts of the file and merges cleanly on its own.
- 2026-05-21 [ship/heal/#53] Two distinct home-page features can collide on a shared `useQuery` `enabled:` PREDICATE, not just imports/state/helpers: #53 (GameDayCard) needs `playerAvailability` when an upcoming game/scrimmage/tournament exists; main needs the same query when `practiceActive`. Resolve by OR-ing both predicates into one (`todaySessions.length>0 || practiceActive || upcomingSessions.some(game/scrimmage/tournament)`) so BOTH features get their data — never pick one side, that silently disables the other's fetch. Distinguish from the superseded quick-observe branches (#15/#50/#52, escalated): #53's feature lives in its OWN new files (`components/home/game-day-card.tsx`, `lib/game-day-utils.ts`) and is purely additive to main, so it's a clean union heal, NOT an escalation — main having an unrelated quick-observe rewrite doesn't make an additive sibling a collision.
- 2026-05-21 [ship/heal/#14] The human owner can be actively pushing to the SAME feat/ branch while you heal it: mid-heal, two `fix: address review blockers` commits landed on `feat/practice-focus-mode`, so my catch-up merge (built on the old tip) was rejected on push — first `! [rejected] (fetch first)`, then `(non-fast-forward)` as the ref kept moving → NEVER force-push a feat/ branch (it would clobber the human's commits); re-fetch the branch, rebuild the merge on the CURRENT remote tip (`git checkout -B B <fetched-tip>; git merge origin/main`; re-resolve; re-gate) and push NORMALLY. A non-FF rejection is harmless — it just means re-fetch and retry. Verify `git merge-base --is-ancestor origin/B HEAD` (clean fast-forward) before every push. The same union resolution re-applies byte-for-byte on the new base.
- 2026-05-21 [ship/heal/#14] A long-stale feat/ branch's catch-up merge pulls main's NEWER jsdom selector engine (`@asamuzakjp/dom-selector`), which turns previously-tolerated invalid CSS selectors into hard `SyntaxError: Invalid selector` — `player-card.test.tsx` did `container.querySelector('.bg-orange-500/15')` (a Tailwind slash class) which old jsdom parsed leniently but the new engine rejects → CSS-escape the slash: `'.bg-orange-500\\/15'` in the JS string (= `.bg-orange-500\/15` CSS), or use `[class*="bg-orange-500/15"]`. Assertions unchanged — this restores the selector's intent, NOT weakens it. Same family as the merge-surfaces-latent-test-bugs lessons (#0040 missing mock methods, #46 dup decl): after a long-stale merge ALWAYS run the merged sibling tests under the pinned Node, not just `tsc` — tsc never sees a runtime querySelector string.
- 2026-05-21 [ship/heal/#14] After the heal mechanics above were resolved, #14's FINAL blocker was simply that it was still a DRAFT with auto-merge un-armed (`isDraft:true`, `autoMergeRequest:null`) despite `mergeStateStatus: CLEAN` + all three gating checks green + only a non-blocking `COMMENTED` review — the `auto-merge.yml` workflow that normally flips drafts→ready + arms squash never fired → PHASE 1 case (e) ("make sure auto-merge is enabled") must FIRST flip the draft: `gh pr ready N` THEN `gh pr merge N --auto --squash` (the merge-`--auto` alone no-ops on a draft); once ready, a CLEAN+green PR merges within seconds. When triaging the lowest-numbered feat/ PR, read `isDraft` alongside `mergeStateStatus` — a healthy-looking PR stuck on draft-state is a one-action heal, not a wait.
- 2026-05-22 [ship/heal/#60] The `--no-file-parallelism` workaround (LESSONS#46, used to dodge fork-worker exhaustion on the constrained machine) trades pool-exhaustion for CROSS-FILE TEST POLLUTION: serializing all 119 files into ONE worker lets an earlier file's leaked global state (fake timers, unrestored mocks, jsdom DOM) fail a LATER file's test that is clean in isolation — here `home-quick-observe-sheet.test.tsx:195` (`expect(onClose).not.toHaveBeenCalled()`) failed only inside the full serial run, yet passed 14/14 when run alone on BOTH `origin/main` and the merged branch (and main is green on CI, which runs with normal per-file isolation) → when a test fails ONLY in the `--no-file-parallelism` full-suite run, RE-RUN IT IN ISOLATION before treating it as a regression; an isolation-pass + green-on-CI-main means it's a serial-pollution artifact, not your change. CI's real per-file isolation never sees it. (Distinct from the TZ/jsdom ENV fails of LESSONS#36, which reproduce identically on pure main in isolation too — that one I confirmed by running the same file on an `origin/main` worktree.)
- 2026-05-22 [ship/heal/#60] Not every DIRTY pre-rewrite feat/ PR is an escalation: #60 (Invite-a-Coach card) is PURELY ADDITIVE at its core — 3 brand-new files (`components/home/invite-coach-card.tsx`, `lib/invite-coach-utils.ts`, the test) that main cannot have "deleted" — so it can only conflict on shared touch-points (the recurring `home/page.tsx` hotspot import+JSX regions, LESSONS#65/#66, and `CONTRIBUTING.md`). Confirmed not-superseded by checking main has NO invite-coach component (only the unshipped backlog ticket 0015) → CLEAN UNION HEAL, push and keep the loop moving. Triage shortcut: `git diff --stat <merge-base> origin/<branch>` — if the PR's deliverable is NEW files (A, not M), it's additive and union-healable; escalate only when the conflict is in code that IS the PR's deliverable AND main reshipped/deleted it.
- 2026-05-22 [ship/heal/#3] A single DIRTY pre-rewrite PR can mix HEAL-able and FAVOR-MAIN hunks per file — resolve each by whether the conflicting code is the PR's OWN deliverable or base code main changed: (1) `drills/page.tsx` LIST-page "Add to Queue" is the PR's actual additive feature, collided only with main's adjacent sport-aware category refactor → UNION (keep branch `useAppStore`+`practice-queue` imports AND main's `getDrillBuilderCategories`, drop the dead inherited `DRILL_CATEGORIES` const main already rewired). (2) `drills/[drillId]/page.tsx` DETAIL-page button was base code (merge-base `c20d5df3` ADDED it) that the branch only REFACTORED, and main no longer has it (traced to junk legacy commit `a212591e` "batch 2b/5", LESSONS#29) → FAVOR MAIN (`git checkout --theirs` the whole file; the refactor is moot, NOT the PR's deliverable, and re-adding would resurrect removed code). (3) `CONTRIBUTING.md` legacy backlog note vs main's slim → favor main (LESSONS#210). Rule: escalate (#2 timer) only when main deleted the PR's CORE feature; favor-main-and-keep-shipping when it deleted a SECONDARY surface the branch merely touched. Verify the shared util stays imported by the surviving surface so `git checkout --theirs` doesn't orphan it; `tsc` after. Distinct: my first heal built on a STALE remote-tracking ref (`git fetch origin <branch>` returned old SHA; `git ls-remote`/`fetch --force …:refs/remotes/…` gave the true tip) → after a non-FF push rejection, refresh with ls-remote, don't trust a plain fetch's cached `origin/<branch>`.
- 2026-05-22 [ship/heal/#259] A PR that ADDS a new gating step (here `scripts/check-backlog.mjs`, wired into the `lint` job by the agent-fleet PR #259) can go red NOT from its own code but from pre-existing data the new gate now enforces: 0017's ticket FILE said `in-progress` while the index said `groomed`, even though 0017's feature already merged via #257 → heal by reconciling the DATA to ground truth (set BOTH file frontmatter + README index row to the real status `shipped`), never by weakening/removing the new check. The clone is shallow with a `main`-only refspec, so `origin/<branch>` doesn't exist until you `git fetch --unshallow` + `git fetch origin <branch>:refs/remotes/origin/<branch>`; do that before any merge-base/heal-count reasoning (LESSONS#61).
- 2026-05-22 [ship] `check-backlog.mjs` only flags file≠index DISAGREEMENT — it is necessary but NOT sufficient. A ticket that shipped but whose FILE *and* index BOTH still say `groomed` (0018: feature merged via #261/`8a20547`, yet both still `groomed`) PASSES the gate, so the gate's green light does NOT mean the backlog is semantically correct → PHASE 2 picks "first FILE status == groomed", so such a ticket gets RE-PICKED and re-shipped. After a feature PR merges, a mark-shipped chore MUST flip BOTH file+index to `shipped` (cf. LESSONS#42, the inverse: index forgotten). Next ship run: before picking 0018, verify it isn't already shipped on main (grep recent commits for "ticket 0018").
- 2026-05-22 [ship/0020] When auto-merge is armed AND all three gating checks pass fast (here e2e took 3m29s), the feature PR can MERGE while you're still in `gh pr checks --watch` — by the time the watch returns, `gh pr view` shows `state:MERGED` and the `feat/` branch's tip is squashed onto main. So the loop's step-9 "update the ticket to `shipped` in a final commit ON THE SAME BRANCH" is impossible: the branch is done. FIX: after the watch returns green, check `gh pr view <N> --json state`; if `MERGED`, do the shipped-flip as a SEPARATE `chore/<id>-mark-shipped` branch off the freshly-pulled main (flip BOTH file frontmatter + README index per LESSONS#42/#74, run `node scripts/check-backlog.mjs`, open a PR) rather than trying to push to the dead feature branch. Don't push the status flip to main directly (branch protection rejects it; LESSONS#31).
- 2026-05-22 [ship/0020] The constrained machine's checkout had ZERO node_modules (`ls node_modules | wc -l` → 0) and `npx vitest` resolved a STALE broken copy from `~/.npm/_npx/<hash>` (`Cannot find module './rolldown-binding.darwin-arm64.node'`, MODULE_NOT_FOUND) → run `npm ci` under the pinned Node FIRST (`PATH="$N20:$PATH" npm ci`), then invoke the PROJECT-LOCAL binary `./node_modules/.bin/vitest` (not `npx vitest`, which can grab the poisoned npx cache). Verify with `ls node_modules/.bin/vitest` before running the gate.
- 2026-05-23 [ship/0023] A "prompt uses clipboard voice (no banned words)" contract test scans `${system}\n${user}` for the AGENTS.md banned words (journey/amazing/exciting/elevate/empower/synergy), but the prompt INSTRUCTION itself listed them to forbid them ("Banned words: journey, amazing, …") → the test caught its own forbidden tokens and failed on a prompt that was actually compliant → when an AI prompt must AVOID certain words, instruct the model positively ("write like a coach's clipboard, not a marketing landing page; avoid breathless hype words") instead of enumerating the banned tokens verbatim; a verbatim ban-list makes the prompt fail any banned-words assertion that lints the prompt string. Same family as LESSONS#0021 (a doc/instruction string colliding with the tooling that reads it).
- 2026-05-23 [ship/0023] The ticket prose said `<UpgradeGate feature="weekly_digest" …>`, but `UpgradeGate` resolves entitlement via `useTier().canAccess(feature)` → `canAccess(tier, feature)`, so the `feature` PROP must equal the TIER KEY exactly or the gate silently never unlocks for entitled coaches → pass `feature="feature_weekly_digest"` (the real tier key registered in `TIER_LIMITS`), not the ticket's shorthand, and register that same key in `FEATURE_CONFIG` for the benefit copy. Reconcile to the real `canAccess` contract (cf. LESSONS#0002), document it in the Implementation log; the `<UpgradeGate>` `feature` prop is NOT a free label — it is the tier-feature lookup key.
- 2026-05-22 [ship/0021] A test file's JSDoc header that literally writes the vitest exclude glob `**/*.spec.ts` inside the `/* … */` block makes vitest's oxc transform die with a misleading `[PARSE_ERROR] Unexpected token` at the glob's column → the `*/` substring inside `*.spec.ts` CLOSES the block comment early, so the rest of the comment parses as code → when documenting the `.test.ts`-not-`.spec.ts` rule (LESSONS#38) in a comment, never embed the raw `*/`-containing glob; phrase it as "excludes the spec glob" or escape it. The error points at the glob, not at the unbalanced comment, so it reads like a syntax bug in the test body.
- 2026-05-25 [ship/0027] An in-app share control that lives on a CONTEXTUAL surface (the `GameRecapCard` on the session page, which only renders its action row once a recap is loaded) can't be e2e-tested at a stable URL the way coach-card's control was (a fixed `/settings/referrals`) — the authed Playwright path must navigate to a seeded GAME session AND seed a `game_recap` plan tied to it (`session_id` set) so the card's `useQuery` on `plans` auto-loads the saved recap and the "Share this recap" button appears WITHOUT a live AI call; threading the loaded plan's `id` into component state (alongside `recap`) is what lets the button POST `planId` to `/api/recap-card/create`. The public-page e2e (seed-backed `game_recap_shares` row) remains the load-bearing CI coverage; the authed control test skips when E2E creds are unset (coach-card precedent), and the vitest component test (`data-share-url`) is the always-green proof of the control.
- 2026-05-22 [ship/0022] When a ticket RELOCATES an existing CTA to a second surface (0022 put the 0019 "Start your own team" self-signup link on the reaction success screen AND kept it at the page bottom — AC required both), the vitest component test PASSES (the form renders in isolation → one link) but the seeded e2e fails `strict mode violation: getByRole('link', {name:/start your own team/i}) resolved to 2 elements` because the real portal renders BOTH identical links by design → the component-isolation green is a false floor for any page-level duplicate; only the real-rendered e2e sees the collision (same family as LESSONS#50). FIX: add a stable `data-testid` on the NEW surface's actions container (a zero-UI hook, like the existing `data-share-url`) and scope the e2e to it (`page.getByTestId('reaction-success-actions').getByRole('link', …)`); never `.first()`-paper-over or weaken the assertion when both elements legitimately exist. Anticipate this whenever an AC says "ADD … and keep the existing one" — grep the spec for the shared accessible name before pushing.
- 2026-05-25 [ship/0029] An observer-page e2e asserting the host coach's FIRST name in the conversion footer via a page-wide `getByText(/E2E/)` failed CI with `strict mode violation: getByText(/E2E/) resolved to 3 elements` — the seeded coach first name "E2E" ALSO appears in the page header (team name "E2E Test Team" + the "Helping Coach E2E …" line), so the substring matches three nodes; this passed `--list` and is invisible to vitest/tsc (only the seeded real-render surfaces it, same family as LESSONS#0022/#0006) → scope the coach-name assertion to the footer's stable `data-testid` container (`page.getByTestId('observer-conversion-footer').toContainText(/E2E/)`), never a page-wide `getByText`; when the seed's coach name is a substring of the seed's team name, ANY bare name locator on a surface that also shows the team name will strict-mode-collide. The CTA `link.toBeVisible()` (line 88) passed — only the name locator was the bug, so the footer itself shipped correctly the first run.
- 2026-05-25 [ship/0029] The `e2e-tests` job failed at the `Start local Supabase` step (NOT in Playwright) with `failed to bind host port for 0.0.0.0:54322 … address already in use` on a DOCS-ONLY chore PR → a transient runner-level port conflict (a prior/parallel Supabase container hadn't released 54322), unrelated to the diff; distinct from the slow-but-succeeding `supabase start` of the earlier 0006 lessons → treat a `Start local Supabase` port-bind failure as an INFRA FLAKE, not a code regression (especially on a docs-only diff): re-run just the failed job with `gh run rerun <run-id> --failed` and re-poll. The re-run passed clean in 3m32s. Never "fix" the test for an infra port conflict.
- 2026-05-25 [ship/0028] Adding extra `coaches` rows to `tests/e2e/fixtures/seed.sql` failed the `e2e-tests` job at the `Seed test data` step (psql `ON_ERROR_STOP=1`, exit 3) with `coaches_id_fkey … Key (id)=(…102) is not present in table "users"` — `coaches.id references auth.users(id) on delete cascade` (migration 001), so EVERY seeded coach needs a matching `auth.users` row first, not just the one whose email a spec signs in as → when seeding N coaches, seed N `auth.users` rows with the SAME fixed UUIDs in the SAME idempotent block BEFORE the `insert into coaches` (the original seed already did this for the single E2E coach; it's easy to miss when adding a multi-coach fixture). `tsc`/`vitest`/Playwright `--list` never catch it — only the real fresh-DB seed under `ON_ERROR_STOP` does (same family as LESSONS#0009: a fresh-CI-DB constraint the hosted DB tolerated). Fix the SEED, never weaken the gate.
- 2026-05-25 [ship/0031] Storing a config-cascade value in `config_overrides` whose `value` column is `jsonb` (e.g. the program weekly-focus STRING): the live route writes via supabase-js `.upsert({ value: focusRaw })` (the JS client JSON-encodes the string automatically), but the e2e SEED is raw SQL under psql `ON_ERROR_STOP=1`, where a bare `'spacing & off-ball movement'::jsonb` is INVALID JSON (a jsonb string literal must itself be a quoted JSON string) and would fail the seed step → wrap the string as a JSON string literal in SQL: `'"spacing & off-ball movement"'::jsonb`. `resolveConfig` returns the decoded string verbatim either way, so the app read is unchanged; only the seed needs the explicit JSON quoting. Same fresh-DB-only family as the other seed lessons — `tsc`/`vitest` never see it.
- 2026-05-25 [ship/0033] `git commit -m "<long body>"` in the agent's non-interactive zsh SILENTLY FAILED TO COMMIT when the message contained backticks, parens, and em-dashes (e.g. "Coach this team — free", `{ programs: [] }`): zsh ran command substitution on the backtick spans (`team`/`ref`/`org` → "command not found"), then the surviving fragments were parsed as extra `git commit` pathspecs ("pathspec 'this' did not match…") and the commit aborted — but the loop almost moved on thinking it had committed → for any multi-line/special-char commit body, write it to a temp file and `git commit -F /tmp/msg.txt` (never `-m` with backticks/parens/em-dashes in the agent shell); after committing, verify with `git log --oneline -1` that the new commit is actually present before pushing. (Distinct from the empty-diff loop of LESSONS#29 — here there WAS a diff; the commit just never landed.)
- 2026-05-25 [ship/0032] A pure util test asserting `Math.ceil(elapsed / MS_PER_WEEK)` on an EXACT week boundary (earliest = `Date.now() - 42*day` captured at module load, asserted to equal 6) PASSED in isolation but FAILED in the full `--no-file-parallelism` run (got 7) → the util calls a FRESH `Date.now()` internally, so during the ~7.5-min serial suite the module-load `now` went minutes stale and `Date.now() - earliest` crept just past 42 days, tipping `ceil(6.0001)` to 7; a fast isolated run never drifted far enough to cross the boundary → for any time-elapsed assertion, (a) compute `Date.now()` at ASSERTION time, not module load, and (b) pick a fixture safely INSIDE the bucket (40 days → ceil(5.71)=6, robust until 42) instead of sitting on the exact boundary. Distinct from the TZ/jsdom env fails (LESSONS#36, which reproduce identically on main) and the serial-pollution artifact (LESSONS#70, a leaked global): this is a self-inflicted clock-drift boundary in the test's OWN fixture, and the fix is in the test, not the util.
- 2026-05-25 [ship/0034] A migration-content vitest that scans the raw `.sql` for banned tokens (a COPPA "no descriptive minor field" guard checking the new migration doesn't add `similarity`/`dob_match`/`biometric`/etc., and a "column is nullable, never NOT NULL" regex) FALSELY tripped on the migration's OWN `--` HEADER COMMENT, which legitimately NAMES those concepts to DOCUMENT what it is deliberately NOT adding ("there is no name-similarity, dob-match, biometric, or photo-match data here — only the FK") → scanning the whole file conflates documentation with DDL (same family as LESSONS#0021/#0023: a doc/instruction string colliding with the tooling that lints it) → before asserting banned-token/regex rules over a migration, strip `--` comment lines first (`sql.split('\n').filter(l => !l.trim().startsWith('--')).join('\n')`) and assert only against the executable DDL; keep the explanatory comment (it's the COPPA approval trail), just don't let the test read it.
- 2026-05-25 [ship/0036] A home-feed card that reads its data via a browser-side TanStack `useQuery` fetch IS interceptable by Playwright `page.route()` AND reflects the real seeded DB — the OPPOSITE of the LESSONS#0009 server-component caveat. The /home page is a CLIENT component (`'use client'`), so the season-wrap card's `fetch('/api/season/wrap?...')` runs in the browser network layer; in the e2e I mock only `/api/me` (to set which team is active) and let the wrap `fetch` hit the seeded `/api/season/wrap` for real, which is the load-bearing CI proof. Rule of thumb when deciding whether an e2e can trust `page.route()`: a `'use client'` page's `useQuery`/`fetch` is browser-side (mockable, and real if you DON'T mock it); only a SERVER component's server-side `getX()` fetch is invisible to `page.route()` (LESSONS#0009). For the always-green proof either way, back the assertion with a real seed row.
- 2026-05-25 [ship/0036] A one-tap "start next season" that re-creates the roster on a new season is a coordinated MULTI-STEP service-role write (update team season/current_week, read active players, bulk-insert carried rows with `prior_player_id`), so it belongs in a DEDICATED route (`/api/season/rollover`), NOT the generic `mutate()` helper — `mutate()` does ONE table op and can't express the read-then-derived-insert nor the org-ownership 404 the carry needs. The ticket prose said "uses `mutate()`"; the real contract is "a client action that goes through a server route (never a direct client Supabase write)", which the dedicated route satisfies. Carry ONLY existing player columns + `prior_player_id` (name/jersey/position/nickname/age_group); never let `date_of_birth`/`medical_notes`/`parent_*` ride forward (COPPA), and assert the insert payload keyset against an allow-list in the vitest so a future widening fails the gate.
