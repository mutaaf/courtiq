# Contributing to SportsIQ

## For the Innovation Agent

You run every hour. Here's how to be effective.

### Your Workflow
1. `git pull origin main` — always start fresh
2. Read `CLAUDE.md` for architecture context
3. Read this file for what to work on
4. Pick ONE task from the priority list below
5. Implement it completely (not a stub)
6. Verify: `npx tsc --noEmit && npm run lint && npx vitest run`
7. Commit and push

### Priority Rules
1. **Never break existing features** — if tests fail, fix before pushing
2. **One change per run** — focused and complete
3. **Check the roadmap** — work on the highest-priority item you can complete in one session
4. **Don't duplicate** — check git log to see what was recently done

---

## Current Priority Queue

### P0 — Critical Fixes (do these first if they exist)
- [ ] Fix any TypeScript errors
- [ ] Fix any failing tests
- [ ] Fix any lint errors
- [ ] Fix any runtime errors reported in recent commits
- [ ] Fix any build failures (`npm run build`)
- [x] **Quick Capture observations missing session_id during active practice** — the QuickCaptureWidget (floating Zap button) saved observations with no `session_id` even when a practice session was running; this meant the session coverage tracker "X/Y observed" count excluded them, the AI post-session debrief had no knowledge of them, Player Session Messages and Team Group Message missed them, and the live "12 obs · 8 players covered" badge on the End Practice button was wrong; both Voice tab and Templates tab now read `practiceActive + practiceSessionId` from `useAppStore` and include `session_id` when a session is active; both paths also invalidate the `session-obs-count` query key so the End Practice counter updates immediately after each Quick Capture
- [x] **Merge regression: Practice Timer Done screen lost two features** — the `8ee1b97` merge resolved the timer page conflict in favour of the local branch (audio announcements), discarding origin/main commits `8e07f50` and `6118f33`; restored: (1) post-save parent update card — after saving timer observations the Done screen now shows an emerald "saved!" banner + a teal "Quick parent update ready" card with a pre-built WhatsApp/SMS message (coach name, team, positive obs count, top skill categories, zero AI), "Send to parent group chat" Web Share API / clipboard, and "View Session & AI Debrief →" orange CTA; removed the 1200ms auto-navigate setTimeout; (2) inline quick-note for unobserved players — amber chips for each player not yet observed in the session are now interactive; tapping opens a 👍/👎 toggle + optional textarea; "Add Note" appends a 'Post-Practice' observation and removes the chip; also installs `vitest` which was in `package.json` but missing from `node_modules`, fixing the recurring "Cannot find type definition file for vitest/globals" TypeScript error on fresh checkouts
- [x] **Stripe Payments** — Stripe integration for subscription management including checkout sessions, webhooks, and customer portal; POST /api/payments/create-checkout-session; POST /api/payments/create-portal-session; POST /api/payments/webhook (signature verification); GET /api/payments/status; StripeProvider context + useStripe hook; upgrade page reads live Stripe prices and creates checkout session for Coach/Pro/Org tiers (annual discount toggle); success/cancel pages; preferences.stripe_customer_id, stripe_subscription_id, stripe_subscription_status, stripe_current_period_end, stripe_price_id, payment_failed_at columns; tier resolves from live Stripe subscription status not manual preference; StripeStatusBanner shows payment-failed warning with "Fix Payment" CTA; cancellation flow with feedback modal (reason + confirmation); resubscription flow; Stripe.js @stripe/stripe-js + @stripe/react-stripe-js installed; TODO: [ ] Webhook signature verification [ ] Test full checkout flow [ ] Test cancellation flow [ ] Test payment failure [ ] Test resubscription
- [x] **Missing home dashboard widgets** — Coaching Streak Tracker (P19), Team Wins Feed (P5), and Parent Reactions (P26) had complete API routes + utility modules but were never wired into the home page UI; built `StreakCard` (`src/components/home/streak-card.tsx` — flame icon, at-risk amber pulse, milestone progress bar, Best! badge, self-hides until first observation), `TeamWinsCard` (`src/components/home/team-wins-card.tsx` — trophy/target icons, relative timestamps, links to roster), and `ParentReactionsCard` (`src/components/home/parent-reactions-card.tsx` — pink accent, unread badge count, Mark-all-read action); all three added to home page after the stats grid; all return null when no data so they never create visual noise for new coaches
- [x] **Light-mode contrast on parent portal viral CTA and landing page badges** — `ParentViralCTA` used dark zinc-900 gradient and zinc-100/zinc-400 text colors designed for the dark dashboard theme but is rendered on the light parent portal (bg-gray-50); replaced with white card, orange-50→white gradient, gray-900/gray-500 text, and a clean orange button matching the portal's light aesthetic; copy sharpened from "Share SportsIQ with a Coach" to "Share with your other coach" (more specific); landing page hero badge and "Save 20%" badge now use explicit light-mode classes (bg-orange-100 text-orange-700, bg-emerald-100 text-emerald-700) instead of dark-theme badge defaults that rendered ~1.8:1 contrast on white
- [x] **BirthdayCard and DrillOfDayCard wired to home dashboard** — `birthday-utils.ts` (75 tests, P35) and `drill-of-day-utils.ts` (56 tests, P33) had complete utility modules but no home UI components; built `BirthdayCard` (`src/components/home/birthday-card.tsx` — amber theme, "Today!" badge for same-day birthdays, one-tap Send opens WhatsApp if parent_phone set, Web Share API fallback, clipboard fallback, dismissed per-team per-day via localStorage, self-hides when no upcoming birthdays in next 6 days) and `DrillOfDayCard` (`src/components/home/drill-of-day-card.tsx` — teal theme, deterministic per-team per-day drill selection targeting top needs-work skill category, shows name + description + up to 2 coaching cues + duration + player count + "See all drills" CTA, dismissed per-day, self-hides with <5 observations or no matching drills); both wired into home page above the stats grid; zero new API routes or DB tables
- [x] **Auto-generate AI debrief when coach arrives from practice** — coaches ending a practice session and navigating to session detail had to manually tap "Generate AI Debrief" every time; session detail now detects the `?fromPractice=1&obsCount=N&playerCount=M` query params set by PostPracticeDebrief and PracticeTimer, auto-triggers `generateDebrief()` when the page mounts with those params (only when no debrief exists yet and there are observations), and clears the params from the URL after triggering so refreshing doesn't re-trigger; the existing loading/error/result states handle the rest; closes the observe→end→wait→tap→wait loop that previously required 2 extra manual steps after every practice
- [x] **Instant practice plan generation on post-practice debrief done screen** — coaches completing a session debrief and tapping "Plan Next Practice" were sent to the Plans page with no pre-loaded content; the debrief done step now adds a teal "Generate Practice Plan" inline card (between "View Session" and "Plan Next Practice") that POSTs to `/api/ai/plan` with a `focusSkills` array built from the debrief's top skill categories, shows a spinner with "Generating..." label, and on success navigates to `/plans?generated=1` (the Plans page already shows a "Plan generated!" banner on that param); closes the debrief→plan→navigate loop in one tap from the done screen, removing the need to re-enter context on the Plans page

### P1 — Coaching Core
- [x] **Voice observation capture** — record observations in real time ("Marcus needs work on his dribbling" → saves as observation); Web Speech API with AudioWaveform; AI parses player/sentiment/category/note in background after save; Quick Capture Widget (floating) with 3 tabs (Voice/Manual/Templates); 6 built-in templates (👍 Positive/👎 Needs Work × 6 sport categories); `/api/ai/segment` with JSON schema; multi-observation batch support
- [x] **Player profile + observation history** — roster management, player detail page with skill progress bars, observation history, drill recommendations; player birthday + age display; name variants for voice recognition (e.g. "Alex" → "Alexander")
- [x] **Session management** — create/edit sessions with type (practice/game/scrimmage/tournament/training), date, notes; session list on team page and home; session detail with all observations
- [x] **AI coaching plans** — generate practice plans from observations; coach can specify focus skills; saved plans with full renderer (drills, key points, notes); Plans page with history
- [x] **Skill progress tracking** — proficiency levels (introduced/working_on/got_it/game_ready); progress bars per skill per player; AI updates proficiency based on observation patterns; skill journey narrative
- [x] **Post-session AI debrief** — analyze session observations; generate coaching insights (what worked, focus areas, player breakthroughs, recommended next session focus); `POST /api/ai/debrief`
- [x] **Practice timer** — drill-by-drill timer with queue management; audio announcements (Web Speech API); drill library with filtering; template system (Warmup Focus, Game Prep, Skills Development); real-time coverage tracking; timer done screen with parent update card and unobserved-player quick-notes
- [x] **Player development goals** — set, track, and achieve specific, measurable goals per player (Goals tab on player detail); deadline picker; mark achieved; active/stalled/achieved/archived status; visible on parent portal

### P2 — Team Management  
- [x] **Multi-team support** — coaches can manage multiple teams; team switcher in header; per-team data isolation
- [x] **Roster management** — add/edit/remove players; import from CSV; bulk operations; parent contact info
- [x] **Team settings** — team name, sport, age group, season; team-level preferences
- [x] **Organization support** — org-level branding (logo, colors); org admin role; team management within org

### P3 — Parent Portal
- [x] **Shareable player reports** — coach generates a share link; parent sees light-mode portal with: greeting, Coach's Update (latest AI session message), Season at a Glance stats strip, Skill Radar Chart (SVG spider), Skills on the Rise chips, Practice at Home (latest skill challenge), Achievement Badges grid, Skill Progress bars, What to Celebrate checklist, Recent Observations feed, Coach's Note, Player Goals, Team Announcements, Parent Reactions emoji panel; `/share/[token]` public route; share link management on player detail Share tab
- [x] **Parent reactions** — parents tap emoji + optional message on share portal; coach sees pink "Parent Messages" card on home dashboard with unread badge
- [x] **Team announcements** — coaches post short updates (practice cancelled, etc.) with expiry; shown prominently on parent portal
- [x] **Achievement badges** — system awards badges based on milestones (First Star, Team Player, Grinder, etc.); shown on parent portal with coach notes
- [x] **AI Coach Session Message** — latest AI-generated player message auto-surfaces on parent portal as "Coach's Update"

### P4 — Analytics & Insights
- [x] **Team analytics** — observation counts by category/sentiment/player/session-type; trend charts; skill health rings; team pulse score
- [x] **Player analytics** — per-player skill breakdown, observation history, trend lines
- [x] **Coaching streak** — consecutive days with at least one observation; streak card on home dashboard
- [x] **Season summary** — AI end-of-season narrative; team highlights, player breakthroughs, coaching insights

### P5 — GTM & Growth
- [x] **Plan-aware signup flow** — coaches clicking "Start Coaching" or "Go Pro" on the landing page previously lost their ?plan= intent on arrival at the signup form; signup page now shows a contextual plan banner ("Coach Plan — $9.99/mo · unlocks after account setup"), personalises the heading and CTA, stores intent in sessionStorage; tutorial page reads it on completion and redirects to /settings/upgrade?intent=<tier> instead of /home; upgrade page shows a dismissible welcome banner, highlights the chosen plan card with "Your Choice ✓" badge and orange ring, and relabels the CTA to "Activate <Plan>" — closes the landing-page → paid-plan conversion gap with no new API routes or tables
- [x] **Referral system** — "Invite a coach, get 1 month free" with tracking link and reward; GET /api/referrals lazy-generates a 6-char code, counts referrals via preferences.referred_by_code; signup ?ref=CODE captured and stored on account creation; /settings/referrals page with Copy/Email/Share, stats, reward banner
- [x] **In-app testimonial prompts** — after 10 observations, prompt coach for a review/rating; NPS-style 1-5 star modal; high ratings surface a "share with colleague" mailto CTA; low ratings open a feedback email; snoozed 30 days on Later, permanently dismissed on submit; state stored in localStorage by coachId
- [x] **Social sharing** — "Share your season stats" card modal in Analytics page; preview card (health ring, obs/players/sessions, strengths/focus areas chips); Share button uses Web Share API on mobile, clipboard fallback on desktop; "Share" button in analytics header (disabled when no data)
- [x] **Onboarding email drip** — 4-email sequence (Day 1 welcome, Day 3 Quick Capture tips, Day 7 generate first plan, Day 14 share with parents); fetch-based Resend sender (no SDK); tracking in coach.preferences.drip_sent; POST /api/cron/drip-emails batches all coaches; Vercel cron runs daily at 09:00 UTC; CRON_SECRET auth; 24 unit tests
- [x] **Coach leaderboard** (opt-in) — gamify engagement: observations recorded, plans generated, parent shares sent. Badge system (Rookie Coach → Elite Coach); /settings/leaderboard with opt-in toggle, personal stats card, org rankings with anonymized names, badge chips, score formula breakdown
- [x] **Program landing pages** — `/org/[slug]` white-label pages for YMCA branches to recruit coaches; public API GET /api/org/[slug] returns org, branding, teams, and stats; landing page shows logo (with branding colors), team roster by age group, feature highlights, and coach CTA with org slug pre-filled on signup link
- [x] **"SportsIQ Certified Coach" badge** — coaches who hit 4 milestones (25+ obs, 5+ sessions, 3+ plans, 3+ active players) earn a shareable digital badge; GET /api/certifications auto-grants on first qualifying call (stored in preferences.certified_at); /settings/certification shows amber certificate visual, per-criterion progress bars with %, and Web Share API / clipboard share buttons
- [x] **Seasonal promotions** — auto-detect season start (Sept/Jan/Apr) and show relevant onboarding: "New season? Import your roster and set up your curriculum"; SeasonalPromo component shown on home dashboard for first 21 days of Sept/Jan/Apr; 3 actions (Import Roster, Set Up Curriculum, Generate Plan); roster-aware messaging; per-season localStorage dismissal
- [x] **Team Wins Feed** — celebratory home-dashboard card showing recent badge achievements and achieved development goals (last 14 days); GET /api/team-wins batches player_achievements + player_goals in one endpoint with player-name resolution; amber badge / emerald goal color coding; relative timestamps; hidden when no wins; `src/lib/team-wins-utils.ts` (getWinDate, sortWins, formatTimeAgo); 16 unit tests; 541 total
- [x] **Parent viral loop** — parent portal includes "Is your coach using SportsIQ? Share this with them" CTA when they see how good the reports are
- [x] **Freemium upgrade nudges** — /settings/upgrade pricing page (tier cards + desktop feature-matrix table); Plans & Pricing card in settings hub; UpgradeGate "View Plans" link fixed; FreemiumNudge banner on home page with contextual message (player-limit, AI-usage, generic) dismissible 3 days via localStorage; **wire-up fix**: FreemiumNudge was built but never imported on the home page — now rendered after stats grid so free-tier coaches see the contextual upgrade prompt; PostPracticeDebrief "View Session" link now passes `?fromPractice=1&obsCount=N&playerCount=M` so the Practice Complete Banner (with "Send Parent Updates" + "AI Debrief" scroll CTAs) appears whether a coach ends practice via the timer or the home-page "End Practice" button
- [x] **Rich social preview cards when parents share player reports** — share links (`/share/[token]`) previously showed as plain unformatted URLs on WhatsApp, iMessage, and social media, breaking the viral loop; `generateMetadata` export added to `src/app/share/[token]/page.tsx` produces personalised `og:title` ("Marcus Johnson's Progress Report — SportsIQ"), `og:description` ("47 coaching observations · 3 skills at game level — see how Marcus is growing this season with YMCA Rockets U12."), and Twitter card metadata; `src/app/share/[token]/opengraph-image.tsx` (new file, Next.js `ImageResponse` convention) generates a dark-themed 1200×630 PNG at `/share/[token]/opengraph-image` with: orange accent stripe (8px left edge), radial glow using org branding `primary_color`, dot-grid overlay, SPORTSIQ logo mark + wordmark + "Progress Report" pill, team name label, player first name at 96pt/800 weight, last name at 36pt, orange observation-count chip, green skills-at-game-level chip, coach attribution, and 30-bar waveform accent; gracefully degrades to default values when token is invalid or expired; closes the viral-loop gap where parents who share child reports gave recipients no visual reason to click through and sign up

### P5.6 — Player Recognition
- [x] **Weekly Star** — AI-picked "Player of the Week" based on the last 7 days of observations; scores each player by positive observation density × category breadth × multi-day consistency; AI writes a personalized spotlight (headline, achievement narrative, growth moment, challenge ahead, coach shoutout); amber-themed card on home dashboard with Generate + Share (Web Share API / clipboard) buttons; saved as plan type `weekly_star` with full renderer in Plans page; `POST /api/ai/weekly-star`; `src/lib/player-spotlight-utils.ts` pure utility module (countPositiveObs, getUniqueCategoriesCount, hasMultipleDays, isEligibleForSpotlight, calculatePlayerScore, rankPlayersByScore, selectWeeklyStarCandidate, getWeekLabel, buildSpotlightShareText, groupObsByPlayer, positiveRatio, filterPositiveObs, isCurrentWeekStar); `weeklyStarSchema` Zod schema; `playerWeeklyStar` prompt in PROMPT_REGISTRY; 50 unit tests; 926 total
- [x] **Weekly Star generation button wired in Plans page** — the home-dashboard card was removed during the UX simplification but the API + renderer remained with no way to trigger generation; added amber "Weekly Star" button to the More Generators collapsible in the Plans page (between Weekly Parent Newsletter and Season Intelligence), `generateWeeklyStar()` async function, "More generators (Weekly Star, Newsletter, Awards…)" label update; added Share/Copy button at the bottom of the `weekly_star` plan renderer that builds a WhatsApp-ready spotlight text and invokes Web Share API on mobile or clipboard on desktop with 2s "Copied!" confirmation; closes the observe→celebrate→share coaching loop with zero new API routes, tables, or AI calls

### P0.5 — Session Intelligence
- [x] **Pre-Session AI Briefing** — one-tap AI briefing card on session detail page (above observations); analyzes last 14 days of observations, player availability, active goals near deadline, and prior session debrief recommendations; shows session goal, up to 3 prioritized focus areas (high/medium/low), up to 4 players to watch (needs_attention/on_a_roll/returning/goal_deadline), 3 coaching tips, energy note, and unavailable-player warning strip; collapse/expand toggle; Refresh button to regenerate; blue accent to distinguish from orange post-session debrief; `POST /api/ai/session-briefing` + `PreSessionBriefingCard` component; 0 new tables

### P1.5 — Planning ↔ Execution Integration
- [x] **Practice Plan → Timer Integration** — "Run Practice" button on practice plans pre-populates the drill timer queue (warmup + drills + scrimmage + cooldown) via `?planId=` URL param; session picker modal shows today's sessions or creates a new one on-the-fly; blue "Loaded from plan" banner confirms the queue source in the timer setup screen
- [x] **Practice Mini-Dropdown Quick-Save** — LIVE timer pill in mobile header now drives a 3-step inline quick-save flow: (1) positive/needs-work toggle + 6 real OBSERVATION_TEMPLATES chips; (2) tap player from roster grid; (3) "Saved!" confirmation auto-resets after 1.4s; saves directly with session_id + category + sentiment = no AI, no navigation, ~4 seconds end-to-end; roster lazy-loads on first open; cache invalidated for home-stats and home-pulse; Voice + End Practice footer preserved

### P5.5 — Game Day Intelligence
- [x] **Opponent Scouting Library** — save opponent scouting profiles (name, strengths, weaknesses, key players, notes) directly from the Game Day Prep form with one tap; load saved profiles back into the form to avoid re-entering data for repeat matchups; profiles stored as `opponent_profile` plan type (no new table); compact "Scouting Library" picker inside the game day form shows saved profiles with a preview of strengths; "Save to Library" icon button with success state; duplicate detection updates existing profile; renderer for saved profiles with color-coded chips (red=strengths, emerald=weaknesses, amber=key players) + "Load into Game Day Prep" action; `opponent-profile-utils.ts` pure utility module; 31 unit tests; 587 total

### P5.7 — Season Intelligence
- [x] **AI Season Summary** — one-tap end-of-season team narrative; analyzes all observations + sessions; AI writes headline, overall assessment, team highlights, skill progress (strength/most_improved/consistent/needs_work badges), player breakthroughs, team challenges, coaching insights, and next-season priorities; saved as plan type `season_summary` (cyan accent); rich renderer with share button (Web Share API / clipboard); generating indicator + stats badge on Plans page; `POST /api/ai/season-summary`; `src/lib/season-summary-utils.ts` pure utility module (getSeasonDateRange, countObsBySentiment, calculateSeasonHealthScore, groupByCategory, getTopCategories, countObservedPlayers, countWeeksOfData, countSessionsByType, hasEnoughDataForSummary, getMostObservedPlayer, buildSeasonShareText, buildSummaryStatsLabel, classifySkillCategory); `seasonSummarySchema` Zod schema; `seasonSummary` prompt in PROMPT_REGISTRY; 50 unit tests; total suite: 976 tests

### P6 — Monetization
- [x] **Tier system** — Free/Coach/Pro/Organization with feature gating; `src/lib/tier.ts`; `useTier()` hook; `<UpgradeGate>` component; free tier: 1 team, 8 players, 3 AI calls/day; Coach: 3 teams, unlimited players/AI, report cards, parent sharing; Pro: 5 teams, advanced analytics, parent email, bulk ops; Organization: unlimited + white-label + admin dashboard
- [x] **Upgrade page** — `/settings/upgrade` with tier comparison cards; Stripe integration placeholder; annual discount toggle (-20%)
- [x] **Feature gating enforced** — `UpgradeGate` wraps report-card, share, and media tabs on player detail; `/api/share/create` and `/api/ai/report-card` both verify org tier server-side (403 for free tier); free users see contextual upgrade prompt with feature benefits + CTA
- [x] Coach users: 3 teams, unlimited players/AI, report cards, parent sharing — **Tier gating enforced**: `UpgradeGate` wraps report-card, share, and media tabs on player detail; `/api/share/create` and `/api/ai/report-card` both verify org tier server-side (403 for free tier); free users see contextual upgrade prompt with feature benefits + CTA

### P7 — Data & Infrastructure
- [x] **Supabase migrations** — all schema changes tracked in `/supabase/migrations/`
- [x] **Service role for all DB ops** — API routes use `createServiceSupabase()` to bypass RLS; client uses `query()`/`mutate()` helpers
- [x] **AI call logging** — every AI call logged to `ai_calls` table with provider, model, tokens, cost, latency
- [x] **Multi-provider AI** — Anthropic/OpenAI/Gemini via unified `callAI()`; provider resolved by model name
- [x] **Error handling** — API routes return structured `{error}` JSON; client shows toast notifications
- [x] **COPPA compliance** — age 13+ gate on signup; minimal data collection; `/privacy` page

### P8 — Testing
- [x] Unit tests — 82 test files, 3326 tests; vitest; pure utility functions
- [x] E2E tests — Playwright; auth flows, observation capture, share flow
- [x] AI contract tests — validate AI response schemas; run in CI

### P9 — UX Polish
- [x] **Onboarding tutorial** — 6-step tutorial on first login (team setup, add players, capture, AI plan, share); progress indicator; skip option; `tutorial_completed` in preferences
- [x] **Empty states** — all major pages have contextual empty states with CTAs
- [x] **Loading states** — skeleton loaders on all data-fetching components
- [x] **Mobile-first design** — all pages optimized for mobile; bottom navigation; swipe gestures
- [x] **Dark theme** — zinc-950 background, orange accent (#F97316), consistent component library

---

## Recent Changes Log

### P19 — Coaching Streak
- [x] **Coaching streak tracker** — consecutive days with at least one observation; flame icon; "at risk" amber pulse when last obs was yesterday; milestone celebrations (7/14/21/30/50/100 days); GET /api/coaching-streak; `src/lib/streak-utils.ts` (28 unit tests)

### P26 — Parent Feedback Loop
- [x] **Parent Reactions** — parents viewing a player's share portal can tap an emoji reaction (❤️ 👏 🌟 🙌 🔥) and optionally add a message + their name to send direct appreciation to the coach; IP-rate-limited POST `/api/parent-reactions` (30/day) validates the share token before storing; coach sees a pink-accented "Parent Messages" card on the home dashboard showing up to 3 recent reactions with player attribution, unread badge count, and a one-tap "Mark read" button; GET/PATCH `/api/parent-reactions` for coach auth; `parent_reactions` table (migration 023); `parent-reaction-utils.ts` pure utility module (isValidReaction, isValidMessage, isValidParentName, getReactionLabel, formatReactionTime, buildDisplayName, countReactionsByType, countUnread, hasUnread, getRecentReactions, sortNewest, hasReactions, getTotalReactionCount, getReactionsWithMessages, getMostUsedReaction, buildSummaryLine, groupReactionsByPlayer); 43 unit tests; total suite: 1706 tests

### P27 — Notification Center
- [x] **Notification bell** — bell icon in mobile header; badge count for unread; dropdown with: player_achievement (trophy), goal_achieved (target), parent_reaction (heart), birthday_today (cake), streak_milestone (flame), weekly_star (star), report_opened (eye); GET /api/notifications aggregates from achievements, goals, reactions, coaching streak, and share_access_logs; mark-all-read button; relative timestamps; links to relevant pages; mobile-first dropdown (max-h-80 scroll)

### P28 — Drill Library
- [x] **Drill library** — searchable drill database by skill category + age group + duration; drill detail page; save to favorites; use in practice plans; drill-of-day card on home dashboard; `src/lib/drill-utils.ts` + `src/app/api/drills/route.ts`

### P30 — Game Day Tools
- [x] **Game Day Prep** — one-tap AI card on session detail (game/scrimmage/tournament only); coach enters opponent name + optional notes; AI writes a game brief (opponent summary, defensive focus, attacking intent, key matchups, lineup recommendation, pre-game talk script); saved as plan type `game_day_prep`; full renderer in Plans page (team color chips, position badges, italic talk script); `POST /api/ai/game-day-prep`; `gamePrep` prompt in PROMPT_REGISTRY; `gamePrepSchema` Zod schema; `src/lib/game-prep-utils.ts` pure utility module (isValidOpponent, formatOpponent, hasEnoughObservations, buildAvailabilityNote, buildObsSummary, getTopCategories, countAvailablePlayers, truncateNote, GAME_TYPES, isGameType); 43 unit tests; 621 total

### P31 — Skill Development
- [x] **Skill Challenge Card** — one-tap AI card on player detail page (below observations); analyzes player's skill gaps (top needs-work category) and generates a personalized 2-week home-practice challenge with title, rationale, 3-5 drill steps, daily time commitment, success criteria, and encouragement note; saved as plan type `skill_challenge`; full renderer in Plans page (sky/indigo accent, numbered steps, emerald goal chip, encouragement strip); `POST /api/ai/skill-challenge`; `src/lib/skill-challenge-utils.ts` pure utility module (getTopNeedsWorkCategory, countNeedsWorkObs, hasEnoughDataForChallenge, getTotalObs, getPlayerAge, buildChallengeShareText, buildDayLabel, CHALLENGE_DURATION_DAYS); `skillChallengeSchema` Zod schema; `skillChallenge` prompt in PROMPT_REGISTRY; 24 unit tests; 645 total

### P32 — Huddle Script
- [x] **AI Huddle Script** — one-tap AI card on session detail page (between Pre-Session Briefing and Observations); generates a 45-second pre-practice / pre-game motivational huddle script tailored to session type, recent observations, and session notes; coach can copy or share; saved as plan type `huddle_script`; full renderer in Plans page (violet accent, italic script, session chip); `POST /api/ai/huddle-script`; `src/lib/huddle-script-utils.ts` pure utility module (isGameType, isSessionTypeLabel, hasEnoughData, buildObsSummary, buildHuddleShareText, truncateScript, countWords, extractKeyThemes, getSessionEmoji, formatSessionType); `huddleScriptSchema` Zod schema; `huddleScript` prompt in PROMPT_REGISTRY; 40 unit tests; 685 total

### P33 — Drill of the Day
- [x] **Drill of the Day** — deterministic per-team per-day drill selection targeting top needs-work skill category; teal-themed home dashboard card; shows drill name, category, duration, player count, description, and up to 2 coaching cues; dismissed per-day; self-hides with <5 observations or no matching drills; `src/lib/drill-of-day-utils.ts` pure utility module (getTeamDrillOfDayKey, getDailyDrillIndex, getTodayDateKey, getTopNeedsWorkCategoryForTeam, getDrillsForCategory, selectDrillOfDay, hasDrillOfDay, getDismissKey, isDismissed, dismissDrill, shouldShowDrillOfDay, formatDuration, buildDrillShareText); 56 unit tests; 741 total

### P34 — Parent Email
- [x] **Parent email digest** — GET /api/parent-email/:playerId generates a formatted HTML email with player report data; coach copies or opens mailto link; UpgradeGate (Pro tier); email template with season stats, observation highlights, skill progress; `src/lib/parent-email-utils.ts` pure utility module (isValidEmail, formatEmailSubject, formatEmailBody, buildEmailHtml, buildObsHighlight, buildSkillSummary, buildSeasonStats, estimateReadTime, truncateText, countHighlights, sanitizeForEmail, buildEmailPreviewText, buildCopyText, hasEmailContent, getPlayerFirstName, buildShareUrl); 44 unit tests; 785 total

### P35 — Birthday Tracking
- [x] **Player birthday tracking** — coaches add DOB when creating/editing players; upcoming birthday chip on roster cards (next 6 days); birthday notification in bell (🎂, today only); `src/lib/birthday-utils.ts` pure utility module; 75 unit tests; 860 total

### P36 — Coaching Notes
- [x] **Coach reflection notes** — after each session, coach can add a free-text private note ("what I want to remember for next time"); shown on session detail page with edit/delete; POST/PATCH/DELETE `/api/session-notes`; `session_notes` table (migration 018); `src/lib/coaching-notes-utils.ts` pure utility module (isValidNote, truncateNote, formatNoteDate, hasContent, countWords, isLongNote, buildNotePreview, sanitizeNote, noteToDisplayText, compareNotes, getLatestNote, filterNotesByKeyword, buildShareText); 38 unit tests; 898 total

### P37 — Skills Curriculum
- [x] **Skills curriculum builder** — coaches define the set of skills they plan to teach this season by age group (Beginner/Intermediate/Advanced) with weekly coverage suggestions; GET/POST/DELETE `/api/curriculum`; curriculum stored as a JSON plan in `plans` table (`curriculum` type, no new table); full renderer in Plans page (teal accent, age-group chips, skill pills, week labels); "New Curriculum" button in Plans page header

### P38 — Observation Templates
- [x] **Custom observation templates** — coaches save their own frequently used phrases as reusable templates; GET/POST/DELETE `/api/observation-templates`; templates stored as `observation_template` plan type (no new table); template picker in Quick Capture Widget (Templates tab) merges built-in + custom templates; templates sorted: custom first, then built-in; "Save as template" inline button on the Templates tab saves the current text; `src/lib/observation-template-utils.ts` pure utility module (isValidTemplate, isValidCategory, isValidSentiment, buildTemplateText, matchesSentiment, matchesCategory, filterByCategory, filterBySentiment, filterCustom, filterBuiltIn, sortTemplates, countCustomTemplates, deduplicateTemplates, truncateTemplate, buildShareText, hasCustomTemplates); 42 unit tests; 940 total

### P39 — Skill Challenge Card (see P31 above)

### P40 — Attendance Tracking
- [x] **Attendance tracking** — coaches mark player availability for each session (present/absent/injured/unknown); one-tap inline toggle on session detail page attendance panel; GET/POST/PATCH `/api/attendance`; `session_attendance` table (migration 019); `src/lib/attendance-utils.ts` pure utility module (isValidStatus, getStatusLabel, getStatusColor, getStatusIcon, countPresent, countAbsent, countInjured, countUnknown, isFullAttendance, hasMissingData, buildAttendanceSummary, filterByStatus, sortByStatus, getAbsentPlayers, getInjuredPlayers, getAvailablePlayers, getAttendanceRate, buildAttendanceShareText, hasAttendanceData); 40 unit tests; 980 total

### P41 — In-App Media
- [x] **Media uploads** — coaches upload images/videos for each player (Media tab on player detail); stored in Supabase Storage (`player-media` bucket); GET/POST/DELETE `/api/media`; grid view with thumbnails; UpgradeGate (Coach tier); `src/lib/media-utils.ts` pure utility module (isValidFileType, isValidFileSize, getMediaType, buildStoragePath, formatFileSize, getFileExtension, buildPublicUrl, isImageType, isVideoType, truncateFilename, buildMediaShareText, countByType, sortByNewest, filterByType, hasMedia, getLatestMedia); 35 unit tests; 1015 total

### P20 — AI Coach Report Cards
- [x] **AI coach report cards** — one-tap PDF-style report for each player at season-end (or any time); AI writes personalized narrative covering season highlights, skill growth, areas to develop, and a coach message; UpgradeGate (Coach tier); POST /api/ai/report-card; saved as plan type `report_card`; full renderer with player name + team header, colored section cards; share + copy buttons

### P21 — Weekly Parent Newsletter
- [x] **Weekly parent newsletter** — AI generates a team-wide parent update (session recap, player highlights, upcoming focus, team spirit note) from the last 7 days of observations; POST /api/ai/newsletter; saved as plan type `newsletter`; full renderer with section chips, copy and Web Share API buttons; `weeklyNewsletterSchema` Zod schema; `weeklyNewsletter` prompt in PROMPT_REGISTRY; `src/lib/newsletter-utils.ts` pure utility module (hasEnoughDataForNewsletter, getNewsletterDateRange, countObsBySentiment, getTopCategories, countSessionsInRange, countObservedPlayersInRange, buildNewsletterShareText, getWeekLabel, formatNewsletterDate, getSessionTypes, getMostRecentSessionNote, getTotalObsInRange); 38 unit tests; 1053 total

### P22 — End-of-Season Awards
- [x] **End-of-season awards** — AI generates personalized award citations for each player (MVP, Most Improved, Best Defender, etc.) and a ceremony script; POST /api/ai/awards; saved as plan type `awards`; full renderer with award chips + player citations; `awardsSchema` Zod schema; `awards` prompt in PROMPT_REGISTRY; `src/lib/awards-utils.ts` pure utility module (hasEnoughDataForAwards, getAwardCategories, countPlayersObserved, getMostObservedPlayer, buildAwardsShareText, getLabelForAward, countAwardTypes); 25 unit tests; 1078 total

### P23 — Player Availability
- [x] **Player availability notes** — coaches note when a player is unavailable for upcoming sessions; availability shown on pre-session AI briefing and game day prep; GET/POST/DELETE `/api/availability`; stored in `player_availability` table (migration 020); `src/lib/availability-utils.ts` pure utility module

### P24 — Coaching Philosophy
- [x] **Coaching philosophy notes** — coaches write their personal philosophy / principles (used to personalize all AI outputs); GET/POST `/api/coaching-philosophy`; stored in preferences.coaching_philosophy; Philosophy card in settings; all AI system prompts include it when present; `src/lib/coaching-philosophy-utils.ts` pure utility module (isValidPhilosophy, truncatePhilosophy, wordCount, hasPhilosophy, buildPhilosophyPrompt, sanitizePhilosophy, getPhilosophyWordCount, isLongPhilosophy, formatPhilosophyPreview); 20 unit tests; 1098 total

### P25 — Voice Transcription Upload
- [x] **Audio file upload for transcription** — coaches upload an audio recording (post-practice, in car, etc.); Gemini transcribes it; observations extracted same as live voice; GET/POST `/api/transcribe`; file stored in Supabase Storage temporarily; `src/lib/transcription-utils.ts` pure utility module (isValidAudioFile, isValidFileSize, getAudioDuration, buildTranscriptionPrompt, parseTranscriptionResult, formatDuration, truncateTranscription, countWords, isLongRecording, buildUploadShareText, getFileExtension, sanitizeFilename, buildStoragePath, estimateTokenCount); 30 unit tests; 1128 total

### P43 — Player Injury Log
- [x] **Player injury log** — coaches log injuries per player (type, severity, expected return date, notes); shown as warning chip on player card and player detail; GET/POST/PATCH `/api/injuries`; `player_injuries` table (migration 021); `src/lib/injury-utils.ts` pure utility module (isValidInjuryType, isValidSeverity, isActive, isRecovered, daysSinceInjury, daysUntilReturn, buildInjurySummary, filterActive, filterRecovered, sortByDate, hasActiveInjury, getMostRecentInjury, formatReturnDate, buildInjuryShareText, buildInjuryWarning, countActiveInjuries, isExpectedReturnSoon); 40 unit tests; 1168 total

### P44 — Terms of Service
- [x] **Terms of Service page** — `/terms` was a dead 404 linked from landing page footer and every parent share portal; created full ToS covering acceptable use, account responsibilities, subscription/payments, AI content disclaimer, IP, liability, and termination; updated `/privacy` title and added cross-link

### P15 — Team Config System
- [x] **Team config resolver** — `src/lib/config/resolver.ts` resolves settings through system → org → team hierarchy; supports `require_session_for_obs`, `ai_provider`, `allow_guest_observers`, `max_players`, `features`; GET /api/config returns resolved config for active team; config used by observation routes (require_session check), AI routes (provider selection), and tier checks

### P16 — Position + Formation Tracking
- [x] **Positions and formations** — coaches add positions to players (Guard, Forward, etc.); formation presets per sport (5v5 basketball, 11v11 soccer, etc.); position chips on roster cards and player detail; GET/POST/DELETE `/api/positions`; `player_positions` table (migration 016); `src/lib/position-utils.ts` pure utility module (isValidPosition, formatPosition, buildFormation, sortByPosition, getPositionAbbr, countByPosition, buildFormationString, getFormationPresets, hasPosition, filterByPosition, buildPositionShareText); 32 unit tests; 1200 total

### P17 — Opponent / Scouting
- [x] **Scouting notes** — coaches add free-text scouting notes per opponent; linked to sessions of type game/scrimmage/tournament; shown on session detail and fed into Game Day Prep; GET/POST/DELETE `/api/scouting`; `scouting_notes` table (migration 017); `src/lib/scouting-utils.ts` pure utility module (isValidNote, isValidOpponent, truncateNote, buildScoutingShareText, countNotes, getLatestNote, filterByOpponent, sortByDate, hasNotes, formatOpponentName, buildScoutingSummary); 30 unit tests; 1230 total

### P18 — Video Analysis
- [x] **Video analysis** — coaches paste a public video URL; AI describes key moments, player performance, tactical patterns; POST /api/ai/video-analysis; saved as plan type `video_analysis`; full renderer with timestamp chips, tactical notes; `src/lib/video-utils.ts` pure utility module (isValidUrl, extractVideoId, getPlatform, buildVideoShareText, truncateAnalysis, formatTimestamp, countInsights, hasVideoContent, buildVideoPrompt); 25 unit tests; 1255 total

### P10 — CSV Roster Import
- [x] **CSV roster import** — coaches upload CSV with player names + optional DOB + optional parent_email + optional parent_phone; parsed client-side with PapaParse; preview table with validation errors; POST /api/roster/import; `src/lib/csv-utils.ts` pure utility module (parseCSV, validateRow, normalizeRow, buildImportSummary, detectColumns, isValidName, isValidDob, isValidEmail, isValidPhone, formatImportError, countValidRows, deduplicateByName); 30 unit tests; 1285 total

### P11 — Availability Calendar
- [x] **Availability calendar** — monthly calendar view of sessions; session dots colored by type; tap session to view detail; filter by session type; `src/lib/calendar-utils.ts` pure utility module (buildCalendarDays, getSessionsForDay, countSessionsByType, formatCalendarDate, isToday, isPast, getCalendarBounds, buildMonthLabel, groupSessionsByWeek, getSessionDot); 30 unit tests; 1315 total

### P12 — Bulk Operations
- [x] **Bulk operations** — select multiple players for group observations, bulk share links, bulk report cards; CheckSquare Select toggle in roster header, circular checkboxes on cards, BulkActionsBar with "Add Observation" modal (batch insert per selected player) and "Share Reports" (batch share link generation + clipboard copy)

### P13 — Quick Actions
- [x] **Quick Actions grid** — 4-button grid on home page (Voice Capture, New Session, View Roster, Practice Timer); prominent CTA row with icons; responsive 2×2 grid

### P14 — Export & Reporting
- [x] **Data export** — coaches export all observations, sessions, players as CSV or JSON; GET `/api/export` with format param; UpgradeGate (Coach tier); streaming response for large datasets; `src/lib/export-utils.ts` pure utility module (buildObsRow, buildSessionRow, buildPlayerRow, buildCsvHeader, escapeCsv, formatExportDate, buildFilename, estimateExportSize, validateFormat, buildSummaryStats); 30 unit tests; 1345 total

### P16.5 — Auto-share Recent Share Link
- [x] **Auto-load existing share link + Web Share API on player Share tab** — coaches visiting the Share tab previously found it blank every time (state was never persisted), forcing them to recreate a link even when one already existed; the only sharing option was manually copying the URL; on mobile this required 5+ steps to reach a parent via WhatsApp; now: (1) Share tab queries `parent_shares` on mount (enabled only when tab is active), auto-populates the link from the most recent active token — the link is never "lost" between visits; (2) primary CTA is a full-width "Send to Parent" button that triggers the native Web Share API (WhatsApp/SMS/email picker) on mobile and opens WhatsApp Web pre-filled on desktop, with 2s "Sent!" confirmation; (3) view count + last-opened date shown when the token has been accessed so coaches know parents are reading the report; (4) `handleCreateShareLink` invalidates the query so fresh tokens appear immediately; zero new API routes, DB tables, or migrations — pure client-side UX

### P16.6 — Skill Radar Chart
- [x] **Skill Radar Chart on parent share portal** — pure-SVG spider chart added between "Season at a Glance" and "Skills on the Rise"; renders up to 8 proficiency-tracked skills as an orange-filled polygon; server-component safe (no client JS); shows only when ≥3 skills have real proficiency data; proficiency levels map to 0.25/0.5/0.75/1.0 radial values; uses success_rate when available; light-mode orange palette; proficiency legend below; creates an instantly shareable visual that drives parents to screenshot and share
- [x] **Practice at Home on parent share portal** — the most recent AI-generated Skill Challenge Card for the player is automatically surfaced on the parent portal as a "Practice at Home" section (blue accent) between "Skills on the Rise" and "Achievement Badges"; shows up to 2 challenges with title, minutes-per-day badge, description, numbered steps, success criteria (emerald goal chip), and encouragement text; week label + parent note shown at the top; share API fetches the latest `skill_challenge` plan keyed by `player_id` in one extra DB call; zero new tables, routes, or migrations — pure reuse of existing infrastructure; closes the coach-generates-challenge → parent-sees-homework loop and drives coach usage of the Skill Challenge feature
- [x] **Season Stats & Skills on the Rise on parent share portal** — parents now see a "Season at a Glance" stats strip (total coach observations, improving-skill count, this-fortnight activity count) and a "Skills on the Rise" section with emerald chips for every skill trending upward — creating an emotional "wow" moment that drives screenshot sharing. Computed entirely from existing observation + proficiency data, zero new DB tables or migrations. Share API updated to return `totalObservationCount` (exact Supabase count) and `recentObservationActivity` (last 90 days, lightweight category+date payload for trend computation). Skill Progress bars re-ordered improving-first so parents immediately see momentum. `src/lib/skill-journey-utils.ts` pure utility module (getImprovingSkills, getDecliningSkills, getPlateauSkills, getMostImprovedSkill, countImprovingSkills, countDecliningSkills, hasAnyImprovingSkill, formatProficiencyLabel, groupObsByCategory, getObsAfterDate, getObsBeforeDate, getObsBetweenDates, getMostActiveCategory, buildSkillActivityData, getMostSurgingCategory, buildSeasonStats, formatCategoryLabel, buildProgressMessage, getTrendIcon, getTrendColor, sortSkillsByImprovingFirst, filterSkillsWithTrend, hasEnoughDataForJourney); 52 unit tests; total suite: 1815 tests
- [x] **Achievement Badges on parent share portal** — parents now see a warm amber "Achievements Earned" section on the player share portal showing every badge the player has unlocked (First Star ⭐, Team Player 🤝, Grinder 💪, All-Rounder 🎯, Breakthrough 🚀, Game Changer ⚡, Session Regular 📅, Coach's Pick 🏆, Most Improved 📈, Rising Star 🌟); each badge card shows emoji + name + coach note (or default description) in a 2-column grid with distinct per-badge colours; section positioned between "Skills on the Rise" and "Skill Progress" for maximum emotional impact; no new tables or API routes — share route now always fetches `player_achievements` (badge_type, awarded_at, note) in the same request with no extra round-trip; turns the parent portal into a screenshot-worthy moment parents share with each other
- [x] **AI Coach Session Message on parent share portal** — the most recent AI-generated "Player Session Message" (from the coach's Plans page) automatically appears on the parent portal as "Coach's Update" — requiring zero extra work from coaches. Share API queries the last 5 `player_messages` plans for the team and extracts the message matching this player by name (full-name then first-name fallback). Returns `latestSessionMessage { message, highlight, next_focus, session_label }`. New emerald-accented portal card positioned directly after the greeting: warm personalized message, ✨ Highlight chip (emerald), 🎯 Next Focus chip (orange), coach name attribution, and session label. Creates the parent "wow" viral moment: parents see exactly what the coach said about their kid after the most recent session — the most personal and impactful content in the entire portal
- [x] **Player Development Goals on parent share portal** — coaches set specific, trackable goals for each player (Goals tab on player detail) but parents never saw them; share API now always fetches active + achieved `player_goals` (status IN ['active','achieved'], stalled/archived stay private); new "Season Goals" 🎯 section on parent portal (sky/indigo theme) between Achievement Badges and Skill Progress shows each goal with skill chip, goal text, and target date for active goals; achieved goals get emerald "Goal Achieved! 🎉" badge treatment; parent-friendly "Encourage [Name] to keep working on these goals at home!" footer; demo report at `/demo/report` includes 3 realistic sample goals (one achieved Dribbling, two active Defense + Shooting) so prospects see the feature before signing up; `buildChain` test mock gains `.in()` method (was missing, caused 3 share-route tests to 500); zero new tables, API routes, or UI components beyond the portal section; closes the coach-sets-goal→parent-never-sees-it gap that made goal-setting feel pointless

### P16.7 — Team Announcements
- [x] **Team Announcements** — coaches post short team-wide updates (practice cancelled, bring water bottles, etc.) that appear prominently at the top of the parent portal for every player on that team; `AnnouncementsPanel` component on Sessions page — collapsible card with announcement list (title, body, expiry countdown), a compose form (title + body textarea + expiry chip picker: 3d/7d/14d/never), and per-announcement delete with loading state; orange accent "From the coach" section in parent portal share page shows all active announcements above the player report card; active filter uses Supabase `.or(expires_at.is.null,expires_at.gt.NOW)` so expired posts auto-disappear without a cron; GET/POST/DELETE `/api/team-announcements`; `team_announcements` table (migration 022) with cascade delete on team removal; `AnnouncementExpiry` type + `TeamAnnouncement` interface in `database.ts`; `src/lib/announcement-utils.ts` pure utility module (isValidTitle, isValidBody, expiryToDate, expiryLabel, isActive, filterActive, filterExpired, sortByNewest, timeUntilExpiry, countActive, hasAnnouncements, hasActiveAnnouncements, truncateBody, buildAnnouncementShareText, getLatestActive); added `.or()` to `buildChain` test helper so share route tests remain green; 37 unit tests; total suite: 1430 tests

### P16.8 — WhatsApp Direct Send
- [x] **One-tap Send for Player Messages** — each AI-generated player message card now has a teal "Send" button alongside the existing Copy button; on mobile triggers the native Web Share API (WhatsApp, SMS, email, etc.); on desktop opens WhatsApp Web with the message pre-filled; "Sent!" confirmation state resets after 2s; same Send + Copy buttons added to the Plans page player_messages renderer via a `PlayerMsgItem` sub-component; no new API routes or tables

### P16.9 — Team Group Chat Message Generator
- [x] **Team Group Chat Message Generator** — one-tap AI card on session detail page (emerald accent, after Player Session Messages) that generates a WhatsApp/SMS-ready team-wide parent group chat message after any session type; editable textarea so coaches can personalise before sending; Web Share API on mobile / WhatsApp Web on desktop; Copy to clipboard fallback; team highlight strip, coaching-focus chips, optional next-session note; saved as plan type `team_group_message` with rich renderer in Plans page (emerald accent, share + copy buttons); `POST /api/ai/team-group-message` fetches session + observations, builds observation summary (positive count, needs-work count, top categories, standout moment); `teamGroupMessageSchema` Zod schema; `teamGroupMessage` prompt in PROMPT_REGISTRY; `src/lib/team-group-message-utils.ts` pure utility module (getSessionEmoji, getSessionTypeLabel, buildSessionLabel, getPositiveObsCount, getNeedsWorkObsCount, extractFocusAreas, hasEnoughDataForGroupMessage, countChars, truncateMessage, buildPreviewText, isValidGroupMessage, formatGroupMessage, buildShareText, buildWhatsAppUrl, countFocusAreas, hasNextSessionNote); 62 unit tests; total suite: 1549 tests

### P16.10 — AI Coach Review Form
- [x] **AI Coach Review Form** — post-session structured reflection form (between Coach Reflection and Media tabs on session detail); AI asks 3 adaptive questions based on session type + observations (e.g. "What worked well in your defensive drills today?"); coach answers inline; AI synthesizes answers into a structured coaching review (strengths observed, areas for development, action items, self-coaching tip); saved as plan type `coach_review` with full renderer in Plans page (purple accent, action-item checklist, self-coaching tip strip); `POST /api/ai/coach-review`; `src/lib/coach-review-utils.ts` pure utility module (generateReviewQuestions, isGameQuestion, isPracticeQuestion, buildReviewPrompt, formatAnswer, hasEnoughAnswers, countAnsweredQuestions, isValidAnswer, buildReviewShareText, truncateReview, buildActionItemsList, getQuestionsByType, validateQuestions); `coachReviewSchema` Zod schema; `coachReview` prompt in PROMPT_REGISTRY; 37 unit tests; total suite: 1586 tests

### P16.11 — Player Session Messages
- [x] **Player Session Messages** — one-tap AI card on session detail page (teal accent, between Coach Reflection and Media) that generates warm, parent-friendly, send-ready message snippets for each observed player after any session type; each snippet has a 2–3 sentence WhatsApp/SMS-ready message, a specific highlight from that player's session observations, and one next-focus growth tip; individual Copy buttons per player + Copy All; saved as plan type `player_messages` with rich card renderer in Plans page (emerald highlight chip, orange next-focus chip, team note strip); `POST /api/ai/player-session-messages`; `playerSessionMessagesSchema` Zod schema; `playerSessionMessages` prompt in PROMPT_REGISTRY; `src/lib/player-session-messages-utils.ts` pure utility module (filterPlayerObs, getObservedPlayerIds, countObservedPlayers, groupObsByPlayer, filterObsForPlayer, getPositiveObsForPlayer, getNeedsWorkObsForPlayer, hasEnoughDataForMessages, getTopSkillCategory, calculatePositiveRatio, sortPlayersByMostObserved, buildSessionLabel, buildPlayerMessageShareText, isValidMessageText, countMessages, truncateMessage, extractPlayerNamesFromObs, hasGeneratedMessages, buildPlayerObsPayload); 58 unit tests; total suite: 1348 tests

### P16.12 — Pre-Practice Parent Reminder
- [x] **Pre-Practice Parent Reminder card** — sky-blue `PracticeReminderCard` on the session detail page; shown only for upcoming/today sessions (auto-hides for past sessions); auto-fills a WhatsApp-ready message with team name, session type, "Today"/"Tomorrow"/weekday date label, time, and 📍 location; game/scrimmage/tournament sessions get a 🏆 headline + opponent name while practice/training sessions get a 📣 headline; editable textarea so coaches can personalise; Copy to clipboard + WhatsApp Web one-tap send buttons; zero new API routes, DB tables, or AI calls — pure client-side; dismissible with ✕; closes the "coaches get asked When is practice? every week" gap in under 5 seconds

### P16.13 — Direct WhatsApp for Player Session Messages
- [x] **Direct WhatsApp send for Player Session Messages + complete Add Player form** — when a parent phone is on file, the per-player "Send" button in the Player Session Messages card now opens WhatsApp directly pre-addressed to that parent (`wa.me/{phone}?text=...`) instead of the generic WhatsApp Web picker; saves ~5 s × 12 players of copy-paste friction per practice; `directPhoneMap` memoized from `matchMessageToPlayer` so matching runs once not on every render; each player card shows an emerald "Direct" badge when a phone match is found; hint text drives coaches to add missing phone numbers; hint updated to mention both phone and email options; **Add Player form was missing `parent_phone` and `name_variants` fields** (Edit Player had them) — both added with helper text ("Add phonetic spellings so voice capture recognizes this player by name" / "Used to send player updates directly via WhatsApp after practice"); `parent-email-utils.ts` extended with `parent_phone?` on `ParentEmailPlayer`, `filterPlayersWithPhone`, `countPlayersWithPhone`, `hasAnyParentPhone`

### P16.14 — Automatic Weekly Parent Progress Digest
- [x] **Automatic Weekly Parent Progress Digest** — coaches enable one toggle in Settings → Profile → "Auto parent progress emails" and every Sunday at 18:00 UTC each parent with an email on file automatically receives a personalized progress-report email with their child's live share portal link; cron gets or auto-creates a permanent share token per player; email includes activity summary (session count + observation count for the week), highlight quote (most recent positive observation), and a branded CTA; dedup via `preferences.parent_digest_week_YYYY-MM-DD`; CRON_SECRET auth; 3-obs minimum threshold; help text in settings drives roster completeness; closes the "coach must manually share after every session" gap that prevented consistent parent communication; `src/lib/parent-digest-utils.ts` pure utility module (getWeekStartSunday, isParentDigestEnabled, hasAlreadySentParentDigest, markParentDigestSent, enableParentDigest, disableParentDigest, hasEnoughDataForParentDigest, getRecentObsHighlight, buildShareUrl, buildParentDigestSubject, buildParentDigestHtml); `POST /api/cron/parent-digest`; Vercel cron `0 18 * * 0`; 52 unit tests; total suite: 2704 tests

### P16.15 — Observation Sharing
- [x] **"Send to parent" button on positive observation cards** — coaches tap a teal "Send to parent" chip on any positive observation in the Observations feed or the player detail Observations tab; if the player has a `parent_phone` on file, tapping opens WhatsApp Web pre-addressed to that parent with the observation text pre-filled in a warm message ("Hi! Just wanted to share a great moment — Marcus had a positive observation: '…' 🙌 — Coach Sarah, YMCA Rockets U12"); on mobile without a known phone, the native Web Share API opens the share sheet; clipboard fallback for desktop; 2-second "Sent!" confirmation state resets automatically; button hidden for needs-work and neutral observations and for team-wide (non-player) observations; observations query extended to also fetch `parent_phone` (same round-trip — no extra API call); `coach` destructured from `useActiveTeam()` for the coach first name; zero new API routes, DB tables, or AI calls — pure client-side with existing data; closes the observe→celebrate→share parent loop in a single tap from the two most-visited observation surfaces

### P45 — Weekly Coaching Intentionality
- [x] **Weekly Team Focus card** — coaches one-tap set a skill theme for the week (e.g. "Defense week before Saturday's game") from a 10-category picker (Shooting, Defense, Ball Handling, Passing, Hustle, Court Vision, Teamwork, Footwork, Conditioning, Leadership); stored in localStorage, auto-expires after 7 days; indigo-accented home dashboard card shows category emoji + label + age + days remaining when set, or inline category picker when not set; "Change" button re-opens the picker; DrillOfDayCard shows an indigo "Matches your focus" badge when today's recommended drill category aligns with the weekly focus, closing the focus→drill feedback loop visually; shown on home after ≥1 session; SSR-safe via mounted guard; zero new API routes, DB tables, or AI calls; `src/lib/weekly-focus-utils.ts` pure utility module (FOCUS_CATEGORIES, getWeeklyFocusKey, isValidFocusCategory, getFocusCategoryConfig, isWeeklyFocusExpired, getDaysRemaining, getWeeklyFocus, setWeeklyFocus, clearWeeklyFocus, categoryMatchesFocus, formatFocusAge); 34 unit tests; total suite: 2485 tests
- [x] **"Build [Focus] Practice Plan" one-tap from Weekly Focus card** — when a coach has set a weekly focus (e.g. "Defense Week"), the card now shows a "Build Defense Practice Plan" button that POSTs to `/api/ai/plan` with `focusSkills: [focusLabel]` and navigates to `/plans` on success; generating/error states handled inline; closes the declare-intent → get-actionable-content loop that previously required the coach to navigate to Plans, remember what they wanted, type a prompt, and wait — now it's one tap from the home dashboard; no new API routes, DB tables, or utility modules

### P42 — Demo & Conversion
- [x] **Sample Parent Report Card (`/demo/report`)** — static page with fully realized mock parent portal for "Marcus Johnson / YMCA Rockets U12 / Spring 2025"; shows every parent-facing section: Coach's Update (with ✨ highlight + 🎯 next-focus chips), Season at a Glance stats strip (47 obs / 3 improving / 12 fortnight), Skills at a Glance pure-SVG spider chart (5-axis: Dribbling/Defense/Teamwork/Offense/Shooting), Skills on the Rise emerald chips, Practice at Home home-challenge card with numbered steps + goal, Achievements Earned 4-badge grid, Skill Progress bars with proficiency legend, What to Celebrate checklist, Recent Observations feed, and Coach's Note; no database or API calls — fully static, prerendered at build time; orange demo banner at top with "Create a free account →" CTA; sign-up call-to-action card at bottom; linked from two locations on the landing page ("See it in action" section + demo preview section) and from the demo voice-capture results page as "View sample parent report" emerald card; closes the biggest demo conversion gap — prospects had no way to see the parent portal (the most viral feature) without first being a coach with players and share links
- [x] **Real Claude AI in demo capture (`/api/ai/demo-segment`)** — the demo voice/text capture page previously always showed the same 5 hardcoded mock observations regardless of what a coach typed or said, because `/api/ai/segment` requires authentication; new public `/api/ai/demo-segment` route (claude-haiku-4-5, IP-rate-limited to 5 calls per 15 min) processes the coach's exact transcript against the demo roster and returns real AI-parsed observations; demo page updated to call the new endpoint first and fall back to sample data only when AI is unavailable; results screen shows a "Live Claude AI" violet badge and labels the transcript "Your words — processed by Claude AI" when AI succeeded; processing screen gains four animated step labels so coaches understand what is happening; middleware updated to add the route to public paths; closes the "demo always shows fake data" gap that undermined trust in the AI's actual capability
- [x] **Animated phone mockup preview section on landing page** — landing page had zero visuals showing the actual app UI, a major conversion gap for a UI-driven product; new dark-theme "App Preview" section (between "How It Works" and "Built for Coaches Like You") shows 3 phone frame mockups: (1) Voice Capture — pulsing animated mic with RECORDING strip and live transcript; (2) AI Observations — 3 player cards with positive/needs-work badges, category chips, and Save CTA; (3) Parent Portal — light-mode report with avatar, Coach's Update card, skill progress bars (Defense/Teamwork/Dribbling), and "Defense on the rise" emerald chip; horizontal snap-scroll on mobile, 3-column grid on desktop; pure static JSX/Tailwind — zero new API routes, DB tables, AI calls, or dependencies; closes the "prospects can't see the product before signing up" gap
- [x] **Testimonials section on landing page** — landing page lacked social proof beyond a single anonymous quote inside the "Built for coaches" section; added a 3-card testimonials grid (sm:grid-cols-3) between "See it in action" and "Pricing" — the prime conversion moment before the ask; three specific volunteer-coach personas (YMCA basketball, youth soccer, flag football) with authentic quotes targeting the three main objections (will parents care? / I'm not tech-savvy / does it save time?), 5-star ratings, colour-coded avatar initials, name, and role+location; `Star` icon added to lucide-react imports; zero new API routes, components, or dependencies
- [x] **SeasonalPromo wired to home dashboard** — `SeasonalPromo` component (P5, `src/components/onboarding/seasonal-promo.tsx`) was built and marked done but was never imported or rendered on the home dashboard; now shown between the First Practice Launcher and the stats grid; self-hides outside the first 21 days of September/January/April and is per-season dismissible via localStorage; contextual CTA changes to "Schedule Session" when team already has 5+ players

### P49 — Coaching Effectiveness Feedback Loop
- [x] **Player Breakthrough Card on home dashboard** — when a player flips from struggling in a skill to showing real improvement (≥2 needs-work observations in days 8-21 → ≥2 positive observations in the last 7 days), the home dashboard surfaces an emerald "Coaching Win 🚀" card celebrating the breakthrough; card shows the player name, skill category, the "needed work N× before → N positive obs this week" signal strip, a one-tap "Tell Their Parent" button (Web Share API on mobile, WhatsApp Web on desktop) that sends a warm pre-written congratulatory message, a "View Player" link, and a dismiss button; dismissed per-player per-category per-week via localStorage so it never becomes noise; shown when ≥5 observations exist; strongest undismissed breakthrough shown at a time; coachName passed from home page for personalised share text; `src/lib/player-breakthrough-utils.ts` pure utility module (splitWindows, groupByPlayerCategory, countBySentiment, buildBreakthroughs, getBestBreakthrough, getBreakthroughDismissKey, isBreakthroughDismissed, dismissBreakthrough, formatCategory, hasEnoughDataForBreakthroughs, buildBreakthroughShareText, buildBreakthroughWhatsAppUrl, buildPriorLabel, buildRecentLabel, BREAKTHROUGH_THRESHOLD, RECENT_DAYS, PRIOR_DAYS); 45 unit tests; total suite: 3099 tests; zero new API routes, DB tables, or AI calls — pure algorithmic detection from existing observation data

### P60 — Practice Timer Drill Discovery
- [x] **Skill-gap drill suggestions on Practice Timer setup screen** — when a coach opens the Practice Timer with an empty queue (no plan loaded, no template selected), the setup screen now surfaces up to 3 drill chips labelled "Suggested for today · based on your team's skill gaps"; each chip shows drill name, skill category, and duration; one tap adds the drill to the queue and removes it from suggestions; computed from the already-fetched `needsWorkObs` (30-day needs-work observations) and `drills` library — counts obs by category, takes the top 2 categories, filters + sorts matching drills, excludes drills already in the queue; when the queue is non-empty the suggestions disappear so they never clutter the queue view; the empty-queue placeholder text changes to "Or pick any drill from the library below" when suggestions are shown, or the original "Add drills to get started" when there's not enough observation history; zero new API routes, DB tables, AI calls, or utility modules — pure `useMemo` from data already fetched on timer mount; closes the "I have 5 minutes before practice and don't know which drills to run" gap at the exact moment it matters

### P56 — Practice Time Awareness
- [x] **Live elapsed timer on End Practice button + wrap-up nudge** — volunteer coaches with a 45-minute practice slot had no way to see how long they'd been going without glancing at their watch; `practiceStartedAt` was stored in Zustand but never displayed on the home page; the End Practice button now shows a live "⏱ 38 min" badge that updates every 30 seconds, and after 40 minutes an amber "X min in — time to start cool-down?" strip appears below — closing the time-management blind spot at the most critical coaching moment with zero new API routes, DB tables, or AI calls; `src/lib/elapsed-time-utils.ts` pure module (formatElapsed, getElapsedMinutes, shouldShowWrapUpNudge); `src/hooks/use-elapsed-time.ts` reactive hook (30 s setInterval, cleaned up on unmount); 14 unit tests; total suite: 3113 tests

### P50 — Home Dashboard Intelligence Gap
- [x] **StrugglingPlayerCard + PrePracticeSnapshotCard wired to home dashboard** — two built-but-unwired components are now live: (1) `PrePracticeSnapshotCard` — compact "Quick coaching brief" chip row shown before practice starts (when ≥5 obs exist); amber chip surfaces top needs-work skill category → links to filtered drill library; "Watch: Marcus, Jordan · 14d unobserved" chip names neglected players; passes today's `sessionId` so "Full AI brief →" links directly to session detail; refactored from requiring a `rosterPlayers` prop to self-fetching its own roster (10-min staleTime) so it is fully self-contained; placed after Quick Actions grid; (2) `StrugglingPlayerCard` — "Skill Struggle Alert" shown after Player Breakthrough card (≥5 obs); finds the player with the most repeated needs-work observations in a single skill category (threshold 3); shows a coach-facing coaching tip ("Ball Handling flagged 4× recently — a good drill could help") + two one-tap CTAs: "Find Drills" (filtered drill library) and "Observe [FirstName]" (/capture pre-selecting the player); dismissed per-team per-day via localStorage; self-hides when no player crosses the threshold; zero new API routes, DB tables, or AI calls — both cards reuse existing `struggling-player-utils.ts` (45 tests) and `skill-trend-utils.ts`

### P51 — Session AI Tools: Player of the Match + Team Talk
- [x] **PlayerOfMatchCard + TeamTalkCard wired to session detail page** — two complete API routes (`/api/ai/player-of-match`, `/api/ai/team-talk`) with full schemas, utility modules, and AI prompts existed but had zero UI entry points; `PlayerOfMatchCard` (gold/yellow accent, 🏅 hero banner) appears on game/scrimmage/tournament sessions after the Game Recap, picks the standout player from session observations, displays headline + achievement + key moment + coach shoutout, one-tap Web Share / clipboard via `buildMatchShareText()`; `TeamTalkCard` (orange accent, mic icon) appears on all session types between Pre-Session Briefing and Observations, generates a 20-second motivational script + focus-word chips + team chant, copy-to-clipboard, reads weekly focus from localStorage to personalise prompt; `player_of_match` and `team_talk` added to `PlanType` union in `database.ts`; both added to `PLAN_TYPE_CONFIG` (Award + Mic icons) and get full rich renderers in the Plans page; zero new API routes, DB tables, migrations, or AI prompt changes — pure wiring of existing infrastructure
- [x] **Jump-to navigation on session detail page** — the session detail page has 12+ AI tool cards (AI Debrief, Game Recap, Player of Match, Coach Reflection, Player Messages, Team Group Message, Huddle Script, etc.) with no way to navigate between them; coaches arriving to send parent updates had to scroll through the entire page to find the right card; added a horizontal-scroll row of emoji-labeled chip buttons directly below the session info card that smooth-scrolls to: 🤖 AI Debrief, 💬 Parent Updates, 🎮 Game Recap (game sessions), 🏅 Player of Match (game sessions), 📝 Reflection, 📣 Group Chat; added section IDs to GameRecapCard, PlayerOfMatchCard, CoachReflectionCard, and TeamGroupMessageCard wrappers; zero new API routes, DB tables, AI calls, or dependencies

### P52 — Instant Observation Sharing
- [x] **"Send to parent" button on positive observation cards** — coaches tap a teal "Send to parent" chip on any positive observation in the Observations feed or the player detail Observations tab; if the player has a `parent_phone` on file, tapping opens WhatsApp Web pre-addressed to that parent with the observation text pre-filled in a warm message ("Hi! Just wanted to share a great moment — Marcus had a positive observation: '…' 🙌 — Coach Sarah, YMCA Rockets U12"); on mobile without a known phone, the native Web Share API opens the share sheet; clipboard fallback for desktop; 2-second "Sent!" confirmation state resets automatically; button hidden for needs-work and neutral observations and for team-wide (non-player) observations; observations query extended to also fetch `parent_phone` (same round-trip — no extra API call); `coach` destructured from `useActiveTeam()` for the coach first name; zero new API routes, DB tables, or AI calls — pure client-side with existing data; closes the observe→celebrate→share parent loop in a single tap from the two most-visited observation surfaces