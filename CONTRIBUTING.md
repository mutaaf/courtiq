# Contributing to SportsIQ

## For the Innovation Agent

You run every hour. Here's how to be effective.

### Your Workflow
1. `git pull origin main` — always start fresh
2. Read `CLAUDE.md` for architecture context
3. Read this file for what to work on
4. Pick ONE task from the priority list below
5. Implement it completely (not a stub)
6. Verify: `npx tsc --noEmit && npm run lint && npx vitest run`
7. Commit and push

### Priority Rules
1. **Never break existing features** — if tests fail, fix before pushing
2. **One change per run** — focused and complete
3. **Check the roadmap** — work on the highest-priority item you can complete in one session
4. **Don't duplicate** — check git log to see what was recently done

---

## Current Priority Queue

### P0 — Critical Fixes (do these first if they exist)
- [ ] Fix any TypeScript errors
- [ ] Fix any failing tests
- [ ] Fix any lint errors
- [ ] Fix any runtime errors reported in recent commits
- [ ] Fix any build failures (`npm run build`)
- [x] **Quick Capture observations missing session_id during active practice** — the QuickCaptureWidget (floating Zap button) saved observations with no `session_id` even when a practice session was running; this meant the session coverage tracker "X/Y observed" count excluded them, the AI post-session debrief had no knowledge of them, Player Session Messages and Team Group Message missed them, and the live "12 obs · 8 players covered" badge on the End Practice button was wrong; both Voice tab and Templates tab now read `practiceActive + practiceSessionId` from `useAppStore` and include `session_id` when a session is active; both paths also invalidate the `session-obs-count` query key so the End Practice counter updates immediately after each Quick Capture
- [x] **Build crash: Stripe lazy init** — `new Stripe(STRIPE_SECRET_KEY)` at module level caused `next build` to throw "Neither apiKey nor config.authenticator provided" during page data collection; converted to `getStripe()` lazy factory; updated all 3 Stripe route files
- [x] **Session ID missing on capture links** — three paths to the capture page were silently dropping the session context: (1) TodaySessionCard mic button used `?session=` instead of `?sessionId=`; (2) active-practice "Capture" quick action on the home page linked to `/capture` with no params; (3) mobile bottom-nav Capture FAB linked to `/capture` with no params. Fixed all three so observations are correctly linked to the active practice session, feeding the coverage tracker, session obs count in the "End Practice" button, and the post-session AI debrief
- [x] **Contextual CTAs on capture review success screen** — after saving session-linked observations the primary CTA was "View Roster" (a dead end); now shows "View Session" when `sessionId` is present, taking coaches directly to the session detail page where AI debrief, Player Session Messages, and Huddle Script are one tap away; "View Roster" preserved for ad-hoc (non-session) captures; also fixed the unmatched-player-names warning which linked to `/settings/sport` (wrong page) instead of `/roster/add`
- [x] **calculateConsistencyRate off-by-one on Mondays** — `getWeeklyObservationCounts` used ISO week keys sampled at `0, 7, 14, 21` days ago; when today is Monday, "1 day ago" falls in the previous ISO week and "22 days ago" lands outside the 4-key lookup window, returning 75% instead of 100% for 4 evenly distributed observations; replaced ISO-week bucketing with 7-day rolling buckets (`floor(ageMs / 7d)`) which are deterministic regardless of day-of-week
- [x] **False "Set up AI" banner on home dashboard** — the banner appeared whenever an org had no DB-level API keys, even though the platform provides AI via `ANTHROPIC_API_KEY` / `OPENAI_API_KEY` / `GEMINI_API_KEY` env vars that `callAI()` falls back to automatically; `GET /api/me` now computes `aiPlatformAvailable` from `process.env` outside the 2-min memCache and includes it in the response; `useActiveTeam` exposes it; home page checks platform availability first so coaches on hosted Vercel deployments never see the misleading "Connect an AI provider" prompt pointing to a settings page that already shows "(set via env)"
- [x] **Missing home dashboard widgets** — Coaching Streak Tracker (P19), Team Wins Feed (P5), and Parent Reactions (P26) had complete API routes + utility modules but were never wired into the home page UI; built `StreakCard` (`src/components/home/streak-card.tsx` — flame icon, at-risk amber pulse, milestone progress bar, Best! badge, self-hides until first observation), `TeamWinsCard` (`src/components/home/team-wins-card.tsx` — trophy/target icons, relative timestamps, links to roster), and `ParentReactionsCard` (`src/components/home/parent-reactions-card.tsx` — pink accent, unread badge count, Mark-all-read action); all three added to home page after the stats grid; all return null when no data so they never create visual noise for new coaches
- [x] **Light-mode contrast on parent portal viral CTA and landing page badges** — `ParentViralCTA` used dark zinc-900 gradient and zinc-100/zinc-400 text colors designed for the dark dashboard theme but is rendered on the light parent portal (bg-gray-50); replaced with white card, orange-50→white gradient, gray-900/gray-500 text, and a clean orange button matching the portal's light aesthetic; copy sharpened from "Share SportsIQ with a Coach" to "Share with your other coach" (more specific); landing page hero badge and "Save 20%" badge now use explicit light-mode classes (bg-orange-100 text-orange-700, bg-emerald-100 text-emerald-700) instead of dark-theme badge defaults that rendered ~1.8:1 contrast on white
- [x] **BirthdayCard and DrillOfDayCard wired to home dashboard** — `birthday-utils.ts` (75 tests, P35) and `drill-of-day-utils.ts` (56 tests, P33) had complete utility modules but no home UI components; built `BirthdayCard` (`src/components/home/birthday-card.tsx` — amber theme, "Today!" badge for same-day birthdays, one-tap Send opens WhatsApp if parent_phone set, Web Share API fallback, clipboard fallback, dismissed per-team per-day via localStorage, self-hides when no upcoming birthdays in next 6 days) and `DrillOfDayCard` (`src/components/home/drill-of-day-card.tsx` — teal theme, deterministic per-team per-day drill selection targeting top needs-work skill category, shows name + description + up to 2 coaching cues + duration + player count + "See all drills" CTA, dismissed per-day, self-hides with <5 observations or no matching drills); both wired into home page above the stats grid; zero new API routes or DB tables
- [x] **TeamSkillTrendsCard missing despite being marked done (P14)** — `skill-trend-utils.ts` had 56 unit tests and the feature was checked [x] in the CONTRIBUTING backlog, but the home-component was never created; built `TeamSkillTrendsCard` (`src/components/home/team-skill-trends-card.tsx`) that fetches last 14 days of observations, splits into two 7-day windows client-side, computes per-category positive-ratio deltas via the existing `buildSkillTrends` / `getTopImprovingSkills` / `getTopDecliningSkills` utilities, and renders up to 3 improving-skill chips (emerald, delta badge + obs count) and 3 declining-skill chips (red, delta badge + obs count, tappable → /plans to generate a targeted plan); self-hides when < 5 scored observations exist across the 14-day window; wired into home page after stats grid, shown when ≥5 total observations; zero new API routes, DB tables, or AI calls
- [x] **Email notification opt-out toggles missing** — the weekly Monday coaching digest (P44) and practice-day reminder (P44) cron routes honour `preferences.disable_weekly_digest` and `preferences.disable_practice_reminders` but there was no UI to set them; coaches received both emails with no way to opt out; added "Weekly coaching digest" and "Practice day reminders" toggle switches to the Communication card in Settings → Profile (alongside the existing Auto Parent Progress toggle); each reads initial state via `isDigestDisabled()` / `isReminderDisabled()` on mount; saves via `mutate()` to `coaches.preferences`; shared `emailPrefSaving` / `emailPrefSaved` states; GDPR-compliant opt-out now accessible in 2 taps

### P0.5 — Billing, Payments & Account Management (MUST BE BULLETPROOF)

**Stripe Integration:**
- [x] Stripe products + prices created (Coach $9.99, Pro $24.99, Org $49.99)
- [x] Database: stripe_customer_id, stripe_subscription_id, subscription_status columns
- [x] /api/stripe/create-checkout — creates Stripe Checkout session
- [x] /api/stripe/webhook — handles subscription lifecycle events
- [x] /api/stripe/portal — opens Stripe Billing Portal
- [x] Vercel env vars set for all Stripe keys + price IDs
- [ ] Webhook signature verification (STRIPE_WEBHOOK_SECRET needed after registering webhook in Stripe dashboard)
- [ ] Test full checkout flow: upgrade → pay → tier changes → features unlock
- [ ] Test cancellation flow: cancel → webhook → downgrade at period end
- [ ] Test payment failure: failed payment → past_due status → warning banner
- [ ] Test resubscription: free user re-upgrades after cancellation

**Switching to Production Stripe:**
To go live, the ONLY changes needed:
1. Replace `sk_test_*` with `sk_live_*` in Vercel env vars
2. Replace `pk_test_*` with `pk_live_*` in Vercel env vars
3. Run the product/price creation script with live keys → get new price IDs
4. Update the 6 `STRIPE_PRICE_*` env vars with live price IDs
5. Register live webhook URL in Stripe dashboard → get live `STRIPE_WEBHOOK_SECRET`
6. Update `STRIPE_WEBHOOK_SECRET` env var
NO code changes needed — it's all env-var driven.

**Role-Based Gating (must work flawlessly):**
- [x] Free users: limited to 1 team, 10 players, 5 AI calls, basic features — player limit already enforced in /api/data/mutate; **monthly AI call quota now enforced server-side in callAI()**: TierLimitError (status 402) thrown when free-tier org exceeds 5 successful ai_interactions this calendar month; handleAIError returns { error, upgrade: true }; AIUpgradePrompt component shown on capture/review page with direct link to /settings/upgrade; all 20+ AI routes get enforcement for free via the shared callAI() entry point
- [x] Coach users: 3 teams, unlimited players/AI, report cards, parent sharing — **Tier gating enforced**: `UpgradeGate` wraps report-card, share, and media tabs on player detail; `/api/share/create` and `/api/ai/report-card` both verify org tier server-side (403 for free tier); free users see contextual upgrade prompt with feature benefits + CTA
- [x] Pro users: unlimited everything + assistant + analytics + media — **analytics** gated (UpgradeGate), **assistant** gated (UpgradeGate), **media_upload (photo capture)** gated on /capture camera button + /capture/photo page (UpgradeGate), **tendencies (game day prep)** gated in plans page; free/coach users see Pro badge + lock icon, clicking redirects to /settings/upgrade
- [x] Org users: everything + multi-coach + admin + custom branding — **Branding Settings**: org admins can set logo URL, accent color, and report header text from Settings → Organization; live preview card; gated behind `canAccess('custom_branding')`; non-Org tiers see contextual upgrade prompt; uses existing GET/PUT /api/branding (no new routes/tables)
- [x] Upgrade prompts: contextual, value-first, link to checkout — **UpgradeGate redesigned**: 9 feature configs (analytics, assistant, report_cards, parent_sharing, multi_coach, org_analytics, custom_branding, tendencies, media_upload) each with a unique icon, headline, tagline, and 5 specific benefit bullets; correct tier + price displayed per gate; auto-escalates to org tier if user is already on required tier; accent colour differentiated by feature; orange "Upgrade →" CTA links to /settings/upgrade; graceful fallback for unknown feature keys
- [x] Downgrade handling: features locked gracefully, data preserved — "subscription ended" banner in DashboardShell when status=canceled; UpgradeGate components already lock features when tier drops to free; data is never deleted on downgrade
- [x] Past-due banner: visible on all pages, links to billing portal — fixed `/api/me` to include `subscription_status`; `Organization` type updated; `useTier()` now exposes `subscriptionStatus`; data flows through to DashboardShell banner
- [x] Cancel-at-period-end: show expiration date, allow resubscription — amber banner in DashboardShell; `useTier()` exposes `cancelAtPeriodEnd` + `currentPeriodEnd`

**Account Management:**
- [x] **Admin panel**: manage coaches, roles, tiers — `/admin` page lists all org coaches + teams; org-plan badge; link to cross-team analytics
- [x] **Invite coaches**: email invite with role assignment — admin enters email + selects initial role (head_coach/coach/assistant); `inviteUserByEmail` embeds `org_id` + `initial_role` in user metadata + sets `redirectTo` to auth callback; invited coach automatically joins the org on first login (no new-org creation)
- [x] **Role changes**: admin can promote/demote coaches — optimistic role-select dropdown per coach row; PATCH `/api/admin/coaches` verifies same-org constraint; rollback on failure with inline error
- [x] Org settings: name, branding, billing email — name+slug already existed; billing_email added (stored in org.settings JSON, no migration); branding section gated behind org plan
- [x] **Data export**: coaches can export their data — `/settings/data` page with one-tap CSV download for observations, roster, and sessions (all tiers, no analytics page required)
- [x] **Account deletion**: COPPA-compliant data removal on request — 3-step confirmation (intent → confirm → type "delete my account"); POST /api/account/delete cascades through all player/team/org tables in FK-safe order when coach is the only member; Supabase auth user deleted last; client signs out and redirects to /login?deleted=1; COPPA notice confirms no backup copies retained
- [x] **Practice Timer observation data quality** — break-screen notes were saved with `sentiment: 'neutral'` + `category: 'general'`, making them invisible to AI debrief, momentum scores, skill trends, drill recommendations, and weekly star; fixed by adding a 👍/👎 sentiment toggle to the break screen, carrying `category` from drill library items through `QueueItem → CapturedNote → save`, and removing the triple player-name lookup in favour of storing `playerId` directly; save button colour matches selected sentiment; Done screen notes show colour-coded sentiment badges

### P39 — Practice Timer Intelligence
- [x] **Last Observed Context in Practice Timer break screen** — when a coach taps a player name on the break-screen observation picker, a compact context chip appears showing that player's most recent observation: text (truncated to 72 chars), skill category, and a time label ("This session" / "Yesterday" / "3d ago" / "1 week ago"). Current-session notes take priority over database observations so coaches see the most up-to-date context — e.g. "You just noted Marcus was doing great; is he still on a roll?" Chip color-coded: orange for current session, emerald for positive prior obs, amber for needs-work. One lightweight DB query (last 30 days, all sentiments) piggybacked at the same 5-min staleTime as the existing focus-callout query — no extra round-trip cost when data is warm. `buildLastObsByPlayer`, `formatLastObsTime`, `truncateObsText` added to `timer-focus-utils.ts`; 26 new unit tests; total suite: 2291 tests
- [x] **Player Focus Callouts in Practice Timer** — during each drill countdown, the timer now surfaces 1–2 amber "Watch closely" chips naming the players who most need work on that drill's skill category (based on the last 30 days of needs-work observations); chips show jersey number when available and a "×N needs work" count for players flagged 3+ times; only displayed when there are enough observations to be meaningful (at least one player with ≥2 needs-work obs in any category); one lightweight DB fetch on mount (max 300 obs), cached 5 minutes so drill transitions stay instant; closes the observe→act coaching loop directly inside the timer — a volunteer coach no longer has to remember which players need extra attention; `src/lib/timer-focus-utils.ts` pure utility module (normalizeCategory, countObsByPlayerForCategory, getFirstName, getPlayerFocusForCategory, hasEnoughObsForFocus, buildFocusLabel); 37 unit tests; total suite: 2179 tests
- [x] **Player Coverage Indicator on Break Screen** — the Practice Timer break screen now shows coaches which players they've already observed this session: unobserved players sort to the front of the player picker so gaps are visible instantly; already-observed players get a subtle emerald ✓ and dimmed styling so they fade to the background; an amber "X/Y observed" count (or emerald "✓ All players observed" when complete) appears beside the Player label; no new API routes, DB tables, or AI calls — computed inline from the already-accumulated `notes` state using a Set; `capturedPlayerIds` prop added to `BreakScreen`; closes the "am I observing everyone?" blind-spot loop at exactly the right moment — when the coach is about to decide who to watch next
- [x] **Voice Input on Practice Timer Break Screen** — coaches tap a mic button in the break-screen textarea to dictate an observation instead of typing — eliminates the biggest friction point for volunteer coaches standing on a court with 12 kids and 30 seconds between drills; mic button (absolutely positioned in textarea corner) toggles Web Speech API via the existing `useVoiceInput` hook; live transcript fills the textarea (read-only) while recording so coaches see what's being captured in real-time; stopping recording commits the final transcript into the note field for optional editing before save; pulsing red dot + "Listening…" strip confirms the mic is active; gracefully hidden when the browser lacks Speech API support; `aria-pressed` + `aria-label` on mic button for accessibility; no new hooks, API routes, or DB tables — pure reuse of existing infrastructure
- [x] **Availability-aware Practice Timer** — players marked injured / sick / unavailable in the Player Availability Tracker are now automatically excluded from the Practice Timer's break-screen observation picker and "Watch closely" focus callouts; timer fetches `/api/player-availability` on mount (5-min cache, no extra round-trip when data is warm) and computes `presentPlayers` vs `absentPlayers`; an amber "X players marked unavailable" banner appears in the setup screen so coaches know their roster is filtered; the "X/Y observed" coverage counter counts against present players only so coaches can actually hit "✓ All players observed" during a session with absentees; `limited` players remain included (they can still participate with restrictions); no new tables, migrations, or API routes — pure reuse of existing infrastructure

### P36 — Practice Timer UX Polish
- [x] **Quick Observation Templates on Break Screen** — 5 sentiment-filtered template chips shown between the sentiment toggle and player picker on the Practice Timer break screen; tapping a chip fills the textarea and carries the template's skill category (overriding the drill's default category) so saved observations are filed under the correct skill rather than the drill's category; sentiment toggle clears the current chip selection; manual typing in the textarea also clears chip selection; category passthrough added as optional 5th arg to `onSave` / `handleBreakSave`; consistent with existing template chips in Quick Capture widget (P25) and Practice Mini-Dropdown (P1.5)
- [x] **Practice Timer crash recovery** — auto-saves captured observations and drill queue to sessionId-scoped localStorage keys on every change so nothing is lost if a coach's phone dies, the tab is accidentally closed, or the app crashes mid-practice; lazy `useState` initialisers restore data synchronously on first render (no effect race conditions); if unsaved notes exist on page load the timer jumps straight to the Done screen with an amber "Recovered N observations" banner and a Discard link; `handleStartFresh` clears both keys and resets to setup mode; localStorage cleared on successful DB insert; `planId` and `templateId` loading effects gain an `|| isRecovered` guard to prevent a URL param from overwriting recovered data

### P0.8 — Practice Mode Focus
- [x] **Home dashboard practice-mode simplification** — during active practice the home page was showing 15+ cards (streak tracker, skill trends, parent reactions, upcoming sessions, freemium nudges, AI tips, seasonal promo, weekly focus, drill of day, getting-started checklist, AI-keys banner) none of which a coach standing on a sideline needs; added `!practiceActive` guard to 11 card renders so the dashboard collapses to just: End Practice controls, live coverage strip, Live Skill Pulse, quick-action shortcuts, and the season stats grid; all 11 cards reappear instantly when practice ends; zero new API routes, components, or state

### P0.9 — Trust & Legal
- [x] **Terms of Service page** — `/terms` was a dead 404 linked from landing page footer and every parent share portal; created full ToS covering acceptable use, account responsibilities, subscription/payments, AI content disclaimer, IP, liability, and termination; updated `/privacy` title and added cross-link

### P1 — Core UX Polish
- [x] Ensure all buttons/links on every page lead to real functionality — AI assistant "Add to Drills" saves drill structured_data via /api/ai/save-drill; "Share with Parents" copies formatted report to clipboard; both show success state + toast
- [x] Remove any remaining `[object Object]` or raw JSON in UI — audit log viewer now uses formatConfigValue helper (human-readable inline format for strings/arrays/objects); plans page renderObjectFields already guarded
- [x] Fix any broken navigation or dead links — UpgradeGate "View Plans" was pointing to /settings with no pricing; now routes to /settings/upgrade
- [x] Ensure all forms save data correctly — admin role-change now rolls back optimistic update + shows error if PATCH fails (was silently leaving UI in wrong state)
- [x] Improve loading states (skeleton screens, not blank pages) — home page stats + Team Pulse skeleton done
- [x] Add pull-to-refresh on mobile pages — PullToRefresh component on Home, Sessions, Roster, Plans; haptic feedback at threshold + on complete
- [x] Add swipe gestures for navigation — useSwipeNavigation hook; left/right swipe cycles Home→Assistant→Capture→Plans→Settings; 80px threshold + vertical-ratio guard avoids conflict with scroll and pull-to-refresh
- [x] Smooth page transitions/animations — CSS fade-in + slide-up via PageTransition component, respects prefers-reduced-motion
- [x] **Getting Started checklist on home dashboard** — replaces buried all-zeros empty-state card with a prominent 3-step progress card (Add Players / Start Practice / Capture Observation) positioned between Quick Actions and Stats; current next step highlighted in orange with tappable row; done steps show green checkmark; auto-hides when all 3 done; 30-day per-team localStorage dismissal; `src/components/home/getting-started-card.tsx`
- [x] **Daily Focus Card on home dashboard** — surfaces the existing (unused) `daily-focus-utils.ts` logic as a persistent blue card on the home page; computes which player has gone longest without an observation, their top needs-work skill (from their own 30-day history), and presents two direct CTAs: "Capture Obs" (pre-selects the player via `?player=` param) and "Find Drills" (opens drill library filtered to the skill category); auto-hides when data is insufficient (< 2 players, < 5 observations, < 2 players covered), when practice is active, or when coach has 0 sessions; no new API routes, tables, or AI calls — pure algorithmic computation via `buildDailyFocusSuggestion()`; `src/components/home/daily-focus-card.tsx`

### P2 — AI Intelligence
- [x] **Context-aware quick suggestions in AI Assistant** — the empty state previously showed 4 hardcoded generic prompts; now fetches the team's recent sessions (±7 days), last 60 observations, and active roster in 3 parallel queries (5-min stale cache) and computes up to 4 personalised prompt suggestions: (1) upcoming session today/tomorrow → "Help me run today's practice vs [opponent]"; (2) top needs-work skill category (last 14 days) → "Create a drill to improve Dribbling — our biggest team gap this week"; (3) player attention gap — never observed or ≥5 days overdue → "[Name] hasn't been observed in N days — suggest a coaching focus"; (4) recent past session (≤5 days) → "Write a parent update from yesterday's game"; falls back to static QUICK_ACTIONS for new coaches with < 5 observations; "Personalised for [Team Name]" ⚡ badge shown when dynamic suggestions are active; removed unused ClipboardList import; zero new API routes, DB tables, or migrations
- [x] Better phonetic matching in observation segmentation — `src/lib/player-match.ts` (Soundex + Levenshtein); replaces inline `findPlayerId` in capture/review and quick-capture-widget; 26 unit tests
- [x] Smart practice plan generation from observation data trends — two-window trend analysis (last 7 vs prior 7 days) classifies skills as Declining/Persistent/Improving; AI prompt gets prioritised drill-time rules; Plans page badge shows colour-coded trend pills
- [x] Post-session auto-debrief improvements — multi-session trend context: fetches last 3 prior debriefs, AI generates trend_note comparing to recent history, recurring areas flagged with badge + persistent focus areas chip row
- [x] Drill recommendation engine based on skill gaps — analyzes needs-work observations (last 30 days), surfaces matching drills in a "Recommended for Your Team" carousel at the top of the Drills Library; sorted by gap severity, up to 6 drills
- [x] Game day prep based on opponent tendencies — dedicated form in Plans page: enter opponent name, strengths, weaknesses, key players; AI generates full prep sheet with scouting report (threat levels, defensive assignments), offensive/defensive game plan, set plays, lineup, halftime adjustments, and sideline reminders
- [x] Auto-generate weekly parent newsletter from observations — AI-written team newsletter with player spotlights, home challenges, team highlight, and coaching note; saved as plan type `newsletter` with rich renderer; violet-themed generation button in Plans page

### P3 — Data & Analytics
- [x] Player comparison view (side-by-side skill radar) — /roster/compare; SVG radar chart (shared skills, up to 12 axes), side-by-side progress bars for all skills, per-player summary stats; Compare button in roster header when 2+ players
- [x] Team progress over time (line charts) — SVG line chart in Analytics page: weekly health score % (emerald line, colour-coded dots) + observation volume (orange area), smoothed Bézier curves, gap handling for weeks with no data, trend delta label
- [x] Session-over-session improvement tracking — per-session health score chart in Analytics; purple line + type-colored dots (practice=orange, game=blue, scrimmage=purple, tournament=amber, training=teal), dot size = obs count, trend delta badge
- [x] Practice-to-game transfer score visualization — dual-bar card in Analytics: practice health% vs game health% per player + team aggregate; color-coded transfer delta (green=skills transfer, amber=slight drop, red=sharp drop); sorted by transfer delta
- [x] Export data as PDF/CSV — CSV export (observations, roster, sessions) via GET /api/export; Export dropdown in Analytics page header triggers browser file download
- [x] Observation heatmap (which players get most attention) — player × week SVG grid in Analytics; cell colour = observation intensity (sqrt-scaled orange), count label inside cell, tooltip on hover, colour legend; top 14 players sorted by total obs

### P4 — Innovative Features (Differentiators)
- [x] **Drill Favorites** — star/unstar any drill in the library to save it for quick access; amber star button on each drill card; amber border on favorited cards; "Favorites" filter chip in the drill library header showing count badge; shows empty-state hint when no favorites yet; stored in coach.preferences.favorited_drills (no new table); GET/PATCH /api/drill-favorites; `drill-favorites-utils.ts` pure module (isFavorited, toggleFavorite, addFavorite, removeFavorite, sortWithFavoritesFirst, filterToFavorites, countFavorites, parseFavoritedDrills); 31 unit tests; 618 total
- [x] **Observation Quick Templates** — 20 pre-defined one-tap observation chips on the capture page (10 positive / 10 needs-work, 8+ skill categories); tap template → player picker bottom sheet → saves instantly (no AI, no typing); success toast with haptic feedback; 19 unit tests; `src/lib/observation-templates.ts` + `src/components/capture/quick-templates.tsx`
- [x] **"Quick Capture" widget** — floating Zap button (bottom-right, above mobile nav) on every dashboard page except /capture. One tap opens a bottom sheet: record, auto-segments with AI, saves observations directly — no review step or navigation required. Haptic feedback, live transcript, success/error states, auto-closes after save
- [x] **AI Practice Timer** — start a practice session timer that shows current drill, countdown, coaching cues, and auto-prompts "what did you observe?" between drills. Full guided practice mode — setup screen (library drills + custom), full-screen countdown, rotating coaching cues, between-drill observation capture with player tagging, batch save to session on done
- [x] **Live Game Stat Tracker** — tap-based stat entry during games (made shot, miss, rebound, assist, steal, turnover). Per-player buttons, live box score, saves W/L/T result to session. Stats stored as observations (no new tables). Game Stats button on session detail header for game/scrimmage/tournament sessions
- [x] **"Coach Replay"** — after a session, show a timeline of all observations with timestamps. Chronological view with per-player color coding, relative timestamps (+m:ss), 15-min bucket headers, sentiment breakdown strip, and inline edit (text + sentiment). Located at /sessions/[id]/replay, linked via Replay button in session header
- [x] **Skill Challenge Cards** — AI generates weekly skill challenges for each player based on their growth areas. Shareable cards parents can use for at-home practice. 1-3 challenge cards with steps, success criteria, difficulty badge; "Copy for parents" plain-text export; saved as plan type `skill_challenge` with player_id
- [x] **Smart Substitution Planner** — input game length + player count, AI generates fair rotation ensuring equal playing time. Tracks actual minutes played. 3-step flow: setup (game format + player selection) → rotation grid view → live tracking with game clock, on-court/bench display, and sub alerts
- [x] **Season Storyline** — AI generates a narrative arc of each player's season ("Marcus started exploring dribbling in Week 1 and by Week 8 was running pick-and-roll confidently"). Chapters by phase (Early Season / Building / Breakthrough), current strengths, trajectory, and coach reflection. Accessible from Plans page (player dropdown) and player detail Storyline tab.
- [x] **Drill builder** — AI-assisted custom drill creator. Describe what you want, AI generates drill with setup, coaching cues, variations, and links to curriculum skills. Bottom sheet on /drills with textarea, category chips, duration selector, example prompts; saved to drills table with source='ai'; AI badge in library grid
- [x] **Player Development Goals** — coaches set specific, trackable goals for each player (skill, goal text, target level, target date); Goals tab on player detail page; AI Suggest generates 3 data-driven recommendations from skill proficiency + needs-work observations; status lifecycle: active → achieved / stalled / archived; `player_goals` table (migration 018); GET/POST/PATCH/DELETE /api/player-goals; `goal-utils.ts` pure utility module; 41 unit tests; total suite: 506 tests
- [x] **Team Calendar** — monthly calendar view of sessions; tap any day to see/schedule sessions; colored dots by type; upcoming sessions list; ?date= pre-fills new session form; CalendarDays icon in sidebar
- [x] **Player self-assessment** (age 13+) — players rate themselves on skills (1–5 stars) per skill tracked in proficiency; coach sees self-rating vs coach level side-by-side; saved as plan type `self_assessment`; history with expand/collapse and +/- vs coach indicator; teal theme, COPPA-compliant framing
- [x] **Global Command Palette** — Cmd/Ctrl+K opens a full-text search overlay; searches players, sessions, plans, drills, and quick-nav actions; arrow-key navigation + Enter to go, Escape to dismiss; lazy-loaded in DashboardShell; search button in sidebar + mobile header; 20 unit tests; ARIA roles (dialog, listbox, option, aria-selected)

### P5 — GTM & Growth
- [x] **Plan-aware signup flow** — coaches clicking "Start Coaching" or "Go Pro" on the landing page previously lost their ?plan= intent on arrival at the signup form; signup page now shows a contextual plan banner ("Coach Plan — $9.99/mo · unlocks after account setup"), personalises the heading and CTA, stores intent in sessionStorage; tutorial page reads it on completion and redirects to /settings/upgrade?intent=<tier> instead of /home; upgrade page shows a dismissible welcome banner, highlights the chosen plan card with "Your Choice ✓" badge and orange ring, and relabels the CTA to "Activate <Plan>" — closes the landing-page → paid-plan conversion gap with no new API routes or tables
- [x] **Referral system** — "Invite a coach, get 1 month free" with tracking link and reward; GET /api/referrals lazy-generates a 6-char code, counts referrals via preferences.referred_by_code; signup ?ref=CODE captured and stored on account creation; /settings/referrals page with Copy/Email/Share, stats, reward banner
- [x] **In-app testimonial prompts** — after 10 observations, prompt coach for a review/rating; NPS-style 1-5 star modal; high ratings surface a "share with colleague" mailto CTA; low ratings open a feedback email; snoozed 30 days on Later, permanently dismissed on submit; state stored in localStorage by coachId
- [x] **Social sharing** — "Share your season stats" card modal in Analytics page; preview card (health ring, obs/players/sessions, strengths/focus areas chips); Share button uses Web Share API on mobile, clipboard fallback on desktop; "Share" button in analytics header (disabled when no data)
- [x] **Onboarding email drip** — 4-email sequence (Day 1 welcome, Day 3 Quick Capture tips, Day 7 generate first plan, Day 14 share with parents); fetch-based Resend sender (no SDK); tracking in coach.preferences.drip_sent; POST /api/cron/drip-emails batches all coaches; Vercel cron runs daily at 09:00 UTC; CRON_SECRET auth; 24 unit tests
- [x] **Coach leaderboard** (opt-in) — gamify engagement: observations recorded, plans generated, parent shares sent. Badge system (Rookie Coach → Elite Coach); /settings/leaderboard with opt-in toggle, personal stats card, org rankings with anonymized names, badge chips, score formula breakdown
- [x] **Program landing pages** — `/org/[slug]` white-label pages for YMCA branches to recruit coaches; public API GET /api/org/[slug] returns org, branding, teams, and stats; landing page shows logo (with branding colors), team roster by age group, feature highlights, and coach CTA with org slug pre-filled on signup link
- [x] **"SportsIQ Certified Coach" badge** — coaches who hit 4 milestones (25+ obs, 5+ sessions, 3+ plans, 3+ active players) earn a shareable digital badge; GET /api/certifications auto-grants on first qualifying call (stored in preferences.certified_at); /settings/certification shows amber certificate visual, per-criterion progress bars with %, and Web Share API / clipboard share buttons
- [x] **Seasonal promotions** — auto-detect season start (Sept/Jan/Apr) and show relevant onboarding: "New season? Import your roster and set up your curriculum"; SeasonalPromo component shown on home dashboard for first 21 days of Sept/Jan/Apr; 3 actions (Import Roster, Set Up Curriculum, Generate Plan); roster-aware messaging; per-season localStorage dismissal
- [x] **Team Wins Feed** — celebratory home-dashboard card showing recent badge achievements and achieved development goals (last 14 days); GET /api/team-wins batches player_achievements + player_goals in one endpoint with player-name resolution; amber badge / emerald goal color coding; relative timestamps; hidden when no wins; `src/lib/team-wins-utils.ts` (getWinDate, sortWins, formatTimeAgo); 16 unit tests; 541 total
- [x] **Parent viral loop** — parent portal includes "Is your coach using SportsIQ? Share this with them" CTA when they see how good the reports are
- [x] **Freemium upgrade nudges** — /settings/upgrade pricing page (tier cards + desktop feature-matrix table); Plans & Pricing card in settings hub; UpgradeGate "View Plans" link fixed; FreemiumNudge banner on home page with contextual message (player-limit, AI-usage, generic) dismissible 3 days via localStorage; **wire-up fix**: FreemiumNudge was built but never imported on the home page — now rendered after stats grid so free-tier coaches see the contextual upgrade prompt; PostPracticeDebrief "View Session" link now passes `?fromPractice=1&obsCount=N&playerCount=M` so the Practice Complete Banner (with "Send Parent Updates" + "AI Debrief" scroll CTAs) appears whether a coach ends practice via the timer or the home-page "End Practice" button

### P5.6 — Player Recognition
- [x] **Weekly Star** — AI-picked "Player of the Week" based on the last 7 days of observations; scores each player by positive observation density × category breadth × multi-day consistency; AI writes a personalized spotlight (headline, achievement narrative, growth moment, challenge ahead, coach shoutout); amber-themed card on home dashboard with Generate + Share (Web Share API / clipboard) buttons; saved as plan type `weekly_star` with full renderer in Plans page; `POST /api/ai/weekly-star`; `src/lib/player-spotlight-utils.ts` pure utility module (countPositiveObs, getUniqueCategoriesCount, hasMultipleDays, isEligibleForSpotlight, calculatePlayerScore, rankPlayersByScore, selectWeeklyStarCandidate, getWeekLabel, buildSpotlightShareText, groupObsByPlayer, positiveRatio, filterPositiveObs, isCurrentWeekStar); `weeklyStarSchema` Zod schema; `playerWeeklyStar` prompt in PROMPT_REGISTRY; 50 unit tests; 926 total
- [x] **Weekly Star generation button wired in Plans page** — the home-dashboard card was removed during the UX simplification but the API + renderer remained with no way to trigger generation; added amber "Weekly Star" button to the More Generators collapsible in the Plans page (between Weekly Parent Newsletter and Season Intelligence), `generateWeeklyStar()` async function, "More generators (Weekly Star, Newsletter, Awards…)" label update; added Share/Copy button at the bottom of the `weekly_star` plan renderer that builds a WhatsApp-ready spotlight text and invokes Web Share API on mobile or clipboard on desktop with 2s "Copied!" confirmation; closes the observe→celebrate→share coaching loop with zero new API routes, tables, or AI calls

### P0.5 — Session Intelligence
- [x] **Pre-Session AI Briefing** — one-tap AI briefing card on session detail page (above observations); analyzes last 14 days of observations, player availability, active goals near deadline, and prior session debrief recommendations; shows session goal, up to 3 prioritized focus areas (high/medium/low), up to 4 players to watch (needs_attention/on_a_roll/returning/goal_deadline), 3 coaching tips, energy note, and unavailable-player warning strip; collapse/expand toggle; Refresh button to regenerate; blue accent to distinguish from orange post-session debrief; `POST /api/ai/session-briefing` + `PreSessionBriefingCard` component; 0 new tables

### P1.5 — Planning ↔ Execution Integration
- [x] **Practice Plan → Timer Integration** — "Run Practice" button on practice plans pre-populates the drill timer queue (warmup + drills + scrimmage + cooldown) via `?planId=` URL param; session picker modal shows today's sessions or creates a new one on-the-fly; blue "Loaded from plan" banner confirms the queue source in the timer setup screen
- [x] **Practice Mini-Dropdown Quick-Save** — LIVE timer pill in mobile header now drives a 3-step inline quick-save flow: (1) positive/needs-work toggle + 6 real OBSERVATION_TEMPLATES chips; (2) tap player from roster grid; (3) "Saved!" confirmation auto-resets after 1.4s; saves directly with session_id + category + sentiment = no AI, no navigation, ~4 seconds end-to-end; roster lazy-loads on first open; cache invalidated for home-stats and home-pulse; Voice + End Practice footer preserved

### P5.5 — Game Day Intelligence
- [x] **Opponent Scouting Library** — save opponent scouting profiles (name, strengths, weaknesses, key players, notes) directly from the Game Day Prep form with one tap; load saved profiles back into the form to avoid re-entering data for repeat matchups; profiles stored as `opponent_profile` plan type (no new table); compact "Scouting Library" picker inside the game day form shows saved profiles with a preview of strengths; "Save to Library" icon button with success state; duplicate detection updates existing profile; renderer for saved profiles with color-coded chips (red=strengths, emerald=weaknesses, amber=key players) + "Load into Game Day Prep" action; `opponent-profile-utils.ts` pure utility module; 31 unit tests; 587 total

### P5.7 — Season Intelligence
- [x] **AI Season Summary** — one-tap end-of-season team narrative; analyzes all observations + sessions; AI writes headline, overall assessment, team highlights, skill progress (strength/most_improved/consistent/needs_work badges), player breakthroughs, team challenges, coaching insights, and next-season priorities; saved as plan type `season_summary` (cyan accent); rich renderer with share button (Web Share API / clipboard); generating indicator + stats badge on Plans page; `POST /api/ai/season-summary`; `src/lib/season-summary-utils.ts` pure utility module (getSeasonDateRange, countObsBySentiment, calculateSeasonHealthScore, groupByCategory, getTopCategories, countObservedPlayers, countWeeksOfData, countSessionsByType, hasEnoughDataForSummary, getMostObservedPlayer, buildSeasonShareText, buildSummaryStatsLabel, classifySkillCategory); `seasonSummarySchema` Zod schema; `seasonSummary` prompt in PROMPT_REGISTRY; 50 unit tests; total suite: 976 tests
- [x] **End-of-Season Player Awards** — AI generates a unique, personalized award title for every player based on season observations (e.g. "Defensive Anchor Award", "Creative Passer Award"); every title is guaranteed unique across the team; colorful per-player award cards in Plans page renderer with per-award Share button (Web Share API / clipboard) + Share All Awards for parent group chat; amber-themed Season Awards button in Plans page (below Season Summary); saved as plan type `season_awards`; `POST /api/ai/season-awards`; `src/lib/season-awards-utils.ts` pure utility module (getPlayerObsMap, countPlayerObs, countPlayerPositiveObs, countPlayerNeedsWorkObs, getPlayerPositiveRatio, getPlayerTopCategory, getPlayerBestObs, countPlayersWithObs, sortPlayersByEngagement, buildPlayerAwardData, buildAwardsPayload, hasEnoughDataForAwards, buildAwardShareText, buildAllAwardsShareText, getAwardAccentClasses, isValidAwardTitle, isValidAwardEntry, countAwards, hasAwards, getPlayerAward, getUniqueAwardTitles, allAwardTitlesUnique, buildAwardsSummaryLabel); `seasonAwardsSchema` Zod schema; `seasonAwards` prompt in PROMPT_REGISTRY; 57 unit tests; total suite: 1663 tests
- [x] **Team Personality Profile** — AI discovers and names the team's identity (e.g. "The Grinders 💪") from observation patterns; shows team type + tagline + 2-3 sentence description, 3-5 scored trait bars (0-100, colour-coded emerald/orange/amber/zinc), strengths vs growth areas grid, coaching tips specific to this team type, and team motto; violet accent; Web Share API / clipboard share button; violet "Team Personality" button in Plans page Season Intelligence section (below Season Awards); saved as plan type `team_personality`; `POST /api/ai/team-personality` fetches all observations (≤500) + sessions, computes category breakdown, effort ratio, teamwork ratio, session quality avg; requires ≥5 sessions and ≥20 obs; `PlanType` union updated; `teamPersonalitySchema` Zod schema; `teamPersonality` prompt in PROMPT_REGISTRY; `src/lib/team-personality-utils.ts` pure utility module (buildCategoryBreakdown, getTopStrengths, getTopChallenges, calculateHealthScore, calculateEffortRatio, calculateTeamworkRatio, calculateSessionQualityAvg, hasEnoughDataForPersonality, getTraitBarWidth, getTraitColor, getTraitTextColor, getPersonalityAccentClasses, buildPersonalityShareText, isValidTeamType, isValidTrait, countTraits, getHighestTrait, getLowestTrait, getAverageTraitScore, hasStrongIdentity, buildStatsBadgeLabel, formatCoachingPatternLabel, selectSampleObservations); 68 unit tests; total suite: 2265 tests

### P6 — Strategic Platform Features
- [x] **Multi-season history** — archive past seasons, compare player progress across seasons; season_archives table with player-skill snapshot JSON; GET/POST /api/seasons; /settings/seasons page with archive form, stats strip, collapsible per-player skill snapshot (proficiency level + trend icons)
- [x] **Cross-team analytics (org)** — program directors see aggregate data: which skills are strongest/weakest across all teams, which coaches are most engaged; /admin/org-analytics with summary cards, team health rings, coach engagement leaderboard (weighted score: obs×1 + sessions×3 + plans×2), and cross-team skill breakdown with sentiment strips; linked from admin panel
- [x] **Curriculum marketplace** — orgs can publish their custom curricula, other orgs can import them; GET /api/marketplace lists public curricula with skill counts; POST publish (pro_coach+ tier) toggles visibility with publisher_name; POST import deep-copies curriculum + skills to importer's org and bumps import_count; /marketplace page: search grid, sport badges, import count, one-tap import, Manage Published tab with publish modal; Marketplace link in sidebar
- [x] **Parent engagement scoring** — track which parents open reports, view progress. Identify disengaged families for coach follow-up; GET /api/parent-engagement scores each player's family (engaged/moderate/stale/never_opened/unshared); ParentEngagementPanel on /roster shows stacked progress bar + expandable follow-up list with days-since-viewed and deep-links to player profiles
- [x] **Parent reaction messages in notification bell** — when a parent writes a text message via the share portal emoji-reaction widget (not just an emoji tap), it now surfaces as a pink ❤️ notification in the bell alongside the 5 existing alert types; unread messages from the last 7 days are fetched in parallel with the existing notification queries (no extra round-trip cost); notification title shows the parent's name or "player's parent" with the emoji; body shows the first 80 chars of the message; href links to `/home` where the full Parent Messages card lives; `parent_reaction_message` added to `NotificationType` union in `/api/notifications/route.ts`; `Heart` icon + `text-pink-400` colour added to `TYPE_CONFIG` in `notification-bell.tsx`; closes the parent→coach feedback loop so coaches see parent appreciation in real time without checking the home page; 5 new unit tests (sort ordering, id generation, coexistence with other types); total suite: 2764 tests
- [x] **AI coaching tips** — proactive suggestions: "You haven't observed Sarah in 2 weeks" or "Your team's defense observations are declining — consider a defensive drill block"; color-coded card on home dashboard (alert/suggestion/praise) with action links; 4-hour staleTime cache; shown after 5+ observations; **UI now wired** — `AICoachingTipsCard` (`src/components/home/ai-coaching-tips-card.tsx`) was missing despite the API route being complete; localStorage 4-hour cache per team (at most 6 AI calls/day), 24-hour dismiss per team, refresh-on-demand button, skeleton loading state, silent failure when no tips; amber=alert, blue=suggestion, emerald=praise; each tip includes a tappable action button; positioned after Coaching Streak card on home page
- [x] **Export/print** — Print / Save as PDF for report cards, practice plans (all types), and analytics summary; PrintButton component; @media print CSS converts dark zinc theme to white/black, hides nav chrome and interactive buttons, keeps accent colours; "Print / PDF" button on Report Card tab, plan detail view, and Analytics page header
- [x] **Bulk operations** — select multiple players for group observations, bulk share links, bulk report cards; CheckSquare Select toggle in roster header, circular checkboxes on cards, BulkActionsBar with "Add Observation" modal (batch insert per selected player) and "Share Reports" (batch share link generation + clipboard copy)
- [x] **Integration webhooks** — notify external systems (Slack, email, TeamSnap) on key events; HMAC-SHA256 signed POST to registered HTTPS endpoints; events: observation.created, session.created/updated, plan.created, player.created; /settings/webhooks with test ping, active toggle, one-time secret reveal

### P7 — Performance & PWA
- [x] Service worker for offline app shell — vanilla SW (public/sw.js): Cache-First for _next/static and images, Network-First for pages with /offline fallback, Network-Only for /api; SwRegister in root layout; /offline public page added to middleware allowlist
- [x] Install prompt (add to home screen) with custom banner — PwaInstallPrompt component: listens for beforeinstallprompt, shows after 2 visits, dismissible for 14 days, positioned above mobile bottom nav
- [x] Image/asset lazy loading with blur placeholders — PlayerAvatar component (Next.js Image + zinc shimmer blurDataURL); replaces raw <img> in player-card, player detail, share page; next.config.ts remotePatterns for any HTTPS host; org logo gets loading="lazy"
- [x] Code splitting for dashboard routes — `next/dynamic` for 4 analytics charts (LineChart, SessionTrendChart, HeatmapGrid, TransferScoreChart) extracted to `src/components/analytics/`; shared types/helpers in `chart-utils.ts`; QuickCaptureWidget lazy-loaded in DashboardShell (ssr:false); analytics page 1924→1365 lines
- [x] Optimistic updates on mutations (instant UI feedback) — game tracker stat/undo mutations; `onMutate` updates cache instantly, `onSettled` syncs; stat buttons no longer blocked while pending; error haptic on failure; undo hidden for un-persisted optimistic entries
- [x] Background sync for observations captured offline — useSyncEngine hook wires online/offline monitoring + periodic sync engine; review page falls back to IndexedDB when offline with amber "Saved Locally" success state; sync engine uses mutate() (API route) instead of direct Supabase client; service worker handles BackgroundSync 'sync' event and notifies open clients; 9 unit tests
- [x] Prefetch adjacent pages on hover/focus — usePrefetchAdjacentPages hook prefetches prev/next nav pages on mount; usePrefetchOnIntent factory wires onMouseEnter+onFocus to all sidebar links and onTouchStart to mobile bottom-nav links

### P8 — Accessibility
- [x] ARIA labels on all interactive elements — recording button (aria-label + aria-pressed + aria-live timer), nav landmarks (aria-label + aria-current), team-switcher/bulk-actions/config-editor form controls, toggle switches (role=switch + aria-checked), plan/drill/session action buttons (delete/expand/filter with aria-pressed/aria-label/aria-expanded)
- [x] Keyboard navigation for all pages — global :focus-visible orange ring (globals.css); skip-to-content link in root layout; #main-content id + tabIndex on <main>; useArrowKeyNav hook wired into sidebar and mobile bottom nav (Up/Down/Home/End)
- [x] Screen reader testing with VoiceOver/NVDA — `tests/screen-reader-accessibility.test.tsx`: 64 automated tests covering NotificationBell ARIA (role=dialog, aria-expanded, aria-haspopup, aria-label with unread count, Escape/click-close, decorative icons aria-hidden, notification links, empty state, loading state); pure ARIA label helpers (getBellAriaLabel, getToggleAriaLabel, getLiveRegionPoliteness, buildCountAriaLabel, getAriaPressed); live-region politeness state machine (polite success, assertive error, loading, cleared); focus trap Tab/Shift+Tab cycle and block logic; 816 total tests
- [x] **Voice input for AI Assistant** — mic button in assistant chat input; Web Speech API live transcript shown in textarea while recording; REC pill indicator; tap to start, tap to stop (transcript appended to input for review/edit before sending); send button disabled during recording; aria-label + aria-pressed on mic button; `useVoiceInput` hook (`src/hooks/use-voice-input.ts`) — support detection, continuous recognition, final + interim accumulation, stop() returns trimmed result; 35 unit tests; total suite: 708 tests

### P12 — Multimodal Capture
- [x] **Snap Observation** — photo-based coaching analysis: take/upload a practice photo, AI analyzes player positioning/footwork/technique and generates specific coaching observations; coach selects which to save; Camera button added to /capture page alongside voice/text/audio; `POST /api/ai/snap-observation` uses `snapObservation` prompt + Zod-validated schema; /capture/photo page with file upload, optional focus note, observation chip selector, direct save; Media tab on player detail page wired to `/capture/photo?playerId=` instead of stub; `snap-observation-utils.ts` pure module (isValidSentiment, parseSnapObservation, filterValidObservations, groupObservationsByPlayer, countBySentiment, sortObservationsByValence, hasObservations, buildObservationSummary, deduplicateObservations, isLikelyNonSportsPhoto); 44 unit tests; total suite: 717 tests
- [x] Focus trap in modals/dialogs — `useFocusTrap` hook; Tab/Shift+Tab cycling, Escape-to-close, focus restoration; applied to QuickCaptureWidget, TestimonialPrompt, WelcomeTour, BulkActionsBar; aria-labelledby on all dialogs; 25 unit tests
- [x] Color contrast verification (WCAG AA minimum) — `src/lib/color-contrast.ts` utility (hexToRgb, relativeLuminance, contrastRatio, passesAA/AALarge/AAA); 56 tests across dark/light/high-contrast themes; fixed `button.tsx` default variant (`text-white`→`text-zinc-950` on orange: 2.8→7.1:1); fixed `skip-to-content` link (dark text on orange: 7.1:1); added `.light :focus-visible { outline-color: #c2410c }` (orange-700, 5.18:1 on white, was 2.8:1)
- [x] Reduce motion preference support — useReducedMotion hook (reactive, SSR-safe); global CSS `* { animation-duration: 0.01ms; transition-duration: 0.01ms }` disables all Tailwind transition-*/animate-* utilities; useHaptic skips navigator.vibrate(); PullToRefresh skips inline-style transitions + haptics; 13 unit tests
- [x] High contrast mode toggle — useHighContrast hook (localStorage, SSR-safe); .high-contrast CSS class lifts zinc surfaces/text to WCAG AA 4.5:1 (black bg, white text, #555 borders); Settings page Contrast icon toggle card with On/Off pill + aria-checked; 17 unit tests

### P11 — Coach Productivity
- [x] **Observation Feed** — `/observations` page: full-text search, server-side filters (player, sentiment, date range 7/14/30/90/all), client-side category filter; player avatar with initials + colour hash, sentiment badge (emerald/red/zinc), category chip, source icon, relative timestamp; expand/collapse for long text; deep-link to session; paginated 50/page; pull-to-refresh; Eye icon in sidebar (between Sessions and Calendar); `ilike` operator added to `/api/data/route.ts`
- [x] **Inline edit + delete on Observation Feed** — every card now has a pencil edit button and trash delete button in the bottom action row; clicking edit expands an inline form (sentiment toggle, category select, pre-filled textarea) without navigation or a modal; clicking delete shows a two-tap "Delete? Yes / No" confirmation strip; both use optimistic updates via `mutate()` with automatic rollback on failure; also fixes `ObservationSource` TypeScript type to include `template` and `observer` (added by P31 migration 024 but missing from the type), and adds `ClipboardList` + `Users` source icons for those values
- [x] **Notification Center** — bell icon in sidebar + mobile header; GET /api/notifications aggregates 4 alert types per team: unobserved players (14+ days), active goal deadlines (within 7 days or overdue), today's sessions with no observations yet, and achievements earned in last 48h; orange badge count, capped at 9+; popover panel with per-type color-coded icons (amber=unobserved, orange=goal, blue=session, emerald=achievement), priority dots (red/orange/emerald), mark-read per item or "mark all read"; read state persisted to localStorage; Escape/outside-click dismiss; ARIA: aria-expanded, aria-haspopup=dialog, role=dialog; 15 unit tests (buildNotificationId, priorityOrder, sortNotifications); 556 total
- [x] **Private Coach Notes** — confidential per-player journal tab (never shown in parent reports); GET/POST/PATCH/DELETE `/api/player-notes`; pin/unpin, inline edit, search, expand-long-notes, optimistic updates; "Notes" tab on player detail page (between Goals and Challenges); amber theme with Lock privacy notice; `player_notes` table (migration 019); `player-notes-utils.ts` pure utility module (sortNotes, countPinnedNotes, truncateNote, searchNotes, isValidNoteContent); 26 unit tests; 613 total
- [x] **Observation Highlights** — coaches star (★) their best observations to create a curated highlights collection per player; `is_highlighted` boolean on observations (migration 020, partial index); star toggle on every observation card in observation feed + player detail; amber accent on highlighted cards; "Highlights" filter chip in feed with count badge; "Highlights only" toggle in player detail; optimistic toggle with rollback; pure utility module (`filterHighlighted`, `countHighlighted`, `hasHighlights`, `sortHighlightedFirst`, `buildHighlightsSummary`); 29 unit tests; total suite: 673 tests
- [x] **AI Game Recap Generator** — after a game/scrimmage/tournament session, one-tap generates a shareable narrative recap (result headline, intro, key moments with player attribution, player highlights with optional stat lines, team offensive/defensive/effort notes, coach message, looking-ahead close); saved as plan type `game_recap` (rose accent); `GameRecapCard` component on session detail page with generate/refresh/clipboard-copy controls; `POST /api/ai/game-recap` route fetches session + observations and calls AI; rich renderer in Plans page; `game-recap-utils.ts` pure module (parseResultOutcome, getResultColor, getResultBadgeClasses, buildGameTitle, isGameSession, countBySentiment, calculatePerformanceScore, selectTopHighlights, buildShareText, hasEnoughDataForRecap, groupObsByPlayer); 60 unit tests; total suite: 812 tests

### P10 — Coach Operations & Logistics
- [x] **Session attendance tracking** — coaches mark present/absent/excused for each player per session; `/sessions/[id]/attendance` page with summary strip (counts + %), quick "mark all" buttons, per-player toggle row, delete-then-insert save; `session_attendance` table + migration; `AttendanceStatus` + `SessionAttendance` types; Attendance button in session detail header; 8 unit tests
- [x] **Attendance trend report** — GET /api/attendance-stats (team + player modes); TeamAttendancePanel on roster page with summary bar, session-dot sparklines, color-coded % bars; attendance ring + dots + breakdown in player detail overview; 19 unit tests
- [x] **Recurring session scheduler** — create a weekly recurring session (e.g. every Tuesday 4-5pm) that auto-generates sessions for the whole season; RecurringSessionsPanel on sessions page with day-of-week picker, time range, date range, Generate (Zap) button bulk-inserts missing sessions, collapse/expand, delete; migration 015; GET/POST /api/recurring-sessions + DELETE/POST-generate per-id routes
- [x] **Player Injury & Availability Tracker** — coaches mark each player as available / limited / injured / sick / unavailable with optional reason, expected return date, and private notes; `player_availability` table (migration 016) preserves full history; GET/POST/DELETE `/api/player-availability` (deduplication: latest record per player); `AvailabilityBadge` component (5 colour-coded statuses with icons); `PlayerAvailabilityModal` bottom-sheet on player card; amber summary strip on roster page when any player is restricted; `deduplicateByPlayer` / `isPlayerAvailable` / `countUnavailablePlayers` pure utilities; 21 unit tests
- [x] **Player Achievement Badges** — 10 milestone badges (7 auto-awarded, 3 manual coach awards); `player_achievements` table (migration 017, unique per player+badge); GET/POST `/api/player-achievements` (auto-check all criteria in one call, or award manually with optional note); `AchievementBadgesPanel` component in player detail Overview tab: colour-coded badge grid with earned/locked states, Check button (evaluates + awards newly earned), Award modal for coach_pick / most_improved / rising_star; `evaluateAutoBadges` pure utility; 23 unit tests; total suite now 465 tests

### P9 — Testing & Reliability
- [x] Component tests for key UI (roster card, recording button, plan card) — 93 new tests in tests/player-card.test.tsx (25), tests/recording-button.test.tsx (35), tests/plan-type-config.test.ts (33); total suite now 251 tests
- [x] Integration tests for API routes (config, segment, plan, share) — 44 tests: config resolver pure functions, handleAIError, config GET/PUT/DELETE route auth+validation, share [token] GET (404/410/403/200), share create POST (401/400/404/200); Supabase mocked via vi.hoisted; suite now 295 tests
- [x] E2E test: signup → onboarding → capture → review → save — `tests/e2e/signup-onboarding-capture.spec.ts`; public flows (signup form, onboarding sport/team) run unconditionally; auth-gated flows use `E2E_TEST_EMAIL`/`E2E_TEST_PASSWORD` env vars with graceful skip
- [x] E2E test: generate plan → view plan → delete plan — `tests/e2e/plans-flow.spec.ts`; mocks `/api/me`, `/api/data`, `/api/ai/plan`, `/api/data/mutate`
- [x] E2E test: create share link → view parent portal — `tests/e2e/share-flow.spec.ts`; parent portal tests run without auth (public route); share creation tests require credentials
- [x] E2E test: admin panel → invite coach → change role — `tests/e2e/admin-flow.spec.ts`; mocks `/api/admin/coaches` GET/POST/PATCH; includes non-admin access guard test
- [x] Error boundary components on all pages — global-error.tsx (layout-level), app/error.tsx (public pages), (dashboard)/error.tsx (all dashboard pages), (auth)/error.tsx (login/signup/onboarding); AlertTriangle + reset() + home link; prevents blank-page crashes
- [x] Sentry or similar error tracking integration — lightweight self-hosted tracker: `src/lib/error-tracking.ts` (captureException/captureMessage/initErrorTracking, batched fetch with 600ms debounce, sessionStorage session ID); POST /api/errors (IP rate-limit 100/hr, Supabase persistence with graceful fallback, structured console logs); ErrorTrackingInit component in root layout (window.onerror + unhandledrejection); all 4 error boundaries (global/root/dashboard/auth) wired up; migration 013_error_tracking.sql; 23 unit tests
- [x] API rate limiting on AI endpoints — 20 req/hour per coach (configurable via AI_RATE_LIMIT_PER_HOUR); Redis sliding window with in-memory fallback; RateLimitError → 429 + Retry-After header; handleAIError() shared handler across all 14 AI routes; 10 unit tests

### P13 — Coach Growth & Reflection
- [x] **Coach Reflection Journal** — AI-guided post-session reflection; analyzes observation data (player coverage gaps, sentiment ratios, top categories, standout moments, prior session comparison) and generates 4 personalized open-ended questions; coach fills in answers inline on session detail page; saved as plan type `coach_reflection` with rich Q&A renderer in Plans page; PATCH endpoint to save answers to existing draft plan; `coach-reflection-utils.ts` pure module (17 functions: isValidReflectionAnswer, countAnsweredQuestions, hasAllAnswers, hasAnyAnswer, truncateAnswer, getCategoryLabel, getCategoryColor, isValidCategory, countBySentiment, calculateHealthScore, countObservedPlayers, getUnobservedPlayerIds, aggregateByCategory, buildReflectionShareText, buildReflectionPreview, reflectionProgress, groupQuestionsByCategory); fixed PlanType union to include game_recap/weekly_star/season_summary; 58 unit tests; total suite: 1034 tests

### P14 — Real-Time Player Intelligence
- [x] **Player Momentum Score** — algorithmic score (0–100) measuring each player's current trajectory from 4 equally-weighted factors (0–25 each): sentiment ratio of recent observations, observation consistency across sessions, skill proficiency trend (improving/plateau/regressing), and goal progress (active/achieved/stalled). `Rising` (≥70), `Steady` (40–69), or `Needs Attention` (<40) tier badges shown on roster player cards. Full factor breakdown card (mini bars + explanatory detail) on player detail Overview tab. 🔥 Hot Streak callout when score ≥ 80. `GET /api/team-momentum?team_id=` returns all player scores in one batched DB call (observations, proficiency, goals). `src/lib/momentum-utils.ts` pure utility module (positiveRatio, calculateSentimentFactor, countObservedSessions, calculateConsistencyFactor, calculateSkillTrendFactor, calculateGoalProgressFactor, calculateMomentumScore, getMomentumTier, getMomentumLabel, getMomentumColor, getMomentumBadgeClasses, getMomentumDirection, formatMomentumScore, sortByMomentum, filterByTier, getTopMomentumPlayer, getRisingPlayers, getNeedsAttentionPlayers, averageMomentumScore, buildMomentumSummary, isHotStreak); 64 unit tests; total suite: 1098 tests
- [x] **Coaching Pattern Insights** — self-coaching analytics card on the Analytics page helping coaches identify blind spots in their observation habits. Three Gini-based metrics: Coverage (% of active players observed in last 14 days, weighted 35%), Balance (Gini-coefficient evenness across roster, weighted 35%), Consistency (% of last 8 weeks with ≥1 observation, weighted 30%). Overall score yields label: Comprehensive (≥75) / Developing (45–74) / Focused (<45). UI shows 3 colour-coded metric pills, "most observed" and "no observations yet" player lists, and a prioritised insight strip (alert → suggestion → praise). Guarded by `hasSufficientPatternData` (≥5 obs, ≥2 players). No new API route — computes from already-fetched analytics data. `src/lib/coach-pattern-utils.ts` pure utility module (giniCoefficient, calculateObservationBalance, findUnobservedPlayerIds, calculatePlayerCoverageRate, getObservationCountByPlayer, getMostObservedPlayers, getLeastObservedPlayers, getSentimentBreakdown, isoWeekKey, getLastNWeekKeys, getWeeklyObservationCounts, calculateConsistencyRate, calculateCoachingPatternScore, getCoachingPatternLabel, getCoachingPatternColor, buildCoachingPatternInsights, hasSufficientPatternData); 78 unit tests; total suite: 1176 tests
- [x] **Team Skill Trends Card** — home-dashboard widget comparing this week's vs last week's positive ratio per skill category; shows up to 3 improving skills (emerald) and up to 3 declining skills (red) with delta badges (+12%, −8%) and observation counts; declining skills are tappable links to the Plans page; computed entirely from the already-fetched 14-day pulse observations (no extra DB call, no AI); shown only when ≥5 scored observations exist across either window. `src/lib/skill-trend-utils.ts` pure utility module (formatSkillLabel, filterByCategory, filterNeedsWork, filterPositive, filterScored, calcPositiveRatio, getCategories, countByCategory, getTrendDirection, formatTrendDelta, getTrendColor, getTrendBgColor, isSignificantTrend, hasEnoughDataForTrends, buildSkillTrendForCategory, buildSkillTrends, getTopImprovingSkills, getTopDecliningSkills, sortByAbsoluteDelta, filterHasEnoughData, calculateTeamSkillProfile, buildTrendSummary); 56 unit tests; total suite: 1232 tests

### P15 — In-Game Intelligence
- [x] **AI Half-Time Adjustments** — game-session card that analyzes observations captured in the first half and generates tactical adjustments for coaches during the break; shows momentum level (Building/Even/Trailing) with colour-coded banner, what's working (emerald), what needs fixing (amber), 2–3 tactical adjustments with immediate/secondary priority badges, a player to feature in the second half, and a motivational halftime message; Refresh and Copy-to-clipboard buttons; purple accent to distinguish from blue pre-session briefing and rose game recap; requires ≥3 observations before generating; `POST /api/ai/halftime-adjustments`; `HalftimeAdjustmentsCard` component on session detail page (game/scrimmage/tournament only, positioned between observations and post-session debrief); no new tables; `src/lib/halftime-utils.ts` pure utility module (selectPositiveObs, selectNeedsWorkObs, selectPlayerObs, positiveRatio, countObsByCategory, countObsBySentiment, classifyMomentum, getMomentumLabel, getMomentumColor, getMomentumBannerClasses, groupObsByPlayer, getTopPerformers, getStrugglingPlayers, getStrongestCategories, getWeakestCategories, hasEnoughDataForHalftime, buildHalftimeSummaryLines, buildCategoryBreakdown, buildHalftimeShareText); 58 unit tests; total suite: 1290 tests

### P16 — Post-Session Player Communication
- [x] **Player Session Messages** — one-tap AI card on session detail page (teal accent, between Coach Reflection and Media) that generates warm, parent-friendly, send-ready message snippets for each observed player after any session type; each snippet has a 2–3 sentence WhatsApp/SMS-ready message, a specific highlight from that player's session observations, and one next-focus growth tip; individual Copy buttons per player + Copy All; saved as plan type `player_messages` with rich card renderer in Plans page (emerald highlight chip, orange next-focus chip, team note strip); `POST /api/ai/player-session-messages`; `playerSessionMessagesSchema` Zod schema; `playerSessionMessages` prompt in PROMPT_REGISTRY; `src/lib/player-session-messages-utils.ts` pure utility module (filterPlayerObs, getObservedPlayerIds, countObservedPlayers, groupObsByPlayer, filterObsForPlayer, getPositiveObsForPlayer, getNeedsWorkObsForPlayer, hasEnoughDataForMessages, getTopSkillCategory, calculatePositiveRatio, sortPlayersByMostObserved, buildSessionLabel, buildPlayerMessageShareText, isValidMessageText, countMessages, truncateMessage, extractPlayerNamesFromObs, hasGeneratedMessages, buildPlayerObsPayload); 58 unit tests; total suite: 1348 tests

### P17 — Session Quality Feedback Loop
- [x] **Session Quality Rating** — coaches rate each session 1–5 stars after it ends, creating a self-assessment dataset that feeds an analytics quality card; amber star widget in session detail page (above Coach Notes) shows filled/hollow stars with a tap-to-rate interaction, optimistic save via existing `mutate()` helper, and a colour-coded label (Poor → Excellent) with inline "Saved" confirmation; Analytics page gains a "Session Quality" card showing average rating with filled-star display, rating distribution bars (5 down to 1), high-quality % chip (sessions scoring 4–5★), and a trend badge vs prior 5 sessions (Improving/Declining/Stable with ±delta); migration 021 adds `quality_rating INTEGER CHECK (1–5)` to sessions + index; `Session` type updated; no new API routes (uses generic mutate endpoint); `src/lib/session-quality-utils.ts` pure utility module (isValidRating, getRatingLabel, getRatingColor, getRatingBgColor, filterRated, filterByType, calculateAverageRating, countByRating, getHighQualityRate, getBestSession, getQualityTrend, formatTrendDelta, sortByQuality, getRollingAverageQuality, getQualityMotivationMessage, buildQualitySummary, getRatedFraction); 45 unit tests; total suite: 1393 tests
- [x] **Quality rating on Practice Timer Done screen** — "How did practice go?" 1–5 star widget added to the Done screen (shown before the Save Observations button), capturing ratings at the most natural end-of-practice moment without requiring navigation to the session detail page; saves optimistically via `mutate()` to `sessions.quality_rating`; failures are silent so the save flow is never blocked; colour-coded label feedback (Poor → Excellent); feeds the analytics quality card with higher rating capture rates

### P18 — Coach-Parent Communication
- [x] **Team Announcements** — coaches post short team-wide updates (practice cancelled, bring water bottles, etc.) that appear prominently at the top of the parent portal for every player on that team; `AnnouncementsPanel` component on Sessions page — collapsible card with announcement list (title, body, expiry countdown), a compose form (title + body textarea + expiry chip picker: 3d/7d/14d/never), and per-announcement delete with loading state; orange accent "From the coach" section in parent portal share page shows all active announcements above the player report card; active filter uses Supabase `.or(expires_at.is.null,expires_at.gt.NOW)` so expired posts auto-disappear without a cron; GET/POST/DELETE `/api/team-announcements`; `team_announcements` table (migration 022) with cascade delete on team removal; `AnnouncementExpiry` type + `TeamAnnouncement` interface in `database.ts`; `src/lib/announcement-utils.ts` pure utility module (isValidTitle, isValidBody, expiryToDate, expiryLabel, isActive, filterActive, filterExpired, sortByNewest, timeUntilExpiry, countActive, hasAnnouncements, hasActiveAnnouncements, truncateBody, buildAnnouncementShareText, getLatestActive); added `.or()` to `buildChain` test helper so share route tests remain green; 37 unit tests; total suite: 1430 tests

### P19 — Daily Engagement & Habit Formation
- [x] **Coaching Streak Tracker** — consecutive-day activity counter (Duolingo-style) on home dashboard that drives coaches back to the app daily; activity = any observation OR session logged for the team; `StreakCard` shows flame icon + count, "at risk" amber pulse animation when yesterday had activity but today doesn't, progress bar toward next milestone (3/7/14/30/60/100-day), milestone icons, "Best!" badge for new personal records; `GET /api/streak?team_id=` fetches last 365 days of observation + session dates; shown after first observation; `src/lib/streak-utils.ts` pure utility module (getDayKey, normalizeDates, calculateCurrentStreak, calculateLongestStreak, getEarnedMilestones, getNextMilestone, getDaysToNextMilestone, getStreakMessage, buildStreakData, isNewRecord, streakPercentToNextMilestone, formatStreakCount); 57 unit tests; total suite: 1487

### P21 — Coach-Parent Last Mile
- [x] **One-tap Send for Player Messages** — each AI-generated player message card now has a teal "Send" button alongside the existing Copy button; on mobile triggers the native Web Share API (WhatsApp, SMS, email, etc.); on desktop opens WhatsApp Web with the message pre-filled; "Sent!" confirmation state resets after 2s; same Send + Copy buttons added to the Plans page player_messages renderer via a `PlayerMsgItem` sub-component; no new API routes or tables

### P22 — Coach-Team Communication
- [x] **Team Group Chat Message Generator** — one-tap AI card on session detail page (emerald accent, after Player Session Messages) that generates a WhatsApp/SMS-ready team-wide parent group chat message after any session type; editable textarea so coaches can personalise before sending; Web Share API on mobile / WhatsApp Web on desktop; Copy to clipboard fallback; team highlight strip, coaching-focus chips, optional next-session note; saved as plan type `team_group_message` with rich renderer in Plans page (emerald accent, share + copy buttons); `POST /api/ai/team-group-message` fetches session + observations, builds observation summary (positive count, needs-work count, top categories, standout moment); `teamGroupMessageSchema` Zod schema; `teamGroupMessage` prompt in PROMPT_REGISTRY; `src/lib/team-group-message-utils.ts` pure utility module (getSessionEmoji, getSessionTypeLabel, buildSessionLabel, getPositiveObsCount, getNeedsWorkObsCount, extractFocusAreas, hasEnoughDataForGroupMessage, countChars, truncateMessage, buildPreviewText, isValidGroupMessage, formatGroupMessage, buildShareText, buildWhatsAppUrl, countFocusAreas, hasNextSessionNote); 62 unit tests; total suite: 1549 tests

### P31 — Collaborative Observation Capture
- [x] **Observer Mode** — shareable link lets a parent volunteer or assistant coach capture template-based observations during practice without an account; coach taps "Observer" in session detail header → link copied to clipboard (24 h validity); observer opens the URL and uses a 3-tap flow (sentiment toggle → template chip → player grid) to save observations directly into the live session in ~2 seconds; stateless HMAC-SHA256 tokens (no new DB table); IP-rate-limited to 50 obs/hour; observations tagged `source: observer`; migration 024 extends observations.source CHECK to include `template` and `observer` (fixes existing bug); `POST /api/observer-link` (auth-required) · `GET+POST /api/observe/[token]` (public) · `/observe/[token]` mobile-first public page · `src/lib/observer-utils.ts` pure utility module (generateObserverToken, validateObserverToken, buildTokenPayload, parseTokenPayload, isExpired, buildObserverUrl, isValidTemplateId, getTemplateById, getPositiveTemplates, getNeedsWorkTemplates, buildObservationPayload, checkObserverRateLimit, getObserverRateKey, formatObserverCount, getSessionTypeLabel); 45 unit tests; total suite: 1860 tests

### P33 — Daily Practice Habit
- [x] **Drill of the Day** — home dashboard card showing one targeted drill per day, deterministically selected by team+date hash from the sport's drill library filtered to the team's top needs-work skill category (zero extra AI or DB calls — reuses already-fetched Team Pulse data). Shows drill name, category chip, key coaching cue, duration, and player count with one-tap "See Drills" link to the filtered drill library. Dismissed per-day via localStorage. Closes the analyse→act loop: "dribbling is your #1 gap" → "run Figure 8 Dribble today". `src/lib/drill-of-day-utils.ts` pure utility module (getDayKey, buildDayHash, matchesDrillCategory, filterDrillsByCategory, sortDrillsForSelection, selectDrillOfDay, hasEnoughDataForDrillOfDay, getDrillCategoryLabel, getDrillDurationLabel, getDrillCues, getDrillEquipmentLabel, buildDrillDismissKey, buildDrillViewUrl, buildDrillShareText, getDrillPlayerCountLabel); 56 unit tests; total suite: 1916 tests

### P34 — End-of-Practice Ritual
- [x] **Practice Huddle Script** — one-tap AI card on session detail page (lime accent, after Team Group Message) that generates a 30-second end-of-practice script the coach reads aloud to the team: spotlights one player by name from the session's most-observed positive performer, assigns a concrete weekly team challenge based on top needs-work skill, and closes with a chant. Optional "next session info" text input pre-fills the next-session hint. Copy button for clipboard. Saves as plan type `huddle_script` with full renderer in Plans page (lime accent, copy button). `POST /api/ai/huddle-script` fetches session obs + roster, computes spotlight (player with most positive obs), and delegates to AI (80-word cap, ~30s read time). `huddleScriptSchema` Zod schema + `HuddleScript` type in `schemas.ts`. `huddleScript` prompt in `PROMPT_REGISTRY`. `src/lib/huddle-script-utils.ts` pure utility module (isValidPlayerSpotlight, isValidHuddleScript, isValidChallengeText, hasPlayerSpotlight, hasNextSessionHint, hasEnoughDataForHuddle, filterPositiveObs, filterNeedsWorkObs, groupObsByPlayer, countPositiveObsForPlayer, getTopCategoryForPlayer, getPlayerWithMostPositiveObs, getBestPositiveObs, extractTeamStrengths, extractTeamChallenges, buildPlayerSpotlightPayload, formatSpotlightLine, buildHuddleShareText, truncateScript, buildPreviewText, countWordsInScript, estimateReadingSeconds, isScriptReadableInSeconds, buildHuddleSessionLabel, buildObsSummary); 73 unit tests; total suite: 1989 tests

### P35 — Personal Connections
- [x] **Player Birthday Card** — home dashboard card that surfaces upcoming player birthdays (today + next 6 days) so coaches never miss a player's big day; amber themed with high-priority "Today!" banner for same-day birthdays; one-tap "Send" triggers Web Share API on mobile or opens WhatsApp Web on desktop with a pre-written warm birthday message to the parent; dismissed per-team per-day via localStorage; `birthday_today` added as a high-priority notification type in the bell (🎂 Cake icon); no new tables or API routes — reads `date_of_birth` already in the players table; `src/lib/birthday-utils.ts` pure utility module (isValidDob, parseDobMonthDay, hasDob, playersWithDob, getDaysUntilBirthday, isBirthdayToday, isBirthdaySoon, getAgeThisBirthday, filterBirthdaysToday, filterUpcomingBirthdays, filterAllUpcomingBirthdays, hasUpcomingBirthdays, countBirthdaysToday, countUpcomingBirthdays, sortByUpcomingBirthday, formatBirthdayLabel, buildBirthdayMessage, buildBirthdayShareText, buildWhatsAppUrl, buildBirthdayWhatsAppUrl, getBirthdayDismissKey); 75 unit tests; total suite: 2064 tests
- [x] **Birthday field on Add/Edit Player forms** — the birthday feature was non-functional because neither Add Player (`/roster/add`) nor Edit Player (`/roster/[playerId]/edit`) had a `date_of_birth` input; both forms now include an optional birthday date picker (native `<input type="date">`, YYYY-MM-DD) with a helper note; field wired into the mutate payload for both insert and update; Edit form pre-fills from the existing player record

### P38 — Practice-to-Parent Pipeline
- [x] **Practice Complete Banner** — when a coach saves observations from the Practice Timer and lands on the session detail page, a green "Practice complete! 🎉" banner appears at the top with the observation/player count and two one-tap CTAs: "Send Parent Updates" (scrolls to Player Session Messages) and "AI Debrief" (scrolls to AI Post-Session Analysis); banner is dismissible; timer navigates with `?fromPractice=1&obsCount=N&playerCount=M` URL params; section IDs added to AI Debrief and Player Messages for reliable scroll-targeting; closes the practice→parent communication loop in two taps
- [x] **Celebratory post-save screen on observation review** — the plain "Observations Saved" success screen was replaced with a proper coaching win moment: shows "Nice work, Coach! 🎉" with player count, sentiment summary pills (positive / needs-work), and up to 2 named positive observation highlights so coaches feel immediate payoff from their work; primary CTA renamed from "View Session" to "AI Debrief & Parent Updates" so coaches know exactly what they'll get on the other side; non-session captures get a contextual tip encouraging them to start a session next time to unlock AI features; closes the capture→celebrate→act loop at exactly the right moment

### P37 — Game Results & Season Record
- [x] **Season Record Card** — coaches see their W-L-T record at a glance; Analytics page gains a `SeasonRecordCard` (emerald accent) showing: large W-L-T display with record label (Perfect Season / Strong Season / Winning Record / Building), win % tri-color bar (emerald=wins / zinc=ties / red=losses), recent-form bubbles for last 5 games (W/L/T circles), and per-type breakdown (game vs scrimmage vs tournament) when results exist across multiple types; sessions list shows a small W/L/T pill badge (emerald / zinc / red) next to the session-type chip for every session with a recorded result; `parseResult` handles win/W/loss/L/lose/tie/T/draw/d formats from the game tracker; `src/lib/season-record-utils.ts` pure utility module (isGameType, parseResult, filterGameSessions, filterSessionsWithResults, sortSessionsByDate, getLastNGameSessions, calculateSeasonRecord, totalGamesPlayed, getWinPct, getWinPctLabel, hasEnoughDataForRecord, isWinningRecord, isUnbeatenRecord, hasTies, formatRecordString, getRecentFormArray, getRecentFormString, buildSeasonRecordSummary, getCurrentStreak, formatStreakLabel, getRecordLabel, getRecordColor, getResultBadgeClasses, getResultLabel, countBySessionType); 78 unit tests; total suite: 2142 tests
- [x] **Quick Game Result entry** — coaches can record W/L/T + optional score (e.g. "42-38") directly in the session info card for game/scrimmage/tournament sessions; tap Win / Loss / Tie, optionally type the score, tap Save — done in ~3 seconds without navigating to the full game stats tracker; result stored as "win 42-38" in session.result; `parseResult` updated to handle the prefixed format so SeasonRecord card, recentForm bubbles, and W/L/T badges all continue to work; existing results rendered as a colour-coded trophy badge (emerald=win, red=loss, zinc=tie) with score inline and a one-tap Edit button; new `extractScore()` and `buildResultString()` helpers; optimistic save via generic `mutate()`; 17 new unit tests; total suite: 2196 tests
- [x] **Inline W/L/T on sessions list** — game/scrimmage/tournament rows with no logged result now show compact W / L / T buttons directly in the session list without navigating to the session detail; optimistic update shows the colour-coded badge immediately; rollback on API failure; `stopPropagation` on button click prevents the card-level Link from firing; coaches still navigate to session detail for score entry
- [x] **Team Health Score on home dashboard stats grid** — the three bare number cards (Players / Observations / Sessions) were joined by a fourth "Team Health" card showing the percentage of positive observations in the last 14 days; a ↑/↓ delta badge shows week-over-week change so coaches know at a glance whether their recent coaching is trending up or down before opening any other card; score hidden as "—" until ≥5 scored observations exist so new coaches never see a misleading 0%; colour-coded emerald (≥70%) / orange (50-69%) / red (<50%); stats grid changed from `grid-cols-3` to `grid-cols-2 sm:grid-cols-4`; one extra lightweight query (last 28 days, `sentiment + created_at` only) in the existing home-stats queryFn; zero new API routes, DB tables, or migrations; gives coaches a north-star retention metric that drives daily check-ins and observation habit formation
- [x] **Session quality stars on sessions list + last session card** — coaches who rate their practices (1–5 stars via the timer Done screen or session detail page) previously had no way to see those ratings when browsing the sessions list; now every rated session shows a row of 5 amber star icons (filled vs hollow) in the top-right corner of its list card beside the observation count; the "Last Session" card on the home dashboard also surfaces the rating inline with the session type label; both queries updated to explicitly fetch `quality_rating`; closes the observe→rate→reflect loop at the two most-visited screens with zero new API routes or DB changes

### P1 — Session Creation
- [x] **Session Snapshot Card on session detail page** — coaches who open a session detail page after practice previously had no instant "how did it go?" summary — they had to scroll through 10+ AI cards to find trends; new collapsible `SessionSnapshotCard` appears at the top of the Observations section (above the Coverage Tracker) showing: a colour-coded sentiment ratio bar (Excellent/Good/Mixed/Needs Work/Tough Day with % label), top strength skill chips (emerald, up to 3 categories with obs count), top gap skill chips (amber, up to 3 categories with obs count), and a standout player chip (amber star, shown when any player has ≥2 positive observations); hidden until ≥3 observations exist; computed entirely from already-fetched observation data via pure utility functions — zero extra API calls, DB queries, or AI usage; `src/lib/session-snapshot-utils.ts` pure utility module (buildSessionSnapshot, countBySentiment, countScored, getPositiveRatio, getUniqueObservedPlayerCount, getCategoryCounts, getTopCategoriesBySentiment, getPlayerObsCounts, getStandoutPlayer, hasEnoughDataForSnapshot, formatSnapshotCategory, getHealthLabel, getHealthColor, getHealthBarColor); 55 unit tests; total suite: 2759 tests
- [x] **Last Session Summary card on home dashboard** — when no practice is active and no session is scheduled today, coaches returning to the app now see a compact "Last session · X days ago" card between quick actions and the Getting Started checklist; shows session type emoji, label, and observation count with a one-tap "View" link to the session detail page; auto-hides after 7 days of inactivity; bridges the post-practice gap: coaches opening the app 2+ days after practice immediately see what they captured and are one tap away from AI debrief, player messages, and parent sharing; one lightweight DB query (staleTime 5 min), disabled when practice is active
- [x] **Player-grouped observations on session detail page** — observations were previously shown as a flat chronological list, forcing coaches to mentally scan and count by player; now grouped by player (sorted by obs count desc, "Team / General" at end) with a per-player header showing name, jersey number, and ✓/✗ sentiment totals; player name removed from individual cards to avoid redundancy; coaches immediately see "Marcus (3 obs: ✓2 ✗1)" at a glance post-practice; computed with `useMemo` from already-fetched data — zero extra API calls or DB round-trips
- [x] **Tournament and Training session types in New Session form** — the New Session form only showed 3 of 5 session types (Practice / Game / Scrimmage), so coaches running tournament days or extra training sessions had no way to log them via the UI even though the DB, filter bar, analytics, W/L/T badges, and season record all supported all 5 types; added Tournament (🏆, "Multi-game tournament") and Training (💪, "Skills & conditioning") to the type grid; grid changed from `grid-cols-3` to `grid-cols-2 sm:grid-cols-3`; opponent field now also shown for Scrimmage and Tournament (relabelled "Tournament Name" for that type with contextual placeholder); save payload saves opponent for all three competitive types; `isGameType()` already included tournament so W/L/T logging, season record card, halftime adjustments, and game recap all work immediately for new tournament sessions

- [x] **Roster Practice Mode** — when a practice session is active, the roster page becomes a live coaching tool instead of a static list: an animated "Practice Active · X/Y observed" banner appears at the top; each unobserved player card shows an orange mic button that deep-links to `/capture?sessionId=&player=` (one tap to observe, zero navigation); already-observed players show a green ✓ "Observed" badge and are dimmed 60% so coaching blind spots stand out at a glance; the default alpha sort is overridden during practice to float unobserved players to the top within each alpha group; session observation IDs fetched from the same `session-obs-count` query key as the home dashboard coverage strip (30-second refetch, no extra round-trip when warm); `PlayerCard` gains `practiceSessionId?` and `observedInSession?` optional props — no changes to existing non-practice usage; zero new API routes, DB tables, or migrations; closes the mid-practice gap where coaches navigating to their roster had no way to quickly observe a player
- [x] **Session coverage strip on capture page** — the generic "Observations will be linked to your session" banner was replaced with a live coverage tracker: when `?sessionId=` is present, the capture page fetches the session's existing observations + team roster in parallel and shows "X/Y covered" badge; unobserved player first-name chips appear below so coaches immediately know who still needs attention; tapping a chip pre-selects that player for QuickTemplates (orange ring); tapping again de-selects; after each template save the coverage refreshes so newly-observed players disappear from the strip; "All players observed ✓" emerald state when every player is covered; silently hidden when sessionId is absent or roster is empty so non-session captures are unaffected; `QuickTemplates` gains optional `onSaved?: () => void` prop to notify parent when a save completes; zero new API routes, DB tables, or migrations; closes the "I don't know who I've captured yet" blind-spot coaches hit when using the lightweight home-page capture flow mid-practice

### P32 — Drill Discovery
- [x] **Capture page URL param wiring** — session/player context from `?sessionId` / `?playerId` / `?player` URL params was silently ignored; fixed: capture page reads params client-side on mount, passes `sessionId` to QuickTemplates (was always `null`) and to every sessionStorage write so voice/text/upload observations are linked to the correct session; review page reads `session_id` from sessionStorage and includes it in both online and offline save paths; QuickTemplates accepts `preselectPlayerId` and floats that player to the top of the picker with an orange highlight + "suggested" badge; blue context banner shown on capture page when sessionId is present; closes the session coverage tracker "X/Y observed" loop so observations actually count toward it
- [x] **Observation-to-Drill Shortcut** — needs-work observation cards in the Observation Feed now show a "Find drill" link that deep-links to /drills pre-filtered to the same skill category (e.g. a needs-work dribbling obs → /drills?category=Dribbling); the Drills page reads the ?category= URL param on mount and initialises the category filter state from it; closes the observe→diagnose→correct coaching loop in one tap; zero new tables or API routes

### P40 — Practice Timer Drill Intelligence
- [x] **Drill-grouped Done screen + drill_id on observations** — Practice Timer observations now carry full drill context end-to-end: `CapturedNote` gains `drillId?` (populated from `QueueItem.drillId`); `handleSaveObservations` writes `drill_id` to the observations table (field already existed but was never populated); Done screen observations are now grouped by drill instead of a flat list — each drill gets a collapsible header showing 👍 N / 👎 N sentiment counts so coaches can immediately see which drills surfaced the most skill gaps (e.g. "Figure-8 Dribble: 1 positive, 3 needs-work") without requiring AI generation or navigation; closes the observe→drill feedback loop at the most natural post-practice moment
- [x] **Drill Usage History on drill detail page** — every drill detail page now shows a "Your Team's Experience" card: times run this season (distinct session count), last-used time chip (emerald=recent, amber=2–3 weeks, red=stale/never), 👍 positive count, 👎 needs-work count, and the 5 most recent observations from that drill with player name, sentiment, and full text; empty state encourages first use with a "Start a Practice" CTA; queries `observations` table filtered by `team_id + drill_id` (populated since P40) — no new DB tables or API routes; `drills.usage(drillId, teamId)` query key added; `src/lib/drill-usage-utils.ts` pure utility module (countDistinctSessions, getLastUsedAt, formatLastUsed, countBySentiment, getPositiveRatio, hasUsageData, getRecentObservations, buildUsageSummaryLabel, getLastUsedColor, getSentimentClasses, resolvePlayerName, buildDrillUsageSummary, isDrillEffective, isDrillStruggle); 54 unit tests; total suite: 2345 tests
- [x] **Swap Drill mid-practice** — when the current drill isn't engaging kids, a "Swap" button appears on the countdown screen (only when same-category alternatives exist in the drill library); tapping opens a bottom sheet showing up to 4 replacement drills from the same skill category, each with name, duration, description, and first coaching cue; tapping a replacement swaps it into the queue at the current position and restarts the countdown immediately — no drill is wasted, no fumbling with the setup screen; `swapAlternatives` memoized from `drills` filtered by category, excluding drills already in the queue; `handleSwapDrill` replaces the queue item and calls `startTimerForDrill` to restart; closes the "this drill isn't working with my kids today" gap that previously had no solution
- [x] **Category-aware template ordering on break screen** — quick-template chips on the Practice Timer break screen now surface the drill's skill-category template in position 1 (orange ring + "Dribbling drill — top match first" label) so coaches never have to scan 5 chips to find the relevant one; if the drill category has no matching template, or the drill has no category, falls back to insertion order; `drillCategory?` prop added to `BreakScreen`; pure client-side sort — no new API routes, DB tables, or AI calls; closes the "which template is relevant to my current drill?" micro-friction gap at the most time-critical coaching moment
- [x] **Audio drill announcements** — Practice Timer announces each drill name, duration, and first coaching cue out loud via Web Speech API when a drill starts; prompts "Quick break, capture an observation now" at every break; congratulates the coach with their total obs count when practice ends; coaches keep their eyes on the kids instead of glancing at the phone; amber Volume2 / VolumeX toggle button in the countdown top bar; preference persisted to localStorage (default: on); speechSynthesis cancelled on unmount; announcement effect uses stable refs so note saves and queue reorders never trigger spurious speech; `src/hooks/use-announcer.ts` (enabled no-op hook wrapping Web Speech API); `src/lib/announcer-utils.ts` pure utility module (buildDrillAnnouncement, buildBreakAnnouncement, buildPracticeCompleteAnnouncement, buildNextDrillHint); 19 unit tests; total suite: 2504 tests

### P46 — Multi-Session Practice Planning
- [x] **Practice Arc Generator** — coaches input 2–3 sessions, a duration per practice, an optional upcoming event (e.g. "tournament Saturday"), and an optional focus area; AI generates a coherent practice progression where each session builds on the last: Session 1 introduces fundamentals, Session 2 develops the skill in team context, Session 3 applies it at game speed; each session has warmup, 2+ drills (each with coaching cues and a `progression_note` linking to prior session work), cooldown, and a `key_coaching_point`; sessions are connected by a `carries_forward` note; the arc includes a `progression_note` explaining how the sessions fit together and an optional `game_day_tip` when a competitive event was specified; sessions rendered as colour-coded cards (sky → violet → emerald) with progression arrows; sky-themed collapsible form in Plans page with session count toggle (2/3), duration picker (30/45/60/75/90 min), optional event field, and optional focus override; `POST /api/ai/practice-arc`; `practice_arc` plan type added to `PlanType` union; `practiceArcSchema` Zod validation; `practiceArc` prompt in `PROMPT_REGISTRY`; `src/lib/practice-arc-utils.ts` pure utility module (isValidSessionCount, isValidSessionDuration, hasEnoughDataForArc, isValidArcTitle, isValidArcSession, hasGameDayTip, countTotalDrills, getTotalArcMinutes, getSessionDrillCount, getSessionTotalDrillMinutes, getSessionLabel, getProgressionLabel, formatArcDuration, formatSessionDuration, getSessionAccentColor, getSessionBorderColor, getSessionBgColor, extractPrimaryFocusFromNeedsWork, buildArcTitle, buildArcShareText, isLastSession, countSessionsWithCarriesForward, getPrimaryFocusLabel); 79 unit tests; total suite: 2583 tests

### P30 — Roster UX
- [x] **Last Observed time chips on player cards** — every roster card now shows a clock icon + relative time label indicating when that player was last observed (Seen today → emerald, 1d ago → zinc, 2–6d → amber, 7–13d → orange, 14d+ → red, Never observed → dim zinc); visible on mobile where the obs count is hidden; the obsCounts query now also captures max(created_at) per player in a single round-trip; gives coaches an instant "attention queue" scan before practice without leaving the roster page
- [x] **Last Observation Preview on player cards** — below the "Seen X ago" chip, each roster card now shows a truncated (55-char) italic snippet of the most recent observation text in sentiment-coded colour (emerald=positive, amber=needs-work, zinc=neutral); the existing observation query is extended with `text` and `sentiment` fields (no extra round-trip); coaches can instantly recall each player's coaching story when scanning the roster before practice — e.g. "Great defensive positioning on left wing" — without tapping into the player detail page
- [x] **Roster sort: Needs Attention + Momentum** — sort dropdown added alongside the position filter; three modes: A–Z (default), Needs Attention (never-observed first, then oldest last-observation date — surfaces coaching blind spots instantly), By Momentum (lowest momentum score first — shows struggling players at the top); all sorting is client-side from already-fetched data; zero new API routes, queries, or migrations; closes the pre-practice "who do I focus on today?" question in one tap

### P20 — Practice Workflow Quality
- [x] **Post-Practice Debrief — skill categories + completion screen** — two improvements to the debrief modal: (1) templates now carry proper skill categories (hustle, passing, defense, awareness, leadership, shooting, rebounding, dribbling, teamwork, footwork) instead of generic 'General', feeding accurate data into drill recommendations, skill trend analysis, momentum scores, and plan AI; two new templates per sentiment side added (Great shooting / Strong rebounding; Footwork / Free throws); (2) a new 'done' completion step replaces the abrupt close — coach sees celebration summary (X observations for Y players), then two next-action cards: "View Session" (links to session detail with AI debrief + replay) and "Plan Next Practice" (links to plans page for AI-generated plan from today's data); closes the practice loop instead of dropping coaches on the home page
- [x] **Session Coverage Tracker** — compact coverage card in the Observations section of every session detail page; shows observed/unobserved player counts (X/Y badge); unobserved players appear as tappable orange chips that deep-link to /capture with that player pre-selected (one tap to fix a coaching blind spot); all-covered state shows emerald "Every player observed" confirmation; observed players toggleable on demand; no new API routes or tables (uses already-fetched rosterPlayers + session observations)
- [x] **Coverage Tracker next-step CTAs** — when every player is observed AND ≥3 observations exist, two action buttons appear below the "Every player observed" confirmation: orange "AI Debrief" (scrolls to `#ai-debrief-section`) and teal "Send Parent Updates" (scrolls to `#player-messages-section`); closes the "observed everyone — what now?" dead-end at the most natural post-practice moment; zero new API routes, DB tables, or AI calls
- [x] **Coverage-aware PostPracticeDebrief standouts step** — when a coach taps "End Practice," the debrief modal now fetches the session's existing observations on mount and surfaces a coverage badge ("8 of 12 players observed") at the top of the standouts step; player chips are colour-coded: amber = not yet observed this session (coaching blind spot), zinc with faint checkmark = already observed, emerald = selected as standout; a "X missed" count appears on the badge when players were skipped; a legend line "Amber = not yet observed" orients coaches instantly; closes the coaching blind-spot loop at exactly the right moment — when the coach is wrapping up — without requiring navigation to the session detail page; zero new API routes, DB tables, or migrations (reads from observations table already stored)

### P23 — Home Dashboard Session Awareness
- [x] **Today's Session Card + Upcoming This Week** — home dashboard now surfaces schedule context. When a coach has a session scheduled for today (via calendar or recurring scheduler), the green "Start Practice" button is replaced by a TodaySessionCard: session type, time, location, opponent badge, and amber player-availability strip (injured/sick/limited players named). Three quick-action buttons: Open Session, Timer (practice only), Capture. Below the stats section, an UpcomingSessionsCard lists up to 5 sessions in the next 7 days with colour-coded type dots, relative day labels (Tomorrow vs weekday+date), and a "Schedule a session" shortcut. Both cards prevent duplicate session creation by showing the already-scheduled session instead of creating a new one. No new API routes — reuses existing /api/player-availability + generic /api/data endpoint. PulseStats interface gained a `players` array (reusing already-fetched player data) to cross-reference availability status with names.
- [x] **Live observation count + quick-action shortcuts during active practice** — when a coach starts a practice session from the home page, the "End Practice" button now shows a live observation count ("12 obs · 8 players covered") refreshed every 30s from the DB, plus a count badge on the button face, giving coaches instant reassurance that their quick-captures are landing. Two quick-action shortcuts appear below the button: "Capture" (links to /capture) and "Open Timer" (links directly to the active session's timer page) — closing the gap between the lightweight Start Practice flow and the structured Practice Timer without requiring navigation through the sessions list. Zero new API routes or tables.
- [x] **Live coverage strip on home page during active practice** — the home page now shows a compact "Not yet observed" row below the practice quick-actions; amber first-name chips for each unobserved player are one-tap deep-links to /capture pre-selecting that player (3-second fix for a coaching blind spot without navigating away from home); turns emerald "✓ All N players observed" when the roster is fully covered; `sessionObsStats` extended to return `observedIds: Set<string>`; roster query enabled during active practice (previously only enabled when a scheduled today-session existed); `unobservedPlayers` computed via useMemo after sessionObsStats resolves; zero new API routes, DB tables, or AI calls
- [x] **Live Skill Pulse on home page during active practice** — when ≥2 observations in the same skill category are flagged needs-work in the current session, a red "Live Skill Pulse" strip appears below the coverage strip showing the count and category label (e.g. "3× needs-work: Ball Handling — find a drill") with a one-tap link to the drill library filtered to that category; closes the observe→act loop for coaches using the lightweight home-page Start Practice flow (not the structured timer) who previously had no real-time feedback on emerging skill gaps; `sessionObsStats` query extended with `category` and `sentiment` fields, gap computation performed client-side via `gapMap` + threshold filter (≥2 required); `sessionSkillGap` useMemo resolves the drill library URL and display label from `SKILL_TO_DRILL_CATEGORY` / `SKILL_DISPLAY_LABEL` maps; zero new API routes, DB tables, or AI calls

### P24 — Practice Timer UX
- [x] **Practice Template Library** — 6 pre-built, sport/age-specific drill queues load into the practice timer with one tap, eliminating the blank-queue friction for volunteer coaches. Templates: Basketball U6-8 (30 min, 4 drills), U9-12 (45 min, 5 drills), U13-18 (60 min, 5 drills); Soccer U6-8 (30 min, 4 drills), U9-12 (45 min, 4 drills); Generic First Practice (30 min, 4 drills — any sport). Every drill ships with 3-5 rotating coaching cues. Template picker appears only when queue is empty; sport/age ranking surfaces best-fit templates first using team.sport_id + team.age_group. Green "Loaded template" banner (distinct from the blue plan banner). `src/lib/practice-templates.ts` — 14 pure utility functions (getTemplatesForSport, getTemplateById, getTotalMinutes, getDrillCount, matchesAgeGroup, rankTemplates, hasSufficientCues, buildTemplateLabel, buildTemplateSummary, filterByTag, getAllTags, templateFitsSession, scaleTemplateDuration); 57 unit tests; total suite: 1606 tests
- [x] **Drill queue reordering** — replaced the decorative GripVertical icon in the Practice Timer setup screen with functional ChevronUp/ChevronDown buttons; coaches tap ↑/↓ to reorder drills in the queue without deleting and re-adding them; first item hides Up, last item hides Down; disabled state uses opacity-20; no new dependencies or API routes
- [x] **Repeat Last Practice** — on successful observation save the completed drill queue is persisted to `last-practice-queue-{teamId}` in localStorage; on next timer setup an amber "Repeat Last Practice" button appears above the Template Picker showing drill count + total duration; one tap reloads the exact same queue (fresh IDs to avoid key collisions); team-scoped so multi-team coaches stay isolated; zero new API routes, DB tables, or migrations

### P25 — Quick Capture UX
- [x] **Templates tab in Quick Capture widget** — the floating Zap button now offers two tabs: Voice (existing AI flow) and Templates (new no-AI path). Templates tab mirrors the practice timer mini-dropdown UX: sentiment toggle (Positive/Needs Work) → 10 template chips per side from `OBSERVATION_TEMPLATES` → player picker grid (roster lazy-loaded on first open) → "Saved!" auto-reset confirmation; no AI roundtrip, no voice, works in noisy gyms or with spotty WiFi; `source: 'template'` on saved observations; haptic feedback on save; query cache invalidated for home-stats, home-pulse, and observations; all existing voice behavior unchanged

### P26 — Parent Feedback Loop
- [x] **Parent Reactions** — parents viewing a player's share portal can tap an emoji reaction (❤️ 👏 🌟 🙌 🔥) and optionally add a message + their name to send direct appreciation to the coach; IP-rate-limited POST `/api/parent-reactions` (30/day) validates the share token before storing; coach sees a pink-accented "Parent Messages" card on the home dashboard showing up to 3 recent reactions with player attribution, unread badge count, and a one-tap "Mark read" button; GET/PATCH `/api/parent-reactions` for coach auth; `parent_reactions` table (migration 023); `parent-reaction-utils.ts` pure utility module (isValidReaction, isValidMessage, isValidParentName, getReactionLabel, formatReactionTime, buildDisplayName, countReactionsByType, countUnread, hasUnread, getRecentReactions, sortNewest, hasReactions, getTotalReactionCount, getReactionsWithMessages, getMostUsedReaction, buildSummaryLine, groupReactionsByPlayer); 43 unit tests; total suite: 1706 tests

### P27 — Daily Coaching Action
- [x] **Today's Focus Card** — home dashboard shows ONE specific, actionable coaching task: which player to give feedback to today and why. Algorithmically picks the active player unobserved the longest who hasn't been seen today, cross-referenced with the team's top declining skill trend to suggest a skill to focus on. Displayed as a slim indigo card between the stats grid and coaching streak; dismissible for the current calendar day via localStorage; one-tap "Capture Observation" button pre-selects the player in /capture. Uses already-fetched pulse observation data — no extra API route, no AI calls, instant display. `src/lib/daily-focus-utils.ts` pure utility module (getDayKey, daysBetween, getLastObservedAt, getDaysSinceObserved, hasBeenObserved, isObservedToday, getMostNeglectedPlayer, getTopDecliningSkillLabel, getPlayerTopNeedsWorkSkill, getBestSkillFocus, buildCaptureHref, formatDaysSince, capitaliseCategory, buildFocusReason, hasSufficientDataForFocus, buildDailyFocusSuggestion); 57 unit tests; total suite: 1763 tests

### P29 — First-Practice Activation
- [x] **First Practice Launcher** — new coaches who land on the home dashboard with 0 sessions see a "Ready for your first practice?" card showing 2–3 ready-made drill templates ranked by the team's sport and age group; one tap creates a practice session and opens the Practice Timer with the queue pre-loaded (no form-filling or blank setup screen); dismissible via localStorage; timer page now accepts `?templateId=` URL param to auto-load any practice template on arrival (mirrors existing `?planId=` support); WelcomeTour final step replaced from confusing "add AI provider key" instruction to encouraging "Start your first practice" close that reinforces the main CTA; `src/components/home/first-practice-launcher.tsx`; **re-wired onto home page** (was removed in UX simplification); shown below GettingStartedCard when `stats.sessions === 0 && !practiceActive` — complements the Getting Started checklist by giving coaches instant structured-practice options rather than a blank session
- [x] **Bulk paste roster import in onboarding** — onboarding roster step now offers two tabs: "Add One by One" (existing) and "Paste Roster" (new); coaches paste a full team list (one name per line, copied from an email, Google Doc, or league website) into a textarea; live counter shows detected names; "Add N Players" button confirms instantly with no extra steps; uses existing `/api/auth/add-players` endpoint — no new routes or tables; eliminates the biggest friction point for new coaches adding 10-15 players during first-use activation

- [x] **Auto-load existing share link + Web Share API on player Share tab** — coaches visiting the Share tab previously found it blank every time (state was never persisted), forcing them to recreate a link even when one already existed; the only sharing option was manually copying the URL; on mobile this required 5+ steps to reach a parent via WhatsApp; now: (1) Share tab queries `parent_shares` on mount (enabled only when tab is active), auto-populates the link from the most recent active token — the link is never "lost" between visits; (2) primary CTA is a full-width "Send to Parent" button that triggers the native Web Share API (WhatsApp/SMS/email picker) on mobile and opens WhatsApp Web pre-filled on desktop, with 2s "Sent!" confirmation; (3) view count + last-opened date shown when the token has been accessed so coaches know parents are reading the report; (4) `handleCreateShareLink` invalidates the query so fresh tokens appear immediately; zero new API routes, DB tables, or migrations — pure client-side UX

### P28 — Parent Portal "Wow" Moment
- [x] **Active player goals on parent share portal** — parents now see a violet-accented "Coach's Goals for [Name]" section on the player share portal revealing up to 3 active development goals the coach has set (skill area, goal text, optional target proficiency level, optional target date); private coach notes are never exposed; section is hidden when no active goals exist so new coaches aren't affected; positioned between "Skills on the Rise" and "Practice at Home" to create the narrative arc: here's how they're improving → here's what we're working toward → here's what you can do at home; share API now queries `player_goals` filtered to `status = 'active'`; no new tables, migrations, or API routes; closes the goal-setting feature's visibility gap — coaches' intentional work is now visible to the people most motivated by it (parents) and drives coach adoption of the player goals feature
- [x] **Skill Radar Chart on parent share portal** — pure-SVG spider chart added between "Season at a Glance" and "Skills on the Rise"; renders up to 8 proficiency-tracked skills as an orange-filled polygon; server-component safe (no client JS); shows only when ≥3 skills have real proficiency data; proficiency levels map to 0.25/0.5/0.75/1.0 radial values; uses success_rate when available; light-mode orange palette; proficiency legend below; creates an instantly shareable visual that drives parents to screenshot and share
- [x] **Practice at Home on parent share portal** — the most recent AI-generated Skill Challenge Card for the player is automatically surfaced on the parent portal as a "Practice at Home" section (blue accent) between "Skills on the Rise" and "Achievement Badges"; shows up to 2 challenges with title, minutes-per-day badge, description, numbered steps, success criteria (emerald goal chip), and encouragement text; week label + parent note shown at the top; share API fetches the latest `skill_challenge` plan keyed by `player_id` in one extra DB call; zero new tables, routes, or migrations — pure reuse of existing infrastructure; closes the coach-generates-challenge → parent-sees-homework loop and drives coach usage of the Skill Challenge feature
- [x] **Season Stats & Skills on the Rise on parent share portal** — parents now see a "Season at a Glance" stats strip (total coach observations, improving-skill count, this-fortnight activity count) and a "Skills on the Rise" section with emerald chips for every skill trending upward — creating an emotional "wow" moment that drives screenshot sharing. Computed entirely from existing observation + proficiency data, zero new DB tables or migrations. Share API updated to return `totalObservationCount` (exact Supabase count) and `recentObservationActivity` (last 90 days, lightweight category+date payload for trend computation). Skill Progress bars re-ordered improving-first so parents immediately see momentum. `src/lib/skill-journey-utils.ts` pure utility module (getImprovingSkills, getDecliningSkills, getPlateauSkills, getMostImprovedSkill, countImprovingSkills, countDecliningSkills, hasAnyImprovingSkill, formatProficiencyLabel, groupObsByCategory, getObsAfterDate, getObsBeforeDate, getObsBetweenDates, getMostActiveCategory, buildSkillActivityData, getMostSurgingCategory, buildSeasonStats, formatCategoryLabel, buildProgressMessage, getTrendIcon, getTrendColor, sortSkillsByImprovingFirst, filterSkillsWithTrend, hasEnoughDataForJourney); 52 unit tests; total suite: 1815 tests
- [x] **Achievement Badges on parent share portal** — parents now see a warm amber "Achievements Earned" section on the player share portal showing every badge the player has unlocked (First Star ⭐, Team Player 🤝, Grinder 💪, All-Rounder 🎯, Breakthrough 🚀, Game Changer ⚡, Session Regular 📅, Coach's Pick 🏆, Most Improved 📈, Rising Star 🌟); each badge card shows emoji + name + coach note (or default description) in a 2-column grid with distinct per-badge colours; section positioned between "Skills on the Rise" and "Skill Progress" for maximum emotional impact; no new tables or API routes — share route now always fetches `player_achievements` (badge_type, awarded_at, note) in the same request with no extra round-trip; turns the parent portal into a screenshot-worthy moment parents share with each other
- [x] **AI Coach Session Message on parent share portal** — the most recent AI-generated "Player Session Message" (from the coach's Plans page) automatically appears on the parent portal as "Coach's Update" — requiring zero extra work from coaches. Share API queries the last 5 `player_messages` plans for the team and extracts the message matching this player by name (full-name then first-name fallback). Returns `latestSessionMessage { message, highlight, next_focus, session_label }`. New emerald-accented portal card positioned directly after the greeting: warm personalized message, ✨ Highlight chip (emerald), 🎯 Next Focus chip (orange), coach name attribution, and session label. Creates the parent "wow" viral moment: parents see exactly what the coach said about their kid after the most recent session — the most personal and impactful content in the entire portal
- [x] **"Fresh from practice" banner on parent share portal** — when a parent opens the share link within 24 hours of observations being captured, a branded orange-gradient banner appears between the player card and Coach's Update showing "🔥 Fresh from practice!", coach name, observation count ("8 new observations today"), a smart time label ("Just now" / "45m ago" / "2h ago" / "Earlier today"), and the most recent positive observation highlight when available; hidden when fewer than 2 recent observations exist (avoids showing for lone test captures); computed entirely from already-fetched `recentObservationActivity` and `highlights` arrays — zero new API routes, DB queries, or AI calls; creates an urgency moment that encourages coaches to share the link right after practice and gives parents an immediate sense of their coach's active engagement; brandColor gradient matches the org's custom branding

### P41 — Pre-Session Parent Communication
- [x] **Pre-Practice Parent Reminder card** — sky-blue `PracticeReminderCard` on the session detail page; shown only for upcoming/today sessions (auto-hides for past sessions); auto-fills a WhatsApp-ready message with team name, session type, "Today"/"Tomorrow"/weekday date label, time, and 📍 location; game/scrimmage/tournament sessions get a 🏆 headline + opponent name while practice/training sessions get a 📣 headline; editable textarea so coaches can personalise; Copy to clipboard + WhatsApp Web one-tap send buttons; zero new API routes, DB tables, or AI calls — pure client-side; dismissible with ✕; closes the "coaches get asked When is practice? every week" gap in under 5 seconds

### P43 — One-Tap Parent Communication
- [x] **"Email Parents" button on Player Session Messages** — after a coach generates Player Session Messages, a new blue "Email N" button appears in the card header whenever any roster player has a `parent_email` on file; one tap sends each parent a personalized, mobile-friendly HTML email (greeting, session label, player message, emerald Highlight chip, orange Next Focus chip, coach sign-off, SportsIQ footer) via Resend without the coach copy-pasting 12 messages; button shows spinner while sending, then "N sent" confirmation; button hidden until messages are generated + at least 1 parent email exists; a subtle "Add parent emails in Roster to send these updates in one tap" hint shown when 0 players have email, driving roster completeness; `POST /api/send-parent-messages` (auth-required) fetches roster, matches AI player names to roster entries via multi-tier name matching (exact → nickname → variant → first-name → substring), deduplicates by email address, and calls `sendEmail()`; `src/lib/parent-email-utils.ts` pure utility module (filterPlayersWithEmail, countPlayersWithEmail, hasAnyParentEmail, matchMessageToPlayer, buildEmailPayloads, countMatchedEmails, buildParentEmailSubject, buildParentEmailHtml, escapeHtml); 45 unit tests; total suite: 2390 tests
- [x] **Direct WhatsApp send for Player Session Messages + complete Add Player form** — when a parent phone is on file, the per-player "Send" button in the Player Session Messages card now opens WhatsApp directly pre-addressed to that parent (`wa.me/{phone}?text=...`) instead of the generic WhatsApp Web picker; saves ~5 s × 12 players of copy-paste friction per practice; `directPhoneMap` memoized from `matchMessageToPlayer` so matching runs once not on every render; each player card shows an emerald "Direct" badge when a phone match is found; hint text drives coaches to add missing phone numbers; hint updated to mention both phone and email options; **Add Player form was missing `parent_phone` and `name_variants` fields** (Edit Player had them) — both added with helper text ("Add phonetic spellings so voice capture recognizes this player by name" / "Used to send player updates directly via WhatsApp after practice"); `parent-email-utils.ts` extended with `parent_phone?` on `ParentEmailPlayer`, `filterPlayersWithPhone`, `countPlayersWithPhone`, `hasAnyParentPhone`

### P47 — Automatic Parent Communication
- [x] **Automatic Weekly Parent Progress Digest** — coaches enable one toggle in Settings → Profile → "Auto parent progress emails" and every Sunday at 18:00 UTC each parent with an email on file automatically receives a personalized progress-report email with their child's live share portal link; cron gets or auto-creates a permanent share token per player; email includes activity summary (session count + observation count for the week), highlight quote (most recent positive observation), and a branded CTA; dedup via `preferences.parent_digest_week_YYYY-MM-DD`; CRON_SECRET auth; 3-obs minimum threshold; help text in settings drives roster completeness; closes the "coach must manually share after every session" gap that prevented consistent parent communication; `src/lib/parent-digest-utils.ts` pure utility module (getWeekStartSunday, isParentDigestEnabled, hasAlreadySentParentDigest, markParentDigestSent, enableParentDigest, disableParentDigest, hasEnoughDataForParentDigest, getRecentObsHighlight, buildShareUrl, buildParentDigestSubject, buildParentDigestHtml); `POST /api/cron/parent-digest`; Vercel cron `0 18 * * 0`; 52 unit tests; total suite: 2704 tests

### P44 — Coach Retention & Habit Formation
- [x] **Weekly Coach Digest Email** — every Monday at 08:00 UTC, coaches with ≥2 observations + ≥2 active players in the prior week receive a personalised week-in-review email: stat grid (observations / sessions / players observed), ⭐ Player Spotlight (player with most positive obs), 💪 Top Strength this week (skill category with most positive momentum), 👀 Players Needing Attention (0 obs in last 7 days, max 5), and one-tap "Build Next Week's Plan →" CTA linking to /plans; positive-rate chip ("70% positive observations") + needs-work count; multi-team coaches receive one email for their most active team; deduplication via `preferences.digest_week_YYYY-MM-DD`; opt-out via `preferences.disable_weekly_digest = true`; Vercel cron added at `0 8 * * 1`; `src/lib/weekly-digest-utils.ts` pure utility module (getPriorWeekMonday, getWeekWindow, formatWeekLabel, filterObsInWindow, countPositiveObs, countNeedsWorkObs, getObservedPlayerIds, getTopPerformer, getNeglectedPlayerNames, getTopCategory, formatCategoryLabel, getDigestKey, hasAlreadySentDigest, isDigestDisabled, markDigestSent, hasEnoughDataForDigest, buildDigestSubject, getCoachGreeting, buildDigestHtml); `POST /api/cron/weekly-digest`; 69 unit tests; total suite: 2652 tests
- [x] **Pre-Practice Reminder Email** — coaches receive a personalised email at 12:00 UTC on days they have a session scheduled; email shows the session type + start time, an amber "haven't been observed this week" list of neglected players (up to 5), a last-session summary card (observation count, players covered, strongest/weakest skill categories), and three deep-link CTA buttons (Start Timer, Capture Obs, View Roster); graceful all-covered emerald message when no players are neglected; each coach receives at most one reminder per day (preferences key dedup), and can opt out via `preferences.disable_practice_reminders = true`; reminder silently skipped when team has fewer than 2 players or fewer than 3 observations (no data to show); Vercel cron added at `0 12 * * *`; `src/lib/practice-reminder-utils.ts` pure utility module (getTodayKey, getDaysAgo, getReminderKey, hasAlreadySentReminder, isReminderDisabled, markReminderSent, buildSessionTypeLabel, buildSessionTimeLabel, getSessionEmoji, getLastObservationDate, getPlayersNotRecentlyObserved, hasEnoughDataForReminder, countObsByCategory, getTopCategories, buildLastSessionSummary, buildPracticeReminderHtml, buildPracticeReminderSubject); `POST /api/cron/practice-reminder`; 61 unit tests; total suite: 2451 tests

### P45 — Weekly Coaching Intentionality
- [x] **Weekly Team Focus card** — coaches one-tap set a skill theme for the week (e.g. "Defense week before Saturday's game") from a 10-category picker (Shooting, Defense, Ball Handling, Passing, Hustle, Court Vision, Teamwork, Footwork, Conditioning, Leadership); stored in localStorage, auto-expires after 7 days; indigo-accented home dashboard card shows category emoji + label + age + days remaining when set, or inline category picker when not set; "Change" button re-opens the picker; DrillOfDayCard shows an indigo "Matches your focus" badge when today's recommended drill category aligns with the weekly focus, closing the focus→drill feedback loop visually; shown on home after ≥1 session; SSR-safe via mounted guard; zero new API routes, DB tables, or AI calls; `src/lib/weekly-focus-utils.ts` pure utility module (FOCUS_CATEGORIES, getWeeklyFocusKey, isValidFocusCategory, getFocusCategoryConfig, isWeeklyFocusExpired, getDaysRemaining, getWeeklyFocus, setWeeklyFocus, clearWeeklyFocus, categoryMatchesFocus, formatFocusAge); 34 unit tests; total suite: 2485 tests
- [x] **"Build [Focus] Practice Plan" one-tap from Weekly Focus card** — when a coach has set a weekly focus (e.g. "Defense Week"), the card now shows a "Build Defense Practice Plan" button that POSTs to `/api/ai/plan` with `focusSkills: [focusLabel]` and navigates to `/plans` on success; generating/error states handled inline; closes the declare-intent → get-actionable-content loop that previously required the coach to navigate to Plans, remember what they wanted, type a prompt, and wait — now it's one tap from the home dashboard; no new API routes, DB tables, or utility modules

### P42 — Demo & Conversion
- [x] **Sample Parent Report Card (`/demo/report`)** — static page with fully realized mock parent portal for "Marcus Johnson / YMCA Rockets U12 / Spring 2025"; shows every parent-facing section: Coach's Update (with ✨ highlight + 🎯 next-focus chips), Season at a Glance stats strip (47 obs / 3 improving / 12 fortnight), Skills at a Glance pure-SVG spider chart (5-axis: Dribbling/Defense/Teamwork/Offense/Shooting), Skills on the Rise emerald chips, Practice at Home home-challenge card with numbered steps + goal, Achievements Earned 4-badge grid, Skill Progress bars with proficiency legend, What to Celebrate checklist, Recent Observations feed, and Coach's Note; no database or API calls — fully static, prerendered at build time; orange demo banner at top with "Create a free account →" CTA; sign-up call-to-action card at bottom; linked from two locations on the landing page ("See it in action" section + demo preview section) and from the demo voice-capture results page as "View sample parent report" emerald card; closes the biggest demo conversion gap — prospects had no way to see the parent portal (the most viral feature) without first being a coach with players and share links
- [x] **Real Claude AI in demo capture (`/api/ai/demo-segment`)** — the demo voice/text capture page previously always showed the same 5 hardcoded mock observations regardless of what a coach typed or said, because `/api/ai/segment` requires authentication; new public `/api/ai/demo-segment` route (claude-haiku-4-5, IP-rate-limited to 5 calls per 15 min) processes the coach's exact transcript against the demo roster and returns real AI-parsed observations; demo page updated to call the new endpoint first and fall back to sample data only when AI is unavailable; results screen shows a "Live Claude AI" violet badge and labels the transcript "Your words — processed by Claude AI" when AI succeeded; processing screen gains four animated step labels so coaches understand what is happening; middleware updated to add the route to public paths; closes the "demo always shows fake data" gap that undermined trust in the AI's actual capability
- [x] **Animated phone mockup preview section on landing page** — landing page had zero visuals showing the actual app UI, a major conversion gap for a UI-driven product; new dark-theme "App Preview" section (between "How It Works" and "Built for Coaches Like You") shows 3 phone frame mockups: (1) Voice Capture — pulsing animated mic with RECORDING strip and live transcript; (2) AI Observations — 3 player cards with positive/needs-work badges, category chips, and Save CTA; (3) Parent Portal — light-mode report with avatar, Coach's Update card, skill progress bars (Defense/Teamwork/Dribbling), and "Defense on the rise" emerald chip; horizontal snap-scroll on mobile, 3-column grid on desktop; pure static JSX/Tailwind — zero new API routes, DB tables, AI calls, or dependencies; closes the "prospects can't see the product before signing up" gap
- [x] **Testimonials section on landing page** — landing page lacked social proof beyond a single anonymous quote inside the "Built for coaches" section; added a 3-card testimonials grid (sm:grid-cols-3) between "See it in action" and "Pricing" — the prime conversion moment before the ask; three specific volunteer-coach personas (YMCA basketball, youth soccer, flag football) with authentic quotes targeting the three main objections (will parents care? / I'm not tech-savvy / does it save time?), 5-star ratings, colour-coded avatar initials, name, and role+location; `Star` icon added to lucide-react imports; zero new API routes, components, or dependencies
- [x] **SeasonalPromo wired to home dashboard** — `SeasonalPromo` component (P5, `src/components/onboarding/seasonal-promo.tsx`) was built and marked done but was never imported or rendered on the home dashboard; now shown between the First Practice Launcher and the stats grid; self-hides outside the first 21 days of September/January/April and is per-season dismissible via localStorage; contextual CTA changes to "Schedule Session" when team already has 5+ players
- [x] **Curriculum → practice plan one-tap** — the curriculum page highlighted skills with `intro_week === current_week` with a "This Week" badge and orange ring, but had no way to act on them; a new orange CTA card appears below the week stepper showing the week's new skill names and a "Build Week N Practice Plan" button; one tap calls `/api/ai/plan` with those skills as `focusSkills`, then navigates to `/plans`; self-hides when no skills have an `intro_week` matching the current week; fires `curriculum_week_plan_generated` analytics event; closes the curriculum → practice planning loop that previously required coaches to manually remember, navigate, and type
- [x] **Plans list search + type filter chips** — coaches who have accumulated practice plans, game recaps, newsletters, player messages, and other plan types previously had no way to find a specific plan without scrolling; added a search input (filters by plan title or type label) and auto-generated type filter chips (built from plan types actually in the list, sorted by count, capped at 6, shown only when >1 type exists) above the plans list; "X of N" count badge shows filtered vs total; empty-filter state with one-tap "Clear filters" link; search/filter controls visible only when ≥3 plans exist; all filtering is client-side via `useMemo` — zero new API routes, DB tables, or AI calls

---

## Architecture Rules

### Data Access
```typescript
// CLIENT SIDE — always use the API helpers
import { query, mutate } from '@/lib/api';

// SERVER SIDE (API routes) — always use service role
import { createServiceSupabase } from '@/lib/supabase/server';
const admin = await createServiceSupabase();
```

Never use `createClient()` from `@/lib/supabase/client` for database queries. It gets blocked by RLS. The only exception is `supabase.auth.signOut()`.

### Adding New Tables
1. Add to `supabase/migrations/` as a new numbered migration
2. Add types to `src/types/database.ts`
3. Add to the whitelist in `/api/data/route.ts` and `/api/data/mutate/route.ts`

### Adding Feature-Gated Content
```tsx
import { UpgradeGate } from '@/components/ui/upgrade-gate';

export default function MyPage() {
  return (
    <UpgradeGate feature="my_feature" featureLabel="My Feature">
      <ActualContent />
    </UpgradeGate>
  );
}
```
Add the feature key to `TIER_LIMITS` in `src/lib/tier.ts`.

### AI Prompts
All AI prompts live in `src/lib/ai/prompts.ts`. When modifying:
- Keep prompts provider-agnostic (no Anthropic-specific features)
- Include ASR error handling instructions for voice-related prompts
- Include roster with name_variants for phonetic matching
- Zod validation should have try/catch fallback to raw output

### Styling
- Dark theme: `bg-zinc-950`, `text-zinc-100`, accent `orange-500`
- Light mode: CSS overrides in `globals.css` (`.light` class)
- Touch targets: minimum 44px (use `h-12` or `py-3 px-4`)
- Add `touch-manipulation active:scale-[0.98]` for tap feedback
- Use `sm:` prefix for desktop overrides (mobile-first)

---

## Commit Message Format

```
feat/fix/improve: [short description]

[Optional longer description]

Co-Authored-By: Claude Code Innovation Agent <noreply@anthropic.com>
```

## Files You Should NOT Modify
- `package.json` (don't add dependencies without good reason)
- `.env.local` (contains secrets)
- `supabase/config.toml`
- `.vercel/` directory
